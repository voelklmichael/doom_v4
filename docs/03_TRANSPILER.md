# Phase 3: Transpilation & Code Generation Plan

This phase translates the annotated Typed AST from Phase 2 into target programming languages, starting with Rust and extensible to .NET (C#).

---

## 🦀 Target 1: Rust Transpiler

### 1. Architectural Choices
- **Memory Model** -- **Decided in detail** (not yet implemented -- corpus-wide surgery, needs its own scoped build-out before struct translation starts). Corpus investigation found two genuinely different pointer categories, deliberately treated differently rather than one uniform arena:
  - **Level geometry** (`sector_t`, `line_t`, `side_t`, `vertex_t`, `subsector_t`, `node_t`, `seg_t`): bulk-allocated once per level (`p_setup.c`'s `sectors = Z_Malloc(numsectors*sizeof(sector_t), PU_LEVEL, 0)`, one shot, freed all at once on level teardown), individual elements never resized/added/removed mid-level. A pointer into this array is valid for the whole level's lifetime -- no aliasing hazard, no reuse. Maps to a plain index newtype (`SectorId(u32)`, etc.) into a `Vec<T>`; no generation counter needed.
  - **Dynamic entities** (`mobj_t` plus the 8 other structs in `p_spec.h` that embed `thinker_t {prev, next, function}` as their first field -- `fireflicker_t`, `lightflash_t`, `strobe_t`, `glow_t`, `plat_t`, `vldoor_t`, `ceiling_t`, `floormove_t`; a 9th, `slidedoor_t`, is `#if 0`'d out dead code in the corpus itself, confirmed by direct read rather than assumed while translating these -- not a real thinker subclass at all): created/destroyed continuously during play, all linked into one polymorphic list (`thinkercap`) and dispatched via a hand-rolled C vtable (calling `function` as a function pointer). `p_tick.c`'s `P_RemoveThinker` doesn't free immediately -- it sets a sentinel (`function.acv = (actionf_v)-1`); the actual unlink+free happens lazily on the *next* pass through `P_RunThinkers`, which is behavior-relevant timing (a just-marked object is still walkable that same tic) and needs to be preserved exactly, not "simplified" away. **Decided (revised while implementing)**: a hand-rolled *append-only* arena, no generation counter and no free-list -- an initial "generational slot arena with reuse" design was reconsidered once actually specifying its `remove`/`run` semantics: the C original never reuses a slot *position* at all (each thinker is its own independent `Z_Malloc`, not array-backed), and `P_RunThinkers`' single forward pass means a thinker spawned mid-tick (appended at the true tail, ahead of the traversal cursor) is visited in that *same* tick -- a free-list that reused an earlier, already-visited index for that new thinker would make it invisible for the rest of the pass, silently diverging from the original. Always appending at the true end avoids this, and removes the ABA hazard a generation counter would otherwise be defending against -- a hazard *introduced* by slot reuse, not present in the original at all. Tombstoned (removed) slots stay dead permanently within one level's lifetime (bounded -- a level's total thinker count over a whole playthrough is a few thousand at most); the whole arena is discarded and recreated fresh at each level load, matching the original's own `PU_LEVEL` wipe-everything-at-once semantics exactly. **Implemented**: `transpiler/src/codegen/runtime/arena.rs` (`Arena<T>`, `Handle<T>`). Its `run` uses take-then-put-back per slot (`Option::take` the value out, call the per-item closure with both `&mut T` *and* `&mut Arena<T>`, put it back unless the closure removed that exact handle) specifically to support the common real case of a thinker removing itself (or another handle) mid-tick without hitting Rust's aliasing rules -- distinguished internally from removing an *other*, not-currently-processing handle (which clears immediately, safe since nothing borrows that slot).
  - **Polymorphism**: ten concrete thinker types is a small, closed, finite set (confirmed by corpus scan, not assumed) -- **decided**: a closed `enum Thinker { Mobj(Mobj), Ceiling(Ceiling), ... }` with one `match` replacing the function-pointer call in the tick loop, not `Box<dyn Thinker>` trait objects (avoids per-thinker heap allocation, and a `match` gets exhaustiveness-checking for free on a set that will never actually grow). The *other* function-pointer table in the corpus -- `state_t.action`, ~132 `A_Chase`/`A_FaceTarget`-style functions -- was originally guessed here to be homogeneous enough for plain `fn` pointers with no enum needed; **measured wrong**: the real corpus has two distinct signatures (`fn(mobj_t*)` and `fn(player_t*, pspdef_t*)`), so it needed the same closed-enum treatment as `Thinker` after all -- see the Doom Action Pointers entry below for the corrected, corpus-checked design.
  - **List fidelity**: **decided** to keep the intrusive doubly-linked-list *shape* (each sector's thing-list, each blockmap cell's thing-list) as index-based prev/next `ThinkerId`/`MobjId` fields rather than swapping to a `Vec`/`HashSet` per sector -- the original's iteration order through these lists is occasionally behavior-visible (collision/interaction resolution order), and the project's validation strategy is tick-for-tick behavioral equivalence, not "morally similar" output, so a container swap that could silently reorder iteration is a real risk, not just a style choice.
  - Self-referential `mobj_t` fields (`target`, `tracer`, `player`, `subsector`) fall out for free once the arena exists -- `Option<MobjId>` / `Option<PlayerId>` / `SubsectorId`.
  - Global game state encapsulation (e.g. `DoomState` struct or safe static modules) -- still open, likely follows from however the arena(s) end up organized.
- **Fixed-Point Arithmetic** -- **Decided and implemented**: `fixed_t` maps to a dedicated newtype, not a primitive alias, so the compiler catches accidentally mixing fixed-point and plain-`i32` arithmetic (impossible in the original C). `transpiler/src/codegen/runtime/fixed.rs` (`FixedT`) mirrors `m_fixed.c` exactly rather than reimplementing "cleaner" semantics -- `fixed_mul` widens to `i64` before shifting (avoiding the intermediate overflow a 32-bit multiply would hit) then truncates back; `fixed_div` saturates to `i32::MIN`/`MAX` via the original's own cheap pre-check rather than detecting overflow after the fact; `fixed_div2` goes through `f64`, matching the original's actually-compiled path (its `long long`-only alternative is `#if 0`'d out in the source itself) and panics past 32-bit range carrying the original's own (slightly misleading -- it's an overflow, not a literal zero divisor) message. Plain `+`/`-`/unary `-` wrap on overflow rather than panic, matching C `int`'s two's-complement behavior in practice rather than Rust's debug-mode default -- needed since `docs/03_TRANSPILER.md`'s own validation strategy is behavior-identical output, not "morally equivalent" output.
- **Enums** -- **Decided and implemented**: plain integer constants (`pub const S_PLAYER: i32 = 5;` or similar), not real Rust `enum`s with `#[repr(i32)]`. Doom's state/animation system does arithmetic directly on enum values (`state + 1`-style sequencing), which a real Rust enum has no direct equivalent for without a conversion shim at every such site. `transpiler/src/codegen/enum_values.rs` (`compute_enum_values`, `render_enum_consts`) computes each variant's actual value from its AST (implicit `previous + 1` sequencing, or a literal-only constant fold for an explicit value) and renders it. Measured first (`examples/enum_value_survey.rs`), matching this project's usual practice before building an evaluator: of the corpus's 1642 enum variants across 43 defining specs, 1592 are implicit and 50 have an explicit value built purely from integer literals (bare, unary-negated, or combined with `+`/`*`, however deeply nested) -- 1638 of 1642 (99.76%) fold cleanly. The remaining 4 (`doomdef.h`'s `INVULNTICS`/`INVISTICS`/`INFRATICS`/`IRONTICS`, all `N * TICRATE`) reference a `#define`d macro rather than a literal; left unresolved rather than pulling in full macro substitution for four variants, matching `visibility.rs`'s own "not worth more machinery for a two-symbol gap" precedent. Not yet done: deciding which Rust module each constant's block of `pub const`s actually renders into (an analogous "owner" question to `source_symbol_imports`, not yet built for this namespace).
- **Structs** -- **All 9 real thinker subclasses fully translated**, including `mobj_t`: `transpiler/src/codegen/struct_fields.rs` (`map_struct_fields`, `render_struct`) translates a C struct's field list to Rust, field by field, failing loudly on anything it doesn't recognize rather than guessing. `slidedoor_t` (`p_spec.h`) is `#if 0`'d out dead code in the corpus itself (confirmed by direct read), so the real count is 9 -- `fireflicker_t`/`lightflash_t`/`strobe_t`/`glow_t`/`plat_t`/`vldoor_t`/`ceiling_t`/`floormove_t`/`mobj_t` -- not the 10 the Memory Model section originally estimated.
  - **Type mapping decided so far** (each field type maps to exactly one of these, chosen for the reason noted): the embedded `thinker_t thinker;` header is dropped entirely (the `enum Thinker` wrapper + `Handle<Thinker>` replace its role) · `fixed_t` -> `FixedT` · `boolean` -> Rust's native `bool`, not `i32` (Doom never does arithmetic on *this* enum the way it does the animation-sequence ones, so the Enums decision doesn't apply to it) · any locally `typedef enum`'d name -> `i32`, resolved generically via `collect_enum_typedef_names` rather than hardcoded per enum · `int`/`short`/`angle_t` (`typedef unsigned`, `tables.h`) -> `i32`/`i16`/`u32` · `sector_t*`/`struct subsector_s*` -> `SectorId`/`SubsectorId` (`runtime/geometry.rs`; level geometry is bulk-allocated once per level and never individually freed, so a plain index needs no generation counter -- corpus-checked non-`Option` for both) · self-referential `struct mobj_s*` (`snext`/`sprev`/`bnext`/`bprev`/`target`/`tracer`) -> `Option<Handle<Thinker>>` (genuinely nullable: list-end sentinels, "no target yet") · `mobjinfo_t*`/`state_t*` -> `&'static MobjInfo`/`&'static State` (`mobjinfo[]`/`states[]` in `info.h` are static, program-lifetime, read-only tables that never get freed or replaced, so a real reference is simpler than another index -- both tables are now actually translated too, see below) · `struct player_s*` -> `Option<PlayerId>` (`runtime/player.rs`; `player_t players[MAXPLAYERS]` in `g_game.c` is a plain fixed-size array, program lifetime not per-level, same reasoning as `SectorId` -- no separate arena needed) · `mapthing_t` (embedded by value, e.g. `mobj_t.spawnpoint`) -> `MapThing`, itself just five `short` fields.
  - **Two Rust-keyword-collision bugs found and fixed** (both caught by tests asserting actual rendered/mapped output, not by inspection): `boolean`'s own `false`/`true` enum constants don't parse and aren't escapable (fixed in `enum_values.rs` -- they're literal tokens, not identifier-class keywords, so the whole enum's constants are simply skipped, which is moot anyway once `boolean` maps to `bool`); and several structs have a field literally named `type` (`pub type: i32,` doesn't parse either), which *is* escapable via `r#type` (a genuine identifier-class keyword) -- `rust_field_name` applies that generically, failing loudly only for the four keywords raw-identifier syntax truly can't escape (`crate`/`self`/`super`/`Self`) plus `true`/`false`.
  - **Parsing headers for their own struct definitions** needed the rough, typedef-table-free scan (`extract_top_level_decls`), not `parse_full`: several of these headers can't parse standalone (e.g. `p_spec.h`'s `extern boolean levelTimer;` needs `boolean` typedef'd by whichever `.c` file includes it first), and even parsing a `.c` file that includes one doesn't splice the header's own struct *definitions* into that file's `TranslationUnit.items` (`parse_full`'s header handling only seeds typedef names, the same as Step 6b's import model).
  - `info: mobjinfo_t*` is observably redundant with `mobj_t.type` corpus-wide (always `&mobjinfo[type]`, per `p_mobj.c`/`p_saveg.c`), but is kept as its own field -- preserving the original's struct shape 1:1 rather than silently restructuring it away.

- **Level geometry (`r_defs.h`) -- all core structs fully translated**: `vertex_t`, `degenmobj_t`, `side_t`, `subsector_t`, `seg_t`, `line_t` (`struct line_s`), `sector_t`, and `node_t` all map completely. New pieces this needed:
  - **Array support, any dimension**: a C array field (`int blockbox[4];`) maps to a Rust array (`[i32; 4]`), reusing `enum_values.rs`'s own `fold_const_int` for the length rather than a new evaluator -- every array length in the corpus fields mapped so far is a bare integer literal, the exact shape that folder already handles. Nested arrays (`node_t`'s `fixed_t bbox[2][4]`) work too (`wrap_arrays`), but needed the dimension order checked directly against the parser's real AST output rather than assumed: `a[2][4]`'s declarator nests as `Array(Array(Ident(a), 2), 4)`, with the *outermost* AST node holding the *last*-written bracket -- which is C's fastest-varying, innermost dimension (`a[2][4]` is "2 rows of 4", i.e. `[[T;4];2]` in Rust, not `[[T;2];4]`). Getting this backwards would have silently transposed every 2D array; a dedicated test locks the correct order in directly, not just incidentally via `node_t`.
  - **`VertexId`/`SideId`/`LineId`** (`runtime/geometry.rs`) join `SectorId`/`SubsectorId`, same "bulk-allocated once per level, never individually freed, no generation counter" reasoning. A cross-referencing type is sometimes spelled two ways in the corpus for the same underlying type -- `line_t*` (typedef form, e.g. `seg_t.linedef`) vs. `struct line_s*` (tag form, e.g. inside `line_t`'s own neighboring fields) -- both map to `LineId`; same pattern already established for `mobj_t`/`struct mobj_s`.
  - **Three name-based (not type-based) special cases, all corpus-verified before adding**: `specialdata: void*` (`sector_t`/`line_t`) always holds a `ceiling_t*`/`plat_t*`/`vldoor_t*`/`floormove_t*` back-reference (checked for truthiness before starting a new mover, reset to `NULL` when done -- `p_ceilng.c`/`p_plats.c`/`p_floor.c`) -> `Option<Handle<Thinker>>`. `backsector: sector_t*` (`line_t`/`seg_t`) is genuinely nullable for one-sided lines -- `if (!ld->backsector)`/`if (!backsector)` (`p_map.c`, `r_bsp.c`, `p_maputl.c`) -- unlike its sibling `frontsector`, dereferenced with no guard anywhere in the corpus, so only `backsector` gets `Option<SectorId>`. `lines: struct line_s** lines;` (`sector_t`) is a *dynamically*-sized array (`// [linecount] size` in the header itself -- a pointer to a heap-allocated array of pointers, sized by the sibling `linecount` field, not by any fixed declarator dimension) -> `Vec<LineId>`, the standard translation for that C idiom; `linecount` itself stays its own field regardless, redundant with `lines.len()` once `lines` is a real `Vec` -- same "preserve the original's struct shape 1:1" call already made for `mobj_t.info`'s redundancy with `.type`. All three are scoped to their specific field name, not to `void*`/`sector_t*`/`T**` in general, since a blanket type-only rule would be wrong for some other field this step hasn't individually checked.
  - **A real bug caught before shipping**: `backsector` was initially mapped as a bare `SectorId`, matching every other `sector_t*` field mapped so far -- the corpus check (done *because* this project checks nullability before deciding `Option` vs. bare, not after) surfaced the `frontsector`/`backsector` asymmetry, fixed in the same commit rather than shipped and found later.

- **The `Thinker` enum -- implemented, shape only**: `transpiler/src/codegen/thinkers.rs` renders `pub enum Thinker { FireFlicker(FireFlicker), ..., Mobj(Mobj) }` wrapping all 9 translated structs, plus a stub `match` dispatch (one arm per variant, `todo!()` body) replacing the original's hand-rolled C vtable (`thinker_t.function`, called as a function pointer). **Deliberately not real dispatch logic**: every Phase 3 step so far -- module graph, `use` blocks, enum constants, struct fields, this enum -- has been data *shape*, never function-body logic. Translating an actual tick function (e.g. `T_FireFlicker`'s random-timer logic) needs C-statement/expression-to-Rust transpilation, which nothing in this codebase does yet -- a large, separate undertaking, deliberately not started here; guessing the dispatch's real signature (what context a tick needs -- world state, the arena itself, ...) before any real body exists to check it against would just mean redoing it later. Variant names (`FireFlicker`, `VerticalDoor` for `vldoor_t` matching the original's own `T_VerticalDoor` rather than the struct's more cryptic name, ...) are picked explicitly per struct -- no automatic snake_case_t -> PascalCase converter exists, and wouldn't even help for a name like `fireflicker_t` (no underscore to split on).

- **Module placement for translated types -- implemented**: `transpiler/src/codegen/type_placement.rs` decides which Rust module each translated struct/enum lives in, and renders the `use` imports one needs for its own fields' types. Two kinds of home: a corpus-translated type goes wherever its C definition actually lives (confirmed against the real module graph, not assumed -- `p_spec.c` exists, so `p_spec.h`'s thinkers are in the `p_spec` `Source` module, not `HeaderOnly`; `r_defs.h`/`doomdata.h` have no matching `.c`, so `HeaderOnly`); `Thinker` itself goes in `p_tick`, where the original's own thinker-list management (`P_InitThinkers`/`P_AddThinker`/`P_RemoveThinker`/`P_RunThinkers`) already lives, since the enum + dispatch replace exactly that mechanism. A runtime support type (`FixedT`, `Handle`, the `*Id` newtypes, ...) goes in its own `runtime` module. Imports are computed by *scanning* each struct's already-rendered field-type strings for known type names (`Option<Handle<Thinker>>` -> tokens `Option`/`Handle`/`Thinker`, look each up, `Option` isn't in the table so it's silently skipped) rather than hand-writing an import list per struct -- scales to every struct this module or a future one translates. A referenced type not yet in the table (`MobjInfo`/`State`) is simply not imported: the same documented gap as before, not guessed at here either. Still just text + tests -- no file has been written to disk anywhere in this project yet; that's its own separate step, not started.

- **`MobjInfo`/`State` (`info.h`) -- struct shapes translated**: `mobjinfo_t` maps completely -- every field is a plain `int` (several are semantically state indices -- `spawnstate`, `seestate`, ... -- but declared as bare `int` in the corpus itself, not `statenum_t`, so they map as plain `i32` too, matching the field's actual declared type rather than its intent). `state_t` needed `long` (new -- bit-identical to `int` on this project's ILP32 target, so it maps to the same `i32` rather than a separate newtype), and maps `sprite`/`frame`/`tics` before stopping at `action: actionf_t` -- Doom's *other* function-pointer mechanism (the state-table action dispatch, see **Doom Action Pointers** below), a separate question from the `Thinker` enum's own function-pointer replacement. `MobjInfo`/`State` are registered in `type_placement.rs` (home module `info` -- `info.c` exists, so `Source` like `p_spec`/`p_mobj`/`p_tick`, confirmed against the real module graph), closing the "referenced but has no home module" gap `Mobj.info`/`.state` had.
- **`mobjinfo[]` -- the first real *data* table, not just shape, implemented**: `transpiler/src/codegen/mobjinfo_data.rs` renders `info.c`'s `mobjinfo_t mobjinfo[NUMMOBJTYPES] = { {...}, ... };` (137 positional struct-literal entries, 23 values each) as a real `pub static MOBJINFO: [MobjInfo; 137] = [...];`. `info.c` parses cleanly via `parse_full` -- unlike the header-only files struct translation has needed the rough scan for, it's a `.c` file with a complete typedef context via its own `#include` graph, so this is the first piece of Phase 3 to walk a real `Initializer::List` directly, no macro expansion needed. Every value in the whole table is one of three shapes (confirmed by scanning the section for any identifier not already accounted for -- only `FRACUNIT` and the array-size `NUMMOBJTYPES` turned up beyond enum constants): a plain integer literal, rendered as-is; an enum constant name (`S_PLAY`, `sfx_None`, `MF_SOLID`, ...), resolved *symbolically* via a corpus-wide `name -> (home module, value)` index (reusing `enum_values.rs`'s `compute_enum_values`, not folded to a bare integer -- the whole point is generated code that still reads like the original, not a disassembly of it) with correct cross-module imports computed alongside it; or a `FRACUNIT`-scaled expression (`16*FRACUNIT`), special-cased by name to `runtime::FRACUNIT.0` (the field's declared type is plain `int`, not `fixed_t`, so the value needs the wrapped `i32`, not the `FixedT` constant itself). **Verified compiling for real**, not just unit-tested in isolation: assembled the full table alongside the actual rendered `sfxenum_t`/`mobjflag_t`/`statenum_t` constants (`enum_values.rs`'s existing renderer, a different consumer than it had before) into one file and compiled it with `rustc` -- zero errors (a handful of `non_upper_case_globals` style lints for Doom's lowercase `sfx_`/`mus_` names, not a correctness issue).
- **`state_t` now maps completely too**, once `action: actionf_t` had a representation (below).
- **Doom Action Pointers -- decided and implemented**: `transpiler/src/codegen/action_fn.rs` renders `pub enum ActionFn { Mobj(fn(&mut Mobj)), Weapon(fn(&mut Player, &mut PlayerSpriteState)) }` -- the same pattern already used for `thinker_t.function`'s own dispatch union (`thinkers.rs`'s `Thinker`), a closed enum replacing caller-blind-trust with a `match`, not a raw/unsafe translation of the C union. **Two variants, not three**: corpus-wide, every `.acv` (the union's zero-arg shape) reference turned out to be about `thinker_t.function`'s own remove-sentinel (`(actionf_v)-1`), already fully replaced by the `Arena`'s append-only design -- `state_t.action` itself is only ever `.acp1` or `.acp2`, confirmed against all 132 real `A_*` action functions (`info.c`/`p_enemy.c`/`p_pspr.c`): every one is either `fn(mobj_t*)` (the large majority) or `fn(player_t*, pspdef_t*)` (a handful -- `A_ReFire`, `A_Light0/1/2`, `A_WeaponReady`, `A_Lower`, `A_Raise`, the weapon-sprite state chain). `Player`/`PlayerSpriteState` (`player_t`/`pspdef_t`) are registered in `type_placement.rs` (`d_player`/`p_pspr`) even though neither is translated as a real struct yet -- the same forward-reference precedent `MobjInfo`/`State` set from `Mobj` before they existed. **Deliberately loose on the exact parameter list** (just `&mut Mobj`, not yet `&mut Mobj` plus arena/world-state access) -- same reasoning as the `Thinker` dispatch stub: not decidable without a real function body to check it against, so pinned to the minimal literal translation for now rather than guessed. `actionf_t` maps to `Option<ActionFn>` (nullable -- `states[]`'s own data initializes plenty of entries `{NULL}`).
- **`states[]` -- implemented, the table pair with `mobjinfo[]` now complete**: `transpiler/src/codegen/states_data.rs` renders `info.c`'s `state_t states[NUMSTATES] = { {...}, ... };` (967 positional entries, 7 values each) as `pub static STATES: [State; 967] = [...];`, reusing `mobjinfo_data.rs`'s `render_value_expr`/`build_constant_index`/`rough_scan` directly for six of the seven fields. Only `action` needed its own renderer: `actionf_t` is a *union*, so its own C initializer is a nested single-element compound literal (`{NULL}`/`{A_Function}`) -- confirmed directly against the parser's real output, not assumed (`NULL` stays `Expr::Ident("NULL")`, never macro-expanded to a literal). Deciding which `ActionFn` variant a name needs required its own small index: `info.c`'s own forward declarations of the 132 action functions (`void A_Foo();`) use C89's "unspecified arguments" `()` shape, which can't tell arity apart, so `build_action_function_index` reads the *real* definitions in `p_enemy.c`/`p_pspr.c` instead. **A function's home file doesn't predict its shape** -- confirmed empirically after an initial wrong assumption caught by the tests themselves: `A_Punch` (assumed `p_enemy.c`/`fn(mobj_t*)`-shaped while writing the first draft of the tests) is actually defined in `p_pspr.c` as `fn(player_t*, pspdef_t*)` (it's the player's own punch-weapon action, not a monster melee action); `A_OpenShotgun2`/`A_LoadShotgun2`/`A_CloseShotgun2` are the reverse case, defined in `p_enemy.c` but genuinely `fn(player_t*, pspdef_t*)`-shaped (the double-barrel shotgun's own weapon state chain) -- so shape is always resolved by a function's own real parameter count, never guessed from which file happened to define it. **Verified compiling for real**: a representative subset (a no-action entry, a `Mobj`-shaped action, a `Weapon`-shaped action, plus the `dispatch` pattern-match consuming them) compiled clean with `rustc`, alongside stub functions matching each real signature.

- **Function-body transpilation -- started, fourteen real functions translated**: `transpiler/src/codegen/function_body.rs` renders real C statements/expressions as Rust, starting narrowly from one concretely verified function (`T_FireFlicker`, `p_lights.c`) rather than attempting a general C-to-Rust statement transpiler -- `render_stmt`/`render_expr` only recognize the AST shapes actually seen so far (declarations, `if`/`else`, `return`, assignment, member chains, binary/unary arithmetic, calls), failing loudly on anything else, matching every other renderer's discipline. **Confirms the cross-reference problem the `Thinker` dispatch stub's signature deliberately left open**: `flick->sector->lightlevel` dereferences what used to be a real C pointer, but the already-translated `FireFlicker.sector` field is a `SectorId` (a plain index, per the Memory Model decision) -- resolving it back to a real `&mut Sector` needs somewhere to look sector storage up, not just `&mut self`. **`World` -- implemented** (`transpiler/src/codegen/world.rs`): a minimal, hand-rendered (not corpus-mapped, like `Thinker` itself) `struct World { pub sectors: Vec<Sector> }` with `Index`/`IndexMut<SectorId>`, letting a tick body write `world[flick.sector].lightlevel = ...` directly. Lives in `p_tick` alongside `Thinker`, for the same reason -- new, invented infrastructure with no direct corpus counterpart, replacing part of the original's own tick-loop bookkeeping. **`Thinker::tick`'s signature revised** from `fn tick(&mut self)` to `fn tick(&mut self, world: &mut World)`, confirmed necessary (not just anticipated) by this first real body -- match arms stay `todo!()` for now, since wiring a real body into the dispatch also needs the function's own module placement settled (`p_lights.c` vs. `p_spec`'s own `Source` module -- not yet decided) and disk emission to exist, not just the signature. `if (--flick->count) return;` -- Rust has no prefix `--` -- is handled by hoisting the decrement into its own statement immediately before the `if`, matching C's evaluation order (decrement first, then the result is tested); binary-expression rendering is precedence-aware (a small precedence table, parens inserted only where Rust's identical-to-C precedence would otherwise change meaning), not blanket-parenthesized. **Generalized without changes**: `T_LightFlash`/`T_StrobeFlash` (`p_lights.c`'s other two simple light-effect thinkers) rendered correctly against the *unmodified* renderer on the first attempt -- confirming `is_comparison_or_logical` (needed here for `==`, not just `T_FireFlicker`'s `<`) and the precedence-aware parenthesization (`(P_Random() & flash.mintime) + 1`, correctly parenthesized) generalize rather than being hand-tuned to the one function they were built against. **`T_Glow` -- the first genuinely different control-flow shape**: `switch(g->direction) { case -1: ...; break; case 1: ...; break; }`, needing real `switch`/`case` support (`render_switch`) plus `Expr::Unary` (the `case -1:` label) for the first time. C parses `case N: stmt` as a labeled statement wrapping only the *one* statement immediately after the colon -- everything else in that case's intended body is a flat sibling in the same enclosing block, up to the next label or a `break` -- confirmed directly against the real parsed AST (not assumed) before designing the re-grouping `render_switch` needed to turn those flat siblings back into one Rust `match` arm's block per case. Requires each arm to end in an explicit `break` before the next label (C's implicit fallthrough isn't recognized yet -- no real function has needed it); a `switch` with no `default:` gets an implicit trailing `_ => {}` arm, matching C's own "no case matched, do nothing" semantics for a plain-integer subject. **`P_Spawn*` constructor functions -- a genuinely different idiom, not just new statement/expression shapes** (`render_spawn_fn`): `P_SpawnFireFlicker`/`P_SpawnLightFlash`/`P_SpawnGlowingLight` (`p_lights.c`) each follow the same `Z_Malloc` + `P_AddThinker` + field-by-field imperative fill-in idiom that every dynamic-entity spawn function in the corpus uses (confirmed by survey: `p_doors.c` has two more following it, not yet attempted). This becomes one `Thinker::Variant(Struct { ... })` literal handed to `Arena::insert` in a single call -- the `var->thinker.function.acpN = (cast) FnName;` line needs no translation at all, since the enum variant tag already encodes it (the same substitution `Thinker` itself makes on the dispatch side). Every constructor field becomes its own `let field = expr;`, in original source order, rather than a flat struct-literal entry -- **a real bug caught before shipping**: an early flat-literal version rendered `P_SpawnLightFlash`'s `flash->count = (P_Random()&flash->maxtime)+1;` as `flash.maxtime` (referencing a variable that doesn't exist in the translated output, since `flash` was never rebuilt as a real local), missing that a later field can legitimately read an *earlier* field of the same not-yet-finished struct back; `let`-per-field in source order fixes it for free, since `maxtime` then resolves to the already-bound local, and shorthand field-init falls out the same way. Reordering *is* still sound here (every field write groups at the final `insert` call, wherever its assignment fell in the original) only because C's single-threaded, synchronous execution means nothing ever observes the value mid-construction. **`P_SpawnStrobeFlash` -- two more field-construction idioms, same increment**: `minlight` is unconditionally computed and then *conditionally overridden* (`if (flash->minlight == flash->maxlight) flash->minlight = 0;`) -- needed a small pre-scan (`count_ctor_field_assigns`, walking into `if`/`switch` bodies too, not just top-level statements) to mark a field's initial `let` as `mut` whenever it's assigned more than once anywhere in the function; the override statement itself then needed *no* special-case rendering at all, since `FnBodyContext::ctor_var`'s field resolution already applies uniformly inside the ordinary `render_stmt`/`if` path. `count` is a step further -- it has *no* unconditional assignment anywhere, decided entirely by `if (!inSync) flash->count = (P_Random()&7)+1; else flash->count = 1;` -- recognized by `if_else_ctor_field_assign` (both branches assigning the same not-yet-bound field) and rendered as one `let count = if cond { .. } else { .. };`. Its condition, `!inSync` on a plain `int` parameter, needed a new shared `render_bool_expr` (factored out of `render_condition`): C truthiness on a non-`bool` value, so `!x` becomes `x == 0`, not Rust's own `!` (which would silently compile as *bitwise* NOT on an integer -- a real, wrong-behavior trap this renderer has no per-identifier `bool`-vs-`int` tracking to otherwise catch, so the C-truthiness reading is applied unconditionally for now). **`p_doors.c`'s door spawners -- the constructor back-reference idiom, plus two real bugs caught before shipping**: `P_SpawnDoorCloseIn30`/`P_SpawnDoorRaiseIn5Mins` both do `sec->specialdata = door;` -- a back-reference to the constructed value itself, resolvable only once it's a real `Handle<Thinker>`. `render_spawn_fn` now recognizes this (`is_ctor_var_backreference`) and switches the *whole* function to a two-phase render whenever it appears anywhere in the body: every constructor field first (as before), then `let handle = thinkers.insert(..);`, then every remaining "other" statement rendered afterward with a bare `ctor_var` resolving to `handle` (a new `FnBodyContext::ctor_var_handle_name`) -- `p_lights.c`'s four spawners are untouched by this, since their single-phase interleaved rendering is unaffected when no back-reference exists. Two real, distinct bugs surfaced and got fixed working through this, both caught before shipping rather than only at the eventual `rustc` compile: (1) `specialdata` maps to `Option<Handle<Thinker>>` (`struct_fields.rs`'s own name-based special case), so the back-reference needs `Some(handle)`, not a bare `handle` -- an early version emitted the bare form; (2) `P_SpawnDoorRaiseIn5Mins`'s `door->topheight = P_FindLowestCeilingSurrounding(sec); door->topheight -= 4*FRACUNIT;` refines a field via a *compound* assignment right after its own initial value -- an early version's two-phase logic deferred this refinement to *after* the `insert` alongside the back-reference (since it wasn't a plain `=` `ctor_field_assign`), which would have inserted the pre-refinement value; fixed by recognizing any assignment operator targeting a ctor field (`ctor_field_assign_target`, used only for the `mut` pre-scan and this ordering decision) and always rendering such a refinement inline, before the insert, regardless of whether the function has a back-reference elsewhere. **A third, structural gap found and handled by design, not a bug**: `P_SpawnDoorCloseIn30` genuinely never sets `topheight`/`topwait` in the original C (left as whatever `Z_Malloc` returned). `render_spawn_fn` now takes the constructed struct's own field list (`ctor_field_types`, the same `MappedField`s `struct_fields.rs` produces) purely to check completeness, erring loudly by name on any field the function never assigns rather than emitting an incomplete literal that would only fail later, confusingly, at the eventual `rustc` stage -- `P_SpawnDoorCloseIn30` is the first (and, so far, only) function this rejects. **Investigated further and resolved as a permanent rejection, not a temporary gap**: whether `topheight`/`topwait` are ever actually *read* back for a door spawned this way turns out to be answerable now that `T_VerticalDoor` (below) is fully translated. Tracing it: `P_SpawnDoorCloseIn30` sets `direction = 0` (WAITING) and `type = normal`; the WAITING state counts down and transitions to `direction = -1` (DOWN), which reads `sector->floorheight`, not `topheight` -- so far so good. But if the door is *crushed* while closing, `T_VerticalDoor`'s own `crushed` handling checks `door->type` against `blazeClose`/`close` only ("DO NOT GO BACK UP!" -- the corpus's own comment); `normal` isn't either, so it falls to the `default:` arm, reverses to `direction = 1` (UP), and the very next tick reads `door->topheight` directly as `T_MovePlane`'s destination. **This is a genuine latent bug in the original C**, not a translation gap: `Z_Malloc` (`z_zone.c`) is a carve-from-a-fixed-pool allocator with no zeroing anywhere in it (confirmed by reading its full implementation, not assumed) -- it returns whatever memory was last there, so a crushed `P_SpawnDoorCloseIn30` door reads genuinely uninitialized heap memory as its target height, real undefined behavior reachable (if rarely, and perhaps never in practice depending on level design) in real gameplay. Since there is no well-defined C value to be faithful *to* here -- any Rust default (`0`, or anything else) would be a fabrication the original never actually specified -- **`render_spawn_fn`'s rejection is the correct, permanent answer for this function, not a placeholder for a future fix**: there is no faithful value to fill in, so refusing to guess is the honest translation, not an incomplete one. **`EV_StartLightStrobing` -- a third kind of function body, `render_trigger_fn`**: no `self`-struct dispatch (`render_fn`) and no field construction (`render_spawn_fn`) -- a plain "trigger" function that iterates tagged sectors and spawns thinkers into them, needing `world`/`thinkers` like a constructor but none of its field-synthesis logic (`FnBodyContext::ctor_var` stays empty). Reuses a newly-factored `render_params` helper (previously duplicated inline in `render_spawn_fn`). Four new pieces surfaced by this one function, none needed by any tick/constructor body so far: (1) `while ((secnum = P_FindSectorFromLineTag(line,secnum)) >= 0)` -- Doom's tagged-object iteration idiom, an assignment used as the loop's own per-iteration test -- Rust's `while` can't re-run a hoisted statement before each fresh condition check the way `render_condition`'s `--x`-as-condition hoist can (that one only fires once), so `render_while` restructures the whole loop into `loop { <hoisted assignment>; if !(<test>) { break; } <body> }` instead, matching C's own re-evaluate-every-pass semantics. (2) `&sectors[secnum]` -- `sector_t*` already maps to `SectorId` (a plain index), so getting one by index needs no real address-of or array-index operation at all, just `SectorId(secnum as u32)` -- a narrow, name-based special case (`sectors` specifically, matching this codebase's usual style of scoping such rules to the one real identifier they've been confirmed against). (3) `if (sec->specialdata) continue;` -- a bare value used for C truthiness where the value is `Option`-typed (`specialdata` again), needing `.is_some()`, not the `== 0` every other (plain `int`) truthiness check gets; `Stmt::Continue` itself is a trivial new `render_stmt` arm. (4) `render_decl` generalized from "only a bare `int` declaration" to also accept a single-pointer already-known-type local (`sector_t* sec;`) -- Rust's deferred-`let` inference (`let mut sec;`, typed later from use) needs no annotation regardless of which C type it was, so the same rendering already works for both once the `int`-only restriction is lifted. **Verified compiling for real**: all four tick functions, all five successfully-rendered spawn functions, and the new trigger function were assembled into one file (alongside `World`/`Thinker`/stand-in `Arena`/`Handle`/`FixedT` arithmetic) and compiled clean with `rustc` -- the trigger function's own call to `P_SpawnStrobeFlash` deliberately used a 3-argument stub matching the *original* C signature rather than the real, now-5-parameter translated one, the same already-documented cross-function-wiring gap every other forward-referenced call in this module has had since `T_FireFlicker`'s own `P_Random()`. Eleven hand-verified exact-match tests (`test_t_fire_flicker_renders_exactly`, `test_t_light_flash_renders_exactly`, `test_t_strobe_flash_renders_exactly`, `test_t_glow_renders_exactly`, `test_p_spawn_fire_flicker_renders_exactly`, `test_p_spawn_light_flash_renders_exactly`, `test_p_spawn_glowing_light_renders_exactly`, `test_p_spawn_strobe_flash_renders_exactly`, `test_p_spawn_door_raise_in_5_mins_renders_exactly`, `test_ev_start_light_strobing_renders_exactly`, plus `test_p_spawn_door_close_in_30_detects_missing_fields` for the rejection) lock the real parsed AST's rendering (or the expected rejection) against exact expected text, not just a shape check. **`T_VerticalDoor` (`p_doors.c`) investigated, deliberately not attempted whole**: significantly larger than anything translated so far -- nested `switch`es where multiple `case` labels share one body (`case blazeRaise: case blazeClose: ...`, a different shape from `render_switch`'s current one-label-per-arm assumption), self-removal via `P_RemoveThinker(&door->thinker)` (needing `Thinker::tick`'s own signature revised to carry a `Handle<Thinker>` and `&mut Arena<Thinker>`, matching `Arena::run`'s own closure shape -- a consequential change on the same footing as `World`'s own introduction, not made speculatively), and `door->sector->specialdata = NULL` (needing a general `NULL` -> `None` mapping, not yet handled). Only the one clean, narrow, broadly-reusable piece was pulled out and implemented: `if (!--door->topcountdown)` -- the same countdown-to-zero idiom as `T_FireFlicker`'s bare `--flick->count`, just negated (testing for zero via `!`, not nonzero) -- confirmed extremely common corpus-wide (`p_plats.c`, `wi_stuff.c`, `st_stuff.c` all use it for their own countdown timers), and cheap to add given `render_condition`'s existing `PreIncDec`-hoisting machinery just needed factoring out (`hoist_pre_inc_dec`) and reusing for the negated case. Tested against the real sub-expression extracted directly from `T_VerticalDoor`'s own parsed AST (`test_negated_pre_dec_condition_against_real_t_vertical_door`) -- real, corpus-verified C, not a fabricated snippet -- rather than deferring verification until the whole function can be attempted. Two more pieces from the same function followed the identical isolated-extraction approach (a small reusable `find_stmt` test helper now walks into `if`/`switch`/`case`/`default`/`while` bodies to pull one real statement out of a large function without hand-indexing its exact nesting): **`NULL` -> `None`** -- `door->sector->specialdata = NULL;` (a mover clearing the sector's back-reference to itself once it finishes) generalizes the `Some(..)`-wrapping `render_expr_stmt` already had for a constructor's back-reference into a symmetric pair, both keyed off the same `specialdata` name-based special case (`test_null_to_none_against_real_t_vertical_door`). **Self-removal** -- `P_RemoveThinker(&door->thinker);` (a tick function removing itself) is recognized (`is_self_removal_call`, matching only this exact "remove my own handle" shape, not some other handle) and rendered as `arena.remove(handle)`, using fixed `handle`/`arena` names the same way `world`/`thinkers` are already fixed elsewhere (`test_self_removal_against_real_t_vertical_door`). **Deliberately not done alongside this**: threading real `handle: Handle<Thinker>, arena: &mut Arena<Thinker>` parameters through `render_fn`'s own generated signature (and the matching `Thinker::tick` dispatch-stub revision) -- both statement shapes are now individually proven correct in isolation, but committing to *how* a tick function's signature should grow to carry them (always, or only when the body actually self-removes?) is a real design decision better made once a complete function can drive it end-to-end, not speculatively from one statement alone. **Shared `case` labels** -- `render_switch` extended (`collect_case_labels`) to handle multiple labels covering one body (`case blazeRaise: case blazeClose: <shared body>`): C parses each `case X:` as a label wrapping only the *next* statement, so consecutive labels with nothing but another `case` between them nest (`Case{X, stmt: Case{Y, stmt: <real body>}}`) rather than appearing as flat siblings -- confirmed directly against the real parsed AST before writing the peeling logic, matching this codebase's usual discipline. Peeled labels join into one Rust or-pattern (`blazeClose | close => { .. }`). One more real wrinkle the actual corpus surfaced: a group of labels can peel all the way down to a bare `break;` with *nothing* in between (`case blazeClose: case close: break;` -- Doom's own "these variants intentionally do nothing" idiom) -- `collect_case_labels` hands that back as the peeled `first_stmt` itself rather than a separate sibling, so `render_switch` now recognizes when its own peeled `first_stmt` *is* `Stmt::Break` and renders an empty arm instead of mistakenly treating `Break` as a body statement. **A second real bug caught working through this**: self-struct field access (`door->type`, read through `self_field_types`/`extra_cross_ref_idents`, not the constructor `ctor_var` path) never escaped a keyword-colliding field name at all -- only the constructor-field path called `rust_field_name`, so `door->type` would have rendered as the syntactically-invalid `door.type` instead of `door.r#type`. Fixed by applying `rust_field_name` unconditionally to every `Member` field render, not just the constructor one -- harmless for the overwhelming majority of non-colliding field names, and correct now for every path. Both new pieces are tested by cloning the exact real `Case`-chain/condition subtrees out of `T_VerticalDoor`'s own parsed AST and re-wrapping them in a synthetic `Switch`/`Compound` (`test_shared_case_labels_against_real_t_vertical_door`) -- `T_VerticalDoor` still can't render end-to-end (`S_StartSound`'s `(mobj_t *)&door->sector->soundorg` type-punning cast is a separate, unrelated, not-yet-investigated gap), so this is real corpus C, verified in isolation, the same way the `NULL`/self-removal pieces were. **`T_VerticalDoor` -- closed out, fully translated end-to-end**: the last blocker, investigated fresh rather than guessed at, turned out much smaller than feared. `S_StartSound`'s *real* declared signature is `void* origin` (`s_sound.h`), not `mobj_t*` -- so the `(mobj_t *)` cast in `(mobj_t *)&door->sector->soundorg` is C's own type-erasure noise with no real value transformation behind it (any pointer converts to `void*` implicitly in C), not a genuine conversion needing modeling. This let `Expr::Cast` render as a pure passthrough (drop the cast, keep the inner expression), matching the precedent already set by eliding the `(actionf_p1)` cast around action-function pointers in `ActionFn`'s own design. `sector_t.soundorg` itself is a plain embedded `degenmobj_t` *value* field (not a cross-reference index), so `&door->sector->soundorg` needed no pointer trick at all -- just an ordinary Rust `&` reference, so `Expr::Unary(AddrOf)` gained a general fallback (real `&` reference) alongside its existing narrow `&sectors[i]` special case. With both in place, the *entire* function rendered on the very next attempt, no other gaps found. **`render_fn`'s signature-extension logic, now real**: `body_has_self_removal` recursively scans a tick function's body (through `if`/`switch`/`case`/`default`/`while`, since `T_VerticalDoor` buries several `P_RemoveThinker` calls two `switch` levels deep) and only adds `handle: Handle<Thinker>, arena: &mut Arena<Thinker>` to a function's own generated signature when its body actually self-removes -- confirmed against this first complete self-removing function rather than added speculatively to every tick function ahead of real evidence, resolving the design question left open two commits ago. `Thinker::tick`'s own dispatch-stub signature is revised the same way, gaining `handle`/`arena` to match `Arena::run`'s own closure shape (`FnMut(&mut T, Handle<T>, &mut Arena<T>)`) exactly, since the dispatch needs them on hand to route through to whichever variant's own function needs them (not every variant does). **Verified compiling for real**: `T_VerticalDoor`'s full output, assembled with real `World`/`Thinker`/`Arena`/`Handle`/`DegenMobj`/`FixedT` shapes and stub `T_MovePlane`/`S_StartSound` (matching `S_StartSound`'s real `origin: &DegenMobj` parameter, not the vestigial cast), compiled clean with `rustc` -- zero errors, only expected style lints. `test_t_vertical_door_renders_exactly` locks the whole function's real parsed-AST rendering against exact expected text, joining the earlier isolated-fragment tests (kept, not removed, since each still pinpoints one specific piece/bug on its own). **`T_MoveFloor` (`p_floor.c`) -- a real `switch` fallthrough, resolved narrowly**: structurally close to `T_VerticalDoor` (self-removal, `NULL` -> `None`, the sound-cast/reference pair, `r#type`), reusing all of it unchanged, but with a genuine C fallthrough in two places: `case donutRaise: <two statements>; default: break;` -- `donutRaise` has *no* `break` of its own, so execution falls into `default`'s. Since `default`'s own body is just `break;` (no statements at all), the fallthrough has no observable effect either way -- confirmed directly against the real parsed AST before relaxing anything, not assumed. `render_switch` recognized falling into an empty `default: break;` specifically as safe and rendered the case as its own complete arm (a narrow carve-out at the time, later fully generalized -- see `T_MoveCeiling` below). Also confirms `!(leveltime&7)` (`Unary::Not` over a `Binary`, not `PreIncDec`) already renders correctly with zero new code, since `render_bool_expr`'s `Not` case never assumed its operand's shape -- and that Rust's `&`-binds-tighter-than-`==` precedence matches C's exactly, so `leveltime & 7 == 0` needs no parens (confirmed compiling as a real `bool`, not just visually plausible). **Verified compiling for real** alongside real `World`/`Thinker`/`Arena`/`Handle`/`DegenMobj` shapes and the same `S_StartSound`/`T_MovePlane` stub convention as `T_VerticalDoor`. `test_t_move_floor_renders_exactly` locks the whole function against exact expected text.

- **Real `Arena` read access from a `Mobj`-shaped action function -- implemented**: the architectural gap flagged at the end of the previous session (every already-translated function touching `actor->target`/`.tracer` only checked truthiness or passed it opaquely, never dereferenced *through* it). `render_fn` now adds a `thinkers: &Arena<Thinker>` parameter (`body_has_target_deref`, a real recursive expression-tree walk, not a single fixed statement shape like the self-removal check) whenever a body's `Expr::Member` chain actually needs one; a new `render_expr` arm resolves `actor->target->field` via `thinkers.get(actor.target.unwrap())`, corpus-checked safe to `unreachable!()` on `None` since only `mobj_t` ever has `target`/`tracer`. **Generalized to local aliases** (`A_SkullAttack`'s `dest = actor->target; ... dest->x ...` idiom, common corpus-wide): `is_target_tracer_typed` also accepts a bare local registered `Option<Handle<Thinker>>`-typed by a new `collect_target_tracer_aliases` scan (single-level only -- an alias of an alias has no corpus example so far), feeding the same `FnBodyContext::extra_cross_ref_idents` map a trigger function's own locals already use. Two real, independently-confirmed bugs surfaced while proving this against real functions (`A_FaceTarget`, `A_CPosRefire`/`A_SpidRefire`, `P_CheckMeleeRange`), both caught by actually compiling with `rustc`, not just the text-comparison tests: (1) `render_binary_operand`'s `&&`/`||` operands render through the *generic* `render_expr` path, not `render_bool_expr` -- so `!actor->target` used as one operand of a logical chain needed its own `Option`-aware arm added directly to `render_expr`'s own `Unary::Not` handling (`render_bool_expr`'s existing top-level entry point already had this, but that path is bypassed inside a chain). (2) `render_bool_expr`'s own top-level `Unary::Not` handling had no awareness that some callees (`P_CheckSight`, matching the already-known `P_CheckMeleeRange`) return a real `boolean`/`bool`, so `if (!P_CheckSight(..))` rendered as `P_CheckSight(..) == 0` -- doesn't type-check once the callee is a real `bool` -- fixed via a new shared `is_bool_returning_call` helper. Also new: `render_bool_fn`, a thin `-> {return_type}` wrapper over the same underlying `render_fn_impl` every `void A_*` function already shares (added without touching `render_fn`'s ~36 existing always-void call sites), used for `P_CheckMeleeRange`, the first `boolean`-returning function body this renderer produces. **Deliberately not pursued further this session** (each is its own separately-scoped architectural gap, not folded in speculatively): `A_SkullAttack` itself isn't closed out end-to-end -- it has an unrelated gap where a plain-`int`-declared local (`dist`) is assigned `P_AproxDistance`'s `FixedT`-typed result and later compared/reassigned as if still `int`; `A_Tracer`/`A_VileTarget`/`A_PainShootSkull` all need `P_SpawnMobj`-style "write into a newly created Mobj's own fields via a fresh `Handle<Thinker>`" support, the natural next architectural piece after this one; `A_Look`/`A_KeenDie` need `goto`/label support and thinker-list iteration respectively, neither built yet.

**Real `Arena` *write* access via `P_SpawnMobj` -- implemented, closing out the gap flagged at the end of the previous entry**: `A_Tracer`/`A_VileTarget`'s idiom (`th = P_SpawnMobj(...); th->field = ...;`) assigns a fresh handle to a local and then reads/writes its fields afterward -- `P_SpawnMobj` itself stays a forward-reference stub (same as `S_StartSound`/`P_Random`), only its call site's return value needed a real type. Decided narrowly, by name, matching this module's own established style: `collect_spawn_mobj_locals` (mirroring `collect_target_tracer_aliases`'s own shape) registers any local directly assigned from `X = P_SpawnMobj(...)` as `"Handle<Thinker>"`-typed (bare, not `Option`-wrapped -- no real corpus call site ever null-checks the result) into the same `FnBodyContext::extra_cross_ref_idents` map `thing`'s own parameter type already uses. `render_expr`'s existing `thing->player`-only `Handle<Thinker>` read arm is generalized to any field (`_ => unreachable!()`, corpus-checked safe the same way `door->field`/target-tracer dereferencing already are, since every real local reaching it is provably `Mobj`-shaped); a new mirror-image write arm in `render_expr_stmt` handles `th->field = expr;`/`th->field -= expr;` (any real `AssignOp`, not just `=` -- `A_Tracer`'s own `th->tics -= P_Random()&3;` is the first compound-assign write through a `Handle<Thinker>` this module has needed, unlike `EV_VerticalDoor`'s own `mutating_handle` sibling, which only ever needed `=`), each rendering a **fresh** `thinkers.get`/`get_mut` call at its own point of use -- reusing `render_existing_thinker_mutation`'s own fresh-per-access-borrow discipline rather than a single hoisted binding, the same real `rustc` borrow-checker rejection already documented for that sibling case. `render_fn_impl`'s signature-extension logic (previously a strict `self-removal XOR target-deref` choice) is generalized to two independent, composable dimensions: whether the body needs its own `handle: Handle<Thinker>` value (self-removal, unchanged; or a new `needs_self_handle_value` case -- see below), and whether it needs `thinkers` at all, now potentially *mutable* (`needs_spawn_mut`, true whenever a `P_SpawnMobj` local exists) rather than the read-only `&Arena<Thinker>` `needs_target_deref` alone produces -- a single `&mut Arena<Thinker>` parameter serves both needs when a function (like `A_Tracer`) has both, since a `&mut` reborrows immutably for `.get(..)` for free. **A genuinely new need, `A_VileTarget`'s own `fog->target = actor;`**: storing the function's *own* receiver as a `Handle<Thinker>` *value* into a freshly-spawned mobj's field -- `self_param` alone is `&mut Mobj`, not a handle, so this needed `body_has_self_handle_value` (mirroring `body_has_self_removal`'s own recursive scan, scoped to `target`/`tracer` specifically) to add a bare `handle: Handle<Thinker>` parameter (reusing self-removal's fixed name, without its `arena` companion, since nothing here calls `Arena::remove`) whenever the body assigns `self_param` bare into a spawned local's `target`/`tracer` field; the mirror-image self-field write (`actor->tracer = fog;`, a bare `Handle<Thinker>` local written into `self`'s own `Option<Handle<Thinker>>` field) generalizes the existing `specialdata`-only `Some(..)`-wrap the same way. **A genuine latent bug in the original C, preserved rather than silently fixed**: `A_VileTarget`'s own `fog = P_SpawnMobj (actor->target->x, actor->target->x, actor->target->z, MT_FIRE);` passes `actor->target->x` twice -- the second argument is almost certainly meant to be `.y`, but the real corpus source really does say `x` twice; translated exactly as written, not "fixed." **New `FixedT` arithmetic, surfaced by `A_Tracer`'s own angle/slope-adjustment tail and `A_BrainExplode`'s randomized spawn offsets**: `fixed_t` is a bare `typedef int` in the original, so idioms like `40*FRACUNIT`, `dist / actor->info->speed`, `FRACUNIT/8`, `actor->momz -= FRACUNIT/8`, and `mo->x + (P_Random()-P_Random())*2048` never go through the rescaling `fixed_mul`/`fixed_div` at all -- they're raw `int` arithmetic on the representation, the same idea `Add`/`Sub`/`Neg` already modeled for `+`/`-`/unary `-`. `runtime/fixed.rs` gains `Mul<FixedT> for i32`/`Mul<i32> for FixedT` (both operand orders -- the real corpus uses both), `Div<i32> for FixedT`, `Add<i32> for FixedT`/`Add<FixedT> for i32` (ditto), and `AddAssign`/`SubAssign`, each a thin wrapping-arithmetic pass-through, never routing through the true rescaling operations. `dist` itself (`A_Tracer`'s own `fixed_t`-declared local) is treated as a plain scalar throughout the verification harness -- declared `fixed_t` in the original, but only ever divided by/compared against plain `int`s once `P_AproxDistance` computes it, so its stub return type is `i32` to match this real call site's own usage, the same "stub matches the caller, not necessarily the callee's eventual signature" precedent already documented for `S_StartSound`. `finecosine[exact]`/`finesine[exact]` need an explicit `as usize` even for a bare index identifier (unlike `sidenum[side^1]`'s fresh, single-purpose local) since `exact` is *also* used earlier in real `u32` arithmetic against `actor->angle` -- narrowly matched by the array's own name, the same style as `sides`/`sectors`/`textureheight`. **A second, narrower `FixedT` gap, `A_BrainExplode`'s own `th->momz = P_Random()*512;`**: a *plain* `i32` expression (no `FixedT` source anywhere in it) assigned straight into a `FixedT` field -- valid, unremarkable C, but Rust needs an explicit `FixedT(..)` wrap; a new `expr_is_fixed_t_valued` check (conservative -- `false` only means "not provably `FixedT`," never "definitely plain `int`") decides this, scoped to a plain `=` into a field this same `Mobj` shape's own `self_field_types` registers `"FixedT"`. `A_Tracer` and `A_VileTarget` -- the two functions this piece was built for -- plus `A_BrainExplode` (the mixed-arithmetic case), `A_Explode`, `A_SkelWhoosh`, `A_SkelFist`, and `A_FatRaise` (five more straightforward `A_*` bodies picked up in the same pass, none needing anything beyond what already existed) are now fully translated, with exact-match tests for each (`test_a_tracer_renders_exactly`, `test_a_vile_target_renders_exactly`, `test_a_brain_explode_renders_exactly`, `test_a_explode_renders_exactly`, `test_a_skel_whoosh_renders_exactly`, `test_a_skel_fist_renders_exactly`, `test_a_fat_raise_renders_exactly`). **`A_PainShootSkull` investigated, deliberately deferred, not forced**: unlike its two siblings, it isn't itself a `state_t.action` entry (it's a plain two-parameter helper, `mobj_t* actor, angle_t angle`, called by the already-translated `A_PainAttack`/`A_PainDie`) and its body opens by walking the *raw* intrusive thinker list directly (`currentthinker = thinkercap.next; while (currentthinker != &thinkercap) { if (currentthinker->function.acp1 == (actionf_p1)P_MobjThinker && ((mobj_t*)currentthinker)->type == MT_SKULL) count++; currentthinker = currentthinker->next; }`, counting live skulls before spawning another, to enforce a population cap) -- `Arena<Thinker>` has no read-only iteration at all yet (only `get`/`get_mut`/`run`, the last of which is a tick-dispatch pass, not a plain scan), so this needs a genuinely new `Arena` capability, not just a rendering gap. This is the same real need already flagged, for a different function, at the end of the `states[]` entry above (`A_Look`/`A_KeenDie` need `goto`/thinker-list iteration, "neither built yet") -- not a new gap, so it's left for whenever that capability gets built rather than special-cased once here. Verified compiling for real (`rustc --edition 2021 --crate-type lib`), all seven translated functions, against hand-written stand-in `World`/`Thinker`/`Mobj`/`MobjInfo`/`Arena`/`Handle`/`FixedT` shapes and stub forward-referenced functions -- zero errors.

**`T_MoveCeiling` (`p_ceilng.c`) -- general `switch` fallthrough, not just the empty-`default` carve-out**: surveyed the *whole* corpus first (a throwaway example walking every `Switch` in every `p_*.c`/`g_game.c`/`m_menu.c`/... file, not assumed) to find a real fallthrough case with genuine content before designing anything -- `T_MoveCeiling` itself has a real *three*-level chain: `case silentCrushAndRaise: S_StartSound(..); case crushAndRaise: ceiling->speed = CEILSPEED; case fastCrushAndRaise: ceiling->direction = 1; break;`. Since each of the three labels has a genuinely *different* effective body (`silentCrushAndRaise` needs all three statements, `crushAndRaise` needs the last two, `fastCrushAndRaise` needs only its own), they can't share one arm the way `render_switch`'s existing shared-case-label handling does -- each keeps its own identity and pattern. `render_switch` is restructured into two passes: pass 1 splits the switch into raw arms exactly as before, but now *records* (rather than rejecting) whether an arm falls through; pass 2 resolves each arm's real body back-to-front (`resolved[k] = own_stmts[k] ++ resolved[k+1]` when arm `k` falls through), so a falling-through arm's rendered body is its own statements followed by whatever it falls into -- already resolved, handling multi-level chains for free, since each arm only ever looks one step ahead at its already-finished neighbor. The old narrow "falls into an empty `default: break;`" special case is fully subsumed by this (an arm falling into an empty resolved body is just unaffected) and was removed, not kept alongside the general mechanism. Also confirms `render_fn`'s self-removal detection doesn't false-positive: `P_RemoveActiveCeiling(ceiling)` (a different, not-yet-translated function) doesn't match `is_self_removal_call`'s specific `P_RemoveThinker(&self->thinker)` shape, so `T_MoveCeiling`'s own signature correctly stays `world`-only, no `handle`/`arena`. **Verified compiling for real**, including the three-level chain. `test_t_move_ceiling_renders_exactly` locks the whole function against exact expected text; all thirteen earlier tests (including `T_MoveFloor`'s own) still pass unchanged against the generalized algorithm, confirming it's a strict superset, not a behavior change for what already worked.

**A fourth function shape, architecturally supported now**: `EV_DoCeiling`/`EV_DoFloor`-style triggers build their thinker *inline*, mid-loop (`Z_Malloc`/`P_AddThinker`/field-fill-in nested inside a `while` body), unlike `EV_StartLightStrobing`'s call-out to a separate `P_Spawn*` function. `render_spawn_fn`'s core loop is extracted into `render_ctor_body(items, ctor_var, ctor_rust_type, ctor_field_types, base_ctx, depth, fn_name)` -- depth-parameterized (every line prefix was previously hardcoded to 4 spaces, since it only ever rendered at the top of a function body) and driven by a caller-supplied `base_ctx` rather than building its own, so identifiers from an *enclosing* function (a trigger's own params/locals) resolve correctly inside the embedded constructor. `FnBodyContext` gains `embedded_ctor: Option<(&str, &str, &HashMap<String,String>)>` and is now `#[derive(Clone, Copy)]` (needed for `render_ctor_body`'s own `..*base_ctx` struct-update). `render_compound_items` becomes ctor-aware: when `ctx.embedded_ctor` is set, it scans for the first statement matching `is_malloc_assign`, renders everything before it normally, then hands the rest of that block to `render_ctor_body` wholesale -- the same "process my whole remaining scope" behavior `render_spawn_fn` itself already had, so no partial-consumption bookkeeping is needed. `render_trigger_fn` gains matching `embedded_ctor` and `return_type: Option<&str>` parameters (`EV_DoCeiling`/`EV_DoFloor` return `int`; `Stmt::Return` itself already rendered generically, this is only the signature's `-> T`). **A real simplification found while building this**: the back-reference deferral check was over-restrictive (only the exact `is_ctor_var_backreference` assignment shape could be deferred); `EV_DoCeiling`'s own `P_AddActiveCeiling(ceiling)` (`ceiling` as a bare call argument, a different shape) needed allowing too -- simplified to erring on bare `ctor_var` usage only when `!has_backreference` (no `handle` binding exists yet to resolve to); once `has_backreference` is true, *any* statement shape defers safely, since the substitution is correct wherever `ctor_var` appears. **Validated as a strict superset**: all eighteen pre-existing tests passed unchanged after the refactor, with no new tests needed to prove it, since none of their expected outputs changed.

**`EV_DoCeiling` -- closed out end-to-end, the fourth function shape's own precise gap solved**: its own constructor sets several fields (`topheight`/`bottomheight`/`direction`/`speed`) *inside* a `switch(type)` that's part of construction, not via flat statements -- a shape `render_ctor_body` didn't recognize as field assignment at all. Confirmed this would produce genuinely broken output (not just a missing feature) if shipped naively: testing with a reduced field list showed assignment to undeclared locals and reassignment of an immutable `crush` binding, correctly refused by the completeness check rather than silently produced. **`collect_ctor_fields_in`** walks a `switch` (through `case`/`default`/nested `if`, reusing the same recursion shape `count_ctor_field_assigns` already had) collecting every field it assigns anywhere, in first-seen order; each not-yet-bound one is pre-declared with `let mut field;` before the switch, and the switch itself then renders *completely unchanged* through the existing `render_switch`/`render_stmt` path -- `ctx`'s `ctor_var` resolution already turns every `ceiling->field` reference inside its arms into a plain reassignment of that local, exactly like a compound-assignment refinement already did outside a switch, so no new statement-shape handling was needed for the arms' own content. **A real, previously-unexercised bug found while building this**: `count_ctor_field_assigns_stmt` never recursed into `Stmt::Case`/`Stmt::Default` at all, so `mut`-detection would have silently missed any field reassigned inside a switch's own case arms (`crush`, set unconditionally to `false` before the switch and conditionally to `true` inside two of its arms) -- no prior constructor had a switch to expose this. **The harder question -- what value does an unset field get, and how often is it actually needed**: tracing `T_MoveCeiling` (mirroring the `P_SpawnDoorCloseIn30` investigation) confirmed `bottomheight` genuinely is safe to leave unset in the `raiseToHighest` arm specifically (that ceiling always self-removes on reaching the top rather than ever reversing direction and reading it back) -- but actually *compiling* a reduced repro with `rustc` revealed the real scope is bigger: since `type` maps to a plain `i32`, not a real closed Rust `enum`, `render_switch`'s own synthetic `_ => {}` catch-all (for a discriminant value outside the ones this switch actually names -- unreachable in real Doom, since `type` is always one of the known `ceiling_e` values, but Rust can't see that from an `i32`) leaves *every* switch-touched field looking possibly-uninitialized to Rust's real definite-assignment analysis, even `direction`/`speed`/`topheight`, which every genuine `case` sets. A new `CtorSpec.field_defaults` map (an explicit, never-auto-guessed placeholder expression per field, supplied only once a caller has done the same tracing that got `P_SpawnDoorCloseIn30` correctly *rejected* for the opposite reason) is now *required* for every switch-touched field, seeding its pre-declaration (`let mut field = {default};`) instead of leaving it bare. `olddirection` needs one for an entirely different reason -- `EV_DoCeiling` never touches it at all; only `P_ActivateInStasisCeiling`/`EV_CeilingCrushStop`, two unrelated functions, ever read or write it, always write-before-read (confirmed by tracing every real reference in the corpus), so the initial value is never observed regardless of what it is. **Two more real bugs caught working through this**: the parameter `type` (a Rust keyword) was escaped nowhere at all -- not in its own declaration (`render_params`) nor in any bare reference to it (`render_expr`'s `Ident` arm) -- fixed generally via a new `rust_ident_name`, deliberately distinct from `rust_field_name` since `true`/`false` are real Rust boolean *literals* when they appear as values (matching the established `boolean` -> `bool` mapping), not identifiers that need escaping or rejecting the way a field literally named `false` would; and the constructor's own top-level `ceiling_t* ceiling;` declaration became dead, uninferable code once every one of its fields got its own separate `let` instead (nothing ever reads or writes `ceiling` itself in the translated output), so `render_decl` now drops a local matching `embedded_ctor`'s own variable entirely rather than emitting an unconstrainable `let mut ceiling;`. `CtorSpec<'a>` (bundling `ctor_var`/`ctor_rust_type`/`ctor_field_types`/`field_defaults`, now `pub`, re-exported from `codegen::mod`) replaces the growing tuple/parameter list `render_ctor_body`/`FnBodyContext::embedded_ctor`/`render_trigger_fn` had been passing around, once a fourth piece of ctor-specific data made clippy flag both `type_complexity` and `too_many_arguments`. **Verified compiling for real**: the complete function, assembled with real `World`/`Thinker`/`Arena`/`Handle`/`Sector` shapes (including a corrected `Handle<T>` stand-in matching the real project's own hand-written `Copy` impl, not a naive `#[derive(Copy)]` that would wrongly require `T: Copy`), compiled clean with `rustc` -- zero errors, zero warnings. `test_ev_do_ceiling_renders_exactly` locks the whole function against exact expected text.

**`EV_DoDoor` -- second trigger-with-inline-constructor, reusing all of `EV_DoCeiling`'s machinery unchanged, but surfacing two more real, previously-unexercised gaps**: (1) `int secnum,rtn;` declares two locals off one `int` specifier -- every earlier function's own locals happened to each get their own separate line, so `render_decl` had only ever accepted a single declarator; fixed by looping over `d.declarators` (each still checked individually: no initializer, plain non-array/non-function, and still dropped if it names `embedded_ctor`'s own `ctor_var`) instead of destructuring a one-element slice. (2) `door->sector->soundorg` reads a cross-reference-typed constructor field (`sector: SectorId`, itself set from `door->sector = sec;` earlier in the same constructor) back out and dereferences *through* it to reach `.soundorg` -- the `Expr::Member` branch that resolves a bare `ctor_var->field` read to that field's own local hard-coded `is_crossref: false` unconditionally, since until now it only ever needed to produce the field's *name*, never to know whether that name's own value was itself an index needing `world[...]` wrapping when used as a base for further member access. This rendered as the ill-typed `sector.soundorg` instead of `world[sector].soundorg` -- confirmed as a real compile error, not just guessed at, by rendering the function and compiling it with the field hard-coded false first. Fixed by adding `FnBodyContext::ctor_field_types` (mirroring `self_field_types`) and checking it exactly the way the general `Member` fallback already checks `self_field_types` for `self_param`'s own fields. `speed` is set once before the switch and reassigned inside two of its arms -- picked up as `mut` via the *ordinary* reassign-count path, not the switch-field-synthesis path, since it's already `let`-bound by the time the switch statement is reached (the synthesis path skips any field already in `ctor_field_names`). `topheight`/`direction` are switch-only and need `field_defaults`, exactly like `EV_DoCeiling`'s asymmetric fields; `topcountdown` is never touched anywhere in `EV_DoDoor` at all (confirmed by tracing every read of it in `T_VerticalDoor`: only reachable when `door.direction` is `0` or `2`, values `EV_DoDoor` never produces, since it only ever sets `direction` to `1` or `-1`), so it falls through to the general completeness-check default exactly like `EV_DoCeiling`'s `olddirection`. `door->type = type;` is a plain passthrough, exercising the pre-existing dedup (`rhs_text != field`) that skips emitting a redundant `let` when a field's value is already sitting in an identically-named binding -- here, the function's own `r#type` parameter. Verified compiling the complete function with `rustc` directly (hand-written `World`/`Sector`/`VerticalDoor`/`Arena`/`Handle`/`FixedT` stand-ins), zero errors. `test_ev_do_door_renders_exactly` locks the whole function against exact expected text.

**`EV_DoPlat` (`p_plats.c`) -- a third trigger-with-inline-constructor, reusing `EV_DoCeiling`'s whole machinery unchanged, needing two new expression-rendering features and exposing a real nondeterminism bug**: `sides[line->sidenum[0]].sector->floorpic` chains a bare (non-`&`) global-array index (`sides[i]`, mirroring `&sectors[i]`'s existing special case but without the address-of, so it returns `is_crossref: true` directly since nothing dereferences it via `&` first) into a cross-reference-typed field (`side_t.sector`, hand-matched narrowly, the same "no general struct-field-type registry yet" reasoning as everywhere else in this module) into a further plain field read -- rendering as genuinely-correct double `world[...]` indirection (`world[world[SideId(..)].sector].floorpic`): `sides[i]` is a `SideId` *value*, and `.sector` reads a *further* index out of it, so two separate lookups are really needed, not a rendering quirk. `line->sidenum[0]` (the index expression itself) needed a new, fully generic `Expr::Index` fallback arm for a plain fixed-size array *field* (`sidenum: [i16; 2]`) -- nothing before this needed indexing into an ordinary field, only the two special global arrays. `World` gained a second real field (`sides: Vec<Side>` + `Index`/`IndexMut<SideId>`, mirroring `sectors` exactly) for the new `SideId` lookups to resolve against -- the same "grows when a real body needs it, not speculatively ahead of that" incremental pattern `sectors` itself established. `plat->sector->specialdata = plat;` writes *through* a constructor field the same way `EV_DoDoor`'s `door->sector->soundorg` read through one -- already fixed for free by that same `ctor_field_types` addition, no new code needed. **A real nondeterminism bug found while building this**: `count`/`oldstatus` are both untouched by `EV_DoPlat`'s own `switch` (falling to the general completeness-check fallback, not the switch-synthesis path), and that fallback iterated `ctor_field_types.keys()` -- a `HashMap`, whose iteration order is randomized per-process. `EV_DoCeiling` never exposed this (`olddirection` was its only such field, so order was never observable in a single-field loop); with two fields here, an unsorted iteration would make the renderer's own generated code nondeterministic across runs, not just differently-but-validly ordered -- fixed by sorting the remaining field names before that loop. `speed`/`high`/`wait`/`status`/`low` are switch-only, needing `field_defaults` unconditionally per the same empirically-verified rule as `EV_DoCeiling`; `count`/`oldstatus` are safe to default, confirmed by tracing every read in `T_PlatRaise`/`P_ActivateInStasis`/`EV_StopPlat` (both always written before ever read -- the same write-before-read safety as `olddirection`). Verified compiling the complete function with `rustc` directly (hand-written `World`/`Sector`/`Side`/`Line`/`Plat`/`Arena`/`Handle`/`FixedT` stand-ins), zero errors. `test_ev_do_plat_renders_exactly` locks the whole function against exact expected text.

**`EV_DoLockedDoor` (`p_doors.c`) -- a plain trigger (no embedded constructor, only early-returns or a delegating call to the already-translated `EV_DoDoor`), the first function needing three new, narrowly-scoped capabilities**: (1) `thing->player` -- `thing`'s own declared type is `Handle<Thinker>` (a live thinker passed in, the same core blocker surveyed for `EV_VerticalDoor`), needing a real `Arena` lookup plus picking out the one variant (`Mobj`) with a `.player` field out of `Thinker`'s ten -- hand-matched narrowly (only `Mobj` reached this way so far), not a general enum-variant-field lookup mechanism built ahead of a second caller needing one. (2) `p->cards[..]`/`p->message` -- `p`'s own declared type is `Option<PlayerId>` (a `player_t*` local); rather than reshaping the function around a narrowed/shadowed binding once null-checked (a real `let-else` unwrap), every dereference is `.unwrap()`-ed at its own point of use, since the corpus itself already guards each one with its own adjacent `if (!p) return ...;` (redundant after the first check, since `p` provably can't become "null" again -- rendered as-is anyway, matching the original's own defensive redundancy rather than building a new "prove and elide dead code" feature this one function doesn't need). (3) `!p` (bare `Option`-typed local truthiness) needed `render_bool_expr`'s `Unary::Not` handling to become `Option`-aware for a bare identifier, alongside the pre-existing `specialdata`-field special case -- `.is_none()`, not the `== 0` every other negated (plain `int`) value gets. `World` gained a third real field, `players: [Player; MAXPLAYERS]` -- deliberately a fixed array, not `Vec`, matching `runtime/player.rs`'s own already-documented design (`player_t players[MAXPLAYERS]` is genuinely fixed-size at program lifetime, unlike `sectors`/`sides`' per-level tables). **A real, more general gap found along the way**: `S_StartSound(NULL, sfx_oof)` (no sound origin) showed `NULL` was only ever converted to `None` in the one `specialdata = NULL;` assignment shape `render_expr_stmt` already special-cased, not as a plain value anywhere else -- generalized so `Expr::Ident("NULL")` always renders `None` wherever it appears, matching this project's own no-real-pointers memory model regardless of context. `!player->cards[idx]` (used inside `&&`, not as a whole `if`'s own top-level condition) needed no new code at all: it flows through `render_expr`'s already-generic `Unary::Not` arm (plain `!`), not `render_bool_expr`'s specialized top-level handling, and a real `bool` field negates correctly with Rust's own `!` already. Verified compiling the complete function with `rustc` directly (hand-written `World`/`Player`/`Mobj`/`Thinker`/`Arena`/`Handle` stand-ins), zero errors. `test_ev_do_locked_door_renders_exactly` locks the whole function against exact expected text.

**`EV_VerticalDoor` (`p_doors.c`) -- closed out, the function that motivated the whole `p_doors.c` survey**: reuses `EV_DoLockedDoor`'s `thing->player`/`Option<PlayerId>` infrastructure verbatim for its own three-lock-check preamble (confirmed identical shape, just different local/message names), and `EV_DoPlat`'s `sides[i].sector` chain for `sec = sides[line->sidenum[side^1]].sector;` -- no changes needed to either. Three new pieces: (1) `secnum = sec-sectors;` -- real pointer arithmetic, computed but genuinely never read again (confirmed by grep), translated honestly rather than dropped: a `SectorId` already *is* "the index of this pointer" under this project's own memory-model decision, so this needs no arithmetic at all, just `sec.0 as i32`. (2) The function's own core new capability: `if (sec->specialdata) { door = sec->specialdata; ...door->field...; }`, reusing an *existing* thinker instead of always constructing a new one -- `existing_thinker_mutation_shape` detects this exact assignment-from-specialdata shape narrowly (the same "hand-match the one real corpus shape" style used throughout this module); `render_existing_thinker_mutation` renders every remaining statement in the block with a new `FnBodyContext::mutating_handle`, so each `door->field` reference (read or write) gets its own *fresh* `thinkers.get`/`get_mut` call, rather than one binding hoisted across the whole block. That hoisted-binding design was the obvious first attempt (matching how a tick function's own `self` receiver already works) -- rejected by `rustc` itself, not by inspection: the real block reads `door->direction`, then (in one branch) calls `thing->player`, a second unrelated `thinkers.get(..)` borrow, before writing `door->direction` again, and a hoisted `&mut` binding must stay borrowed across that whole span, conflicting with the intervening immutable borrow. Fresh per-access borrows sidestep this outright, and generalize correctly to any future control flow between reads/writes, not just this one shape. (3) `S_StartSound` is called both with a real `&SoundOrg` (the "for proper sound" switch) and with `NULL` -> `None` (the lock-check preamble) *within this one function* -- a real, newly-surfaced gap: this renderer has no cross-function awareness of `S_StartSound`'s own eventual real parameter type, so it can't know whether the real-reference call sites need `Some(..)` wrapping to stay consistent with the `None` ones. Verified compiling only by giving the scratch-check stub's own `S_StartSound` a deliberately generic signature; left as a documented, out-of-scope limitation (falls under the same "cross-function signature wiring... unresolved" umbrella as calling an already-translated function, just for a not-yet-translated external one instead), not fixed. Verified compiling the complete function with `rustc` directly (hand-written `World`/`Sector`/`Side`/`Line`/`Player`/`Mobj`/`VerticalDoor`/`Thinker`/`Arena`/`Handle` stand-ins), zero errors. `test_ev_vertical_door_renders_exactly` locks the whole function against exact expected text.

**`EV_DoFloor` -- confirmed, by reading the whole function, to be a genuinely multi-piece undertaking, not one increment; first piece closed out (bool-as-arithmetic)**: beyond the already-known `for`-loop/`switch`-`break`-scoping issue and pointer arithmetic (`getSide(secnum,i,0)->sector-sectors == secnum`, this time nested inside a comparison rather than a plain assignment), the full read also surfaced `twoSided()`/`getSide()`/`getSector()` (line/side/sector adjacency helpers, nothing modeled yet), a `textureheight[...]` global array (a new kind of table), `line->frontsector->floorpic`-style crossref-through-crossref chaining via a *different* base shape than `sides[i].sector`, and `sec` reassigned mid-construction inside one case. Each is roughly its own feature. **First piece done**: `(8*FRACUNIT)*(floortype == raiseFloorCrush)`, C's bool-as-0-or-1 arithmetic idiom (`raiseFloorCrush`'s extra 8-unit lowering, folded into one expression rather than an `if`) -- a comparison already renders as a real Rust `bool`, which can't multiply/add/etc. directly the way C's `int`-valued comparisons can. `render_binary_operand` (replacing the inline precedence logic `Expr::Binary`'s own arm used to have directly) now special-cases an operand that's itself comparison/logical when the *parent* operator is arithmetic: `({cmp}) as i32`, with its own explicit parens around the comparison (`as` binds tighter than every binary operator this renderer handles, so `x == y as i32` would parse as `x == (y as i32)`, not what's wanted) -- the *outer* arithmetic operator needs no extra parens around that whole `(..) as i32` unit, since Rust's own precedence already binds `as` tighter than `*`/`+`/etc., confirmed both syntactically and *semantically* (the cast multiplies by exactly 0 or 1, not the whole product first) by actually compiling and running a reduced repro, not just inspecting the precedence table. `test_bool_as_arithmetic_against_real_ev_do_floor` locks this one statement, extracted directly from the real parsed AST, against exact expected text. **Second piece done: a real `for` loop, `render_for`** -- no function translated so far had needed C's `for (init; cond; step) body` shape at all; `raiseToTexture`/`lowerAndChange` both scan their sector's own lines via the identical `for (i = 0; i < sec->linecount; i++)` header. Only a plain-assignment init (the counter is already declared earlier in the function, the only shape C89 itself allows) and an `x++`/`x--` step are supported, mechanically becoming `<init>; while <cond> { <body> <step>; }` -- correct only because the body never `continue`s: C's own `for` still runs its step on `continue`, but the translated Rust `while` would jump straight back to the condition and skip it, so `render_for` recursively scans the body first (`body_has_bare_continue`/`stmt_has_bare_continue`, stopping at a nested `while`/`for` since that inner loop consumes its own `continue`) and rejects loudly rather than silently mistranslating -- no real corpus `for` loop has hit this rejection yet. `test_for_loop_header_against_real_ev_do_floor` clones the real `init`/`cond`/`step` straight out of `EV_DoFloor`'s own parsed AST (whichever of the two identical-header loops `find_stmt` reaches first -- extended to walk into `Stmt::For` bodies too) and re-wraps them around a synthetic empty body, the same real-subtree-plus-synthetic-wrapper approach as the shared-case-labels test, since both loops' *real* bodies still depend on pieces tracked separately below (`twoSided`/`getSide`/`textureheight[]`, the crossref-through-crossref chain, `sec` reassignment). **Third piece done: `twoSided`/`getSide`/`getSector` adjacency helpers** -- real corpus functions (`p_spec.c`), not macros, over the same sector/line/side adjacency already-translated code uses (`sides[i].sector`), but not modeled at all before this. Turned out to need almost no new code: `getSide` returns `side_t*` -> `SideId` under the already-decided memory model, exactly like `sec: sector_t*` already works, so `side = getSide(secnum,i,0);` and a later `side->bottomtexture` resolve for free once the caller supplies `side`'s own declared type via the existing `extra_cross_ref_idents` mechanism -- no new rendering logic at all. The one real gap: `if (twoSided (secnum, i))` is the first *bare, non-negated call result* used directly as an `if` condition (`twoSided` genuinely returns a plain C `int`, a bitmasked flag) -- `render_bool_expr` gained a narrow `Expr::Call` arm matched by name (`twoSided` specifically, this codebase's usual style, not a general "any call is int-valued" fallback, since a differently-shaped callee could just as easily be `Option`-valued the way `thing->player` already is elsewhere) rendering `{call} != 0`. `test_two_sided_adjacency_scan_against_real_ev_do_floor` clones the real condition plus the real first statement of the real `if` body (`side = getSide(secnum,i,0);`) out of `EV_DoFloor`'s own parsed AST, re-wrapped around a synthetic one-statement body -- the rest of that body still depends on `textureheight[...]` (next, not modeled yet). `getSector` itself remains unexercised by a test (only used in the `lowerAndChange` case, bundled with the `sec`-reassignment piece). **Fourth piece done: `textureheight[...]`, a genuinely new kind of table** -- unlike `mobjinfo[]`/`states[]` (compile-time literal data straight from the corpus, rendered as a `pub static` array), `textureheight` (`r_data.c`) is allocated at runtime (`Z_Malloc`) and filled from WAD data, so it has no literal initializer to embed; for a function body's own purposes it's just an opaque global identifier, indexed like any other array (the *setup* code that actually allocates/fills it, `R_InitTextures`, is a separate not-yet-transpiled function -- the same already-accepted forward-reference gap every other not-yet-wired cross-function call already has). The one real new piece: `textureheight[side->bottomtexture]` indexes by a *struct field* (`bottomtexture: i16`, a concrete type fixed by `Side`'s own definition) rather than a literal or a fresh, still-type-inferred local (`sidenum[side ^ 1]`, already rendering with no cast, since Rust infers `side: usize` straight from that same indexing use) -- Rust's `Index<usize>` needs an explicit `as usize` here that the generic `Expr::Index` fallback never had to add before; now added whenever the index expression is itself an `Expr::Member`. `test_textureheight_index_against_real_ev_do_floor` clones the real nested `if (side->bottomtexture >= 0) if (textureheight[..] < minsize) minsize = textureheight[..];` straight out of `EV_DoFloor`'s own parsed AST, no synthetic wrapper needed this time -- the whole fragment renders as-is. This closes out `raiseToTexture`'s own adjacency scan entirely (pieces 2-4 combined cover its full body); **Fifth piece done: `line->frontsector->floorpic`, crossref chained through crossref via a different base shape than `sides[i].sector`** (`raiseFloor24AndChange`) -- `line`'s own field `frontsector` (`LineId` -> `SectorId`) is a *trigger function's own parameter*, tracked only in `extra_cross_ref_idents`, which (unlike `self_field_types`/`ctor_field_types`) has no generic field-type registry for a parameter's *own* fields, so a further `.floorpic`/`.special` chain off it had nothing telling it `frontsector` itself is cross-reference-typed. Fixed with one more narrow by-name `Expr::Member` arm (mirroring `thing->player`'s own shape), so `line->frontsector` itself now returns `is_crossref: true`, letting the generic `Expr::Member` fallback wrap the further chain in `world[...]` correctly: `world[sec].floorpic = world[world[line].frontsector].floorpic;`. `test_frontsector_crossref_chain_against_real_ev_do_floor` clones both real assignment statements (`floorpic`/`special`) straight out of `EV_DoFloor`'s own parsed AST. **Sixth and final piece done: `lowerAndChange`'s own `if (getSide(secnum,i,0)->sector-sectors == secnum) {...} else {...}`** -- the already-built `X-sectors` pointer-arithmetic idiom (`EV_VerticalDoor`'s own `secnum = sec-sectors;`) nested inside a comparison for the first time, and chained directly off a *call result* rather than a local (`getSide(secnum,i,0)->sector`, not `side->sector`), needing the `Expr::Call` arm's own new `getSide`/`getSector` -> cross-ref-typed special case (mirroring `twoSided`'s by-name treatment) so the chained `.sector` resolves through `world[...]` before the `-sectors` special case ever sees it. `sec` genuinely gets reassigned mid-construction (`sec = getSector(secnum,i,1);`/`getSector(secnum,i,0);`, a plain local rebind) -- harmless, since `floor->sector = sec;` earlier in the case already copied the *old* value by-value (`SectorId` is `Copy`, exactly matching C's own pointer-copy-not-reference semantics), so no special ctor-field handling was needed at all. **A real, separate gap surfaced along the way**: a genuine loop-exiting `break;` reached several `if`-levels inside the `for` loop's body (not `render_switch`'s own case-delimiter `break`, which is peeled off before individual statements ever reach `render_stmt`) had no generic `render_stmt` arm at all -- fixed with a `Stmt::Break` arm mirroring the existing `Stmt::Continue` one. `test_lower_and_change_sec_reassignment_against_real_ev_do_floor` clones the real `if`/`else` straight out of `EV_DoFloor`'s own parsed AST.

**All six pieces combined now cover `EV_DoFloor`'s entire body**, each independently verified against a real cloned AST fragment rather than fabricated test input.

**Full end-to-end assembly attempted (`render_trigger_fn` with a real `CtorSpec`, the same integration point `EV_DoCeiling`/`EV_DoDoor`/`EV_DoPlat` already went through) -- surfaced two more real rendering gaps, plus a genuine latent bug in the original C.** The two gaps, both narrow, mechanical, and needed before the whole function could render at all: (1) `case raiseToTexture: { int minsize = MAXINT; side_t* side; ... }` wraps its body in real braces, unlike every other case in the same `switch` (flat, brace-less siblings) -- C parses this as one bare `Stmt::Compound`, which `render_switch` hands to `render_stmt` directly, and `render_stmt` had no arm for a bare `Compound` at all (only `render_block`, used for `if`/`while` bodies, already dispatched it) -- fixed by adding the same dispatch to `render_stmt` itself; Rust's own `match` arm braces already provide equivalent scoping for `minsize`/`side`, so no extra nesting was needed. (2) `int minsize = MAXINT;` is this codebase's first declaration with a real initializer -- `render_decl` previously rejected any initializer outright; now renders one inline on the same always-`mut` `let` every declaration already gets (`let mut minsize = MAXINT;`), no per-declarator mutability analysis needed. With both fixed, the function renders in full -- **except `render_ctor_body`'s own completeness check correctly, honestly rejects it**, for a genuine reason rather than a translation gap: `floor->newspecial` is set *only* deep inside `lowerAndChange`'s own `for` loop, inside an `if (sec->floorheight == floor->floordestheight)` with no guarantee of ever matching -- unlike `floor->texture` (also refined there, but first given an unconditional value right before the loop, `floor->texture = sec->floorpic;`). `T_MoveFloor` (already translated) reads `floor->newspecial` unconditionally once a `lowerAndChange` floor reaches `pastdest`, regardless of whether that `for` loop ever found a match -- so a `lowerAndChange` floor whose sector has no two-sided neighbor at exactly its own destination height reads genuinely uninitialized `Z_Malloc` memory in the real original game: real, reachable UB, the same class of finding already documented for `P_SpawnDoorCloseIn30`, confirmed the same way (tracing real call sites/reads across both functions, not assumed) rather than guessed at. Since there's no well-defined C value to be faithful *to*, the rejection is the correct, permanent answer, not a gap to route around with a fabricated default -- unlike this function's *other* four switch-only fields (`direction`/`sector`/`speed`/`floordestheight`), which really are set by every one of `floor_e`'s 12 real call-site values across the *whole* corpus (all ~33 call sites grepped directly); only the enum's 13th variant, `donutRaise`, would reach empty and `EV_DoFloor` is never actually called with it anywhere in the corpus -- provably dead code, the same "never observed" reasoning already used for `EV_DoCeiling`'s own `Ceiling.olddirection`, so those four got real placeholder defaults instead. `test_ev_do_floor_detects_missing_newspecial_default` asserts the rejection names `newspecial` specifically, mirroring `test_p_spawn_door_close_in_30_detects_missing_fields`'s own pattern. **`EV_DoFloor` is therefore considered fully investigated and correctly handled** -- not translated to a complete, disk-emittable function (that would require deciding how to represent this specific kind of latent-UB field, a separate open design question), but every rendering gap it exposed is now fixed and tested, and its one genuine block is a documented finding about the original game, not an unfinished translation.

**First `A_*` action-pointer bodies -- `render_fn`'s existing self-struct-tick shape reused as-is, plus a new `render_weapon_fn` for the other `ActionFn` variant**: `A_Fall`/`A_XScream`/`A_Pain` (`p_enemy.c`) are the first real `fn(mobj_t*)` (`ActionFn::Mobj`, `state_t.action`'s `acp1` variant) bodies translated, needing no new `render_fn` capability -- `actor->flags &= ~MF_SOLID;` confirms C's `~` (bitwise complement) already renders correctly as Rust's own `!` (which is bitwise-NOT on an integer, not just logical-NOT on `bool`); `A_Pain`'s `if (actor->info->painsound)` surfaces a genuinely plain-`int` bare-field truthiness case (`render_bool_expr`'s `Expr::Member` fallback, distinct from the `Option`-valued `specialdata`/`player` special cases). `A_Hoof`/`A_Metal`/`A_BabyMetal` confirm a bare forward-referencing call to a not-yet-translated function (`A_Chase`) already renders correctly through the existing generic `Expr::Call` path, and that `first_param_name` was already name-agnostic (`mo`, not `actor`). `render_weapon_fn` (`A_Light0`/`A_Light1`/`A_Light2`, then `A_OpenShotgun2`/`A_LoadShotgun2`/`A_CloseShotgun2`) is the first renderer for the other real corpus shape, `fn(player_t*, pspdef_t*)` (`ActionFn::Weapon`'s `acp2` variant) -- structurally `render_fn`'s twin, just with two real parameters instead of one `self`, and no `World` yet (none of the six real functions built against it touch a cross-reference field). `player_t` isn't struct-mapped yet, so a caller-supplied field-types map (`extralight: i32`) plays the same role `self_field_types` does elsewhere; an *unregistered* field (`player->mo`) already falls through to plain, correct access with no extra work, confirming the generic `Expr::Member` fallback only treats a field as cross-reference-typed when its *registered* type says so.

**Nineteen more `A_*` bodies, all `ActionFn::Mobj`-shaped, plus two real bugs found by actually compiling the output**: `A_PosAttack`/`A_SPosAttack`/`A_CPosAttack`/`A_PainAttack`/`A_BspiAttack`/`A_CyberAttack`/`A_TroopAttack`/`A_SargAttack`/`A_HeadAttack`/`A_BruisAttack` are the first functions to check `!actor->target` (`Mobj.target: Option<Handle<Thinker>>`) -- `is_option_valued`'s `Expr::Member` arm, previously hardcoded to the one field name `player`, is generalized to any self-struct field whose registered `self_field_types` entry is itself `Option<...>`-shaped. None of these ten dereference *through* `target` any further than this truthiness check or pass it as a bare, opaque argument to a not-yet-translated function (`P_SpawnMissile`/`P_DamageMobj`) -- genuinely reading a field *of* the targeted mobj (`A_FaceTarget`'s own `actor->target->x`, `A_Fire`'s `dest->angle`, `A_CPosRefire`/`A_SpidRefire`'s `actor->target->health`) needs real `Arena` read access from inside a `Mobj`-shaped action function, which nothing in this module has built yet -- deliberately deferred rather than guessed at, the same "measure before designing" call already made for `Thinker::tick`'s own signature growth. `A_TroopAttack`/`A_SargAttack`/`A_HeadAttack`/`A_BruisAttack` also add `render_bool_expr`'s first `boolean`-returning (not plain-`int`-returning, unlike `twoSided`) bare-call condition: `P_CheckMeleeRange`'s real corpus declaration returns `boolean`, which already maps to Rust's native `bool` (the `struct_fields.rs` decision), so `if P_CheckMeleeRange(actor)` needs no `!= 0` cast at all, a new arm distinct from -- not a generalization of -- `twoSided`'s own. `A_Scream` (a `switch` on `actor->info->deathsound`, `case 0: return;` ahead of two ordinary shared-case-label groups and a `default`) exposed a real `render_switch` bug, not just a missing feature: an arm ending in an unconditional `return` with no `break` was being treated as falling through into the next arm's own statements as dead code (the fallthrough test only ever checked for an explicit `Break`) -- fixed by treating a trailing `Return` the same as a `Break` when deciding whether an arm falls through, confirmed against this real corpus shape rather than a synthetic repro. `A_PainDie`'s `actor->angle+ANG90`/`+ANG180`/`+ANG270` confirm a plain `#define`d hex-literal macro (`tables.h`, not an enum constant) already renders as a bare pass-through identifier with no special handling, same as `MISSILERANGE`. `A_VileStart`/`A_StartFire`/`A_FireCrackle`/`A_BrainPain`/`A_BrainDie`/`A_SpawnSound`/`A_PlayerScream` round the batch out with already-established shapes (a single sound call, sound-then-tail-call, a bare no-argument forward-reference, `NULL` as a function's only statement) -- `A_PlayerScream` is the one genuinely new shape among them, the first function with a scalar local declared *with* an initializer (`int sound = sfx_pldeth;`, `render_decl`'s existing inline-initializer support, `EV_DoFloor`'s own `minsize` precedent) outside any constructor context.

**A second real bug, this one only surfaced by actually compiling the batch's output with `rustc`** (assembled with hand-written `Mobj`/`World`/`Handle`/`Arena`/`Thinker` stand-ins, the same discipline every earlier batch's "verified compiling for real" step already followed): `angle = actor->angle;` (`A_PosAttack`, `A_SPosAttack`, `A_CPosAttack`) assigns `angle_t` (`u32`, `struct_fields.rs`'s own mapping) into a plain C `int` local -- C silently reinterprets the bits on this kind of assignment, but Rust's deferred-`let` inference picked up `u32` from this first use, then broke for real (`u32 += i32`, a genuine `rustc` error) on the very next line's `i32`-valued `P_Random()` arithmetic. Fixed generally, not patched at the one call site: `FnBodyContext::plain_int_locals` (`render_fn`'s new `collect_plain_int_locals`, a shallow top-level-only scan -- C89 declarations always sit at the top of their own block, and every real corpus function needing this so far declares everything it needs directly at the function's own top level) tracks which locals are genuinely declared bare `int`; assigning a `self_param` field registered as `u32` straight into one of these now renders an explicit `as i32`, the same bit-reinterpreting idea `EV_VerticalDoor`'s own `secnum = sec-sectors;` already needed a `.0 as i32` for. All nineteen functions re-verified compiling clean with `rustc` after the fix -- zero errors, only the expected style lints.

**Compound-assign `for` steps, `&&`/`||`-chain truthiness casting, a bare `unsigned` local, and five more `A_*` bodies -- three small, independently-motivated renderer generalizations, each proven against a real function before being trusted**: `render_for_step` generalizes from `x++`/`x--` to any compound-assign step (`A_BrainScream`'s own `for (x = mo->x - 196*FRACUNIT; x < mo->x + 320*FRACUNIT; x += FRACUNIT*8)`, scanning a horizontal slice of the map in 4096-unit strides rather than counting by ones) by delegating to `render_expr_stmt`'s own already-general `Expr::Assign` handling instead of duplicating its logic -- the exact gap this section's own "not yet done" list had flagged. `render_binary_operand` gains the reverse direction of its existing bool-as-arithmetic cast: `A_ReFire`'s own `(player->cmd.buttons & BT_ATTACK) && player->pendingweapon == wp_nochange && player->health` chains a bitwise-flag test and a bare `int` field truthiness check together with an already-`bool` comparison -- neither of the first two previously rendered as valid Rust `bool` text inside a `&&` chain (only the negated-Option/negated-bool-call cases had their own dedicated arm before). Fixed by reusing `render_bool_expr`'s own per-shape logic directly rather than duplicating it a second time, but **deliberately scoped to just two shapes** (a bare `Member` and a non-comparison `Binary`), not "every operand that isn't already comparison/logical" as first tried: routing a bare `Unary::Not` operand through the same path too regressed `EV_DoLockedDoor`/`EV_VerticalDoor`'s own `!world[p.unwrap()].cards[idx]` (a genuine Rust `bool` array, correctly rendered as plain `!` before) into a wrongly-cast `== 0` int-truthiness comparison -- caught immediately by the existing test suite (not guessed at, not shipped), since this renderer has no per-field real-C-type tracking able to tell a genuinely-`int` negation from a genuinely-`bool` one inside a chain; `Unary::Not` was left exactly as it was rather than resolving that ambiguity speculatively. The same function also needed a new generic `Expr::PostIncDec`/`PreIncDec` arm in `render_expr` itself: `player->refire++;` is the first bare increment/decrement used as its own standalone statement (not hoisted out of an `if` condition, and not a `for` loop's own step, the only two shapes previously handled) -- `render_for_step` now delegates to this new arm too, instead of keeping its own duplicate copy of the same text. `render_decl` separately gains a bare `unsigned` type-specifier case (`A_Fire`'s own `unsigned an;`, parsed as `[TypeSpecifier::Unsigned]`, a distinct AST shape from bare `int`'s `[TypeSpecifier::Int]`) -- same deferred-inference `let mut` treatment either way, just accepting one more bare specifier. `A_Fire` (`p_enemy.c`, `A_StartFire`/`A_FireCrackle`'s own shared tail call, itself never directly translated before) is the function this last piece was built for: `dest = actor->tracer;` is the already-established target/tracer local-alias idiom, and `dest->angle`/`.x`/`.y`/`.z` confirm the alias-dereference machinery generalizes to *any* field, not just the `x`/`y`/`z` triple `A_SkullAttack` already exercised; `finecosine[an]`/`finesine[an]` reuse the established by-name `as usize` cast unchanged. Also translated in the same pass, exercising no new capability beyond `render_weapon_fn`'s existing shape: `A_BFGsound` (identical to the already-covered `A_OpenShotgun2`/`A_LoadShotgun2`), `A_CheckReload` (a single opaque `P_CheckAmmo(player)` call -- its real corpus body also has an `#if 0`'d-out `P_SetPsprite` call right after it, confirming the parser's own preprocessing already strips dead `#if 0` blocks before this renderer ever sees them, the same "confirmed by direct read" discipline already applied to `slidedoor_t`/`FixedDiv2`'s own dead alternate paths). Six new exact-match tests (`test_a_brain_scream_renders_exactly`, `test_a_re_fire_renders_exactly`, `test_a_bfgsound_renders_exactly`, `test_a_check_reload_renders_exactly`, `test_a_fire_renders_exactly`, plus the pre-existing suite unchanged). **Investigated and deliberately ruled out**: `A_SkelMissile`/`A_FatAttack1`/`A_FatAttack2`/`A_FatAttack3` (`p_enemy.c`) looked like a cheap four-function win (just generalizing `collect_spawn_mobj_locals` to also recognize `P_SpawnMissile`, not only `P_SpawnMobj`) but traced out to a genuinely new, unproven architectural gap instead: each writes one field of a freshly-spawned `Handle<Thinker>`-typed local while its own RHS reads a *different* field of that *same* handle in the same statement (`mo->momx = FixedMul(mo->info->speed, finecosine[an]);`) -- the write side's `thinkers.get_mut(mo)` and a naively-rendered fresh `thinkers.get(mo)` for the RHS read would be two live borrows of `thinkers` at once, a real borrow-checker rejection this module's existing "fresh borrow per access" design has never had to resolve, since every prior write's RHS only ever read *other* variables. Left undone rather than guessed at; the real fix (reusing the write's own already-bound `m` for a same-handle RHS read, instead of a second independent lookup) is a small, well-scoped follow-up once actually needed.

**Same-handle field reads during a spawned-mobj write, closing out the `A_SkelMissile`/`A_FatAttack*` gap the previous entry deliberately left open**: `FnBodyContext` gains `same_handle_write: Option<&'a str>`, set only while rendering the RHS of a write to one field of a `Handle<Thinker>`-typed local -- a further `Member` read through that *exact same* handle inside that RHS (`mo->momx = FixedMul(mo->info->speed, ..);`, `A_FatAttack1`'s own idiom) now resolves to the write's own already-bound `m.info` directly, instead of a second, independent `thinkers.get(mo)` call that would conflict with the write's own `thinkers.get_mut(mo)` borrow for the whole block -- a real `rustc` rejection, confirmed by compiling the naive version first (fifteen `FnBodyContext` construction sites across the module needed the new field added, all but the two/three that already use `..*ctx`/`..ctx` struct-update syntax and inherit it automatically). `collect_spawn_mobj_locals` also recognizes `P_SpawnMissile` now, not just `P_SpawnMobj` (`p_mobj.c` confirms an identical `mobj_t*`-returning shape by direct read), and `expr_is_fixed_t_valued` gains `FixedMul`/`FixedDiv`/`FixedDiv2` recognition (`m_fixed.h`'s own real declared `fixed_t` return type) -- without it, a `FixedT`-field write from a bare `FixedMul(..)` call would have wrongly double-wrapped an already-`FixedT` value in another `FixedT(..)`, a second real `rustc` rejection also caught before shipping (matching `A_Tracer`'s own already-established "stub matches the caller's real usage" precedent for `mobjinfo_t.speed`, a plain `i32` field despite reading like fixed-point speed data). Four functions translated: `A_FatAttack1`/`A_FatAttack2`/`A_FatAttack3` (`p_enemy.c`, `A_FatAttack3` reusing the same `mo` local across two independent spawns in sequence, confirming the by-name matching isn't confused by reuse) and `A_SkelMissile` (the same-handle-write shape through a *compound* assign rather than a plain `=`, and a `mo->tracer = actor->target;` write needing no `Some(..)` wrap since the RHS is already `Option`-typed). Five new exact-match tests. Verified compiling for real (`rustc --edition 2021 --crate-type lib`) against real `Arena`/`Handle`/`FixedT` plus hand-written `Mobj`/`World` stand-ins for each -- zero errors.

**Six weapon-family bodies close out a gap the previous entry's own "not yet done" list had wrongly sized as blocked**: that list flagged `weaponinfo[player->readyweapon].field` as blocking the *entire* weapon-attack family in `p_pspr.c` -- measuring it directly (tracing the exact `render_expr` arms each real statement would hit, before writing any test) found this was an overstatement never actually verified against the real renderer: `weaponinfo` is a plain, unregistered global array, so indexing it (`Expr::Index`'s fully generic fallback arm) and then reading a further field off the result (`Expr::Member`'s own generic fallback, since the base is an `Index`, not a bare `Ident` naming `self_param`) already compose correctly with *zero* new code -- no `World` wrapping is needed the way `sectors`/`sides` need, since `weaponinfo` isn't part of the per-level cross-reference memory model at all, just an ordinary program-lifetime table. Likewise `psp->sy` (`pspdef_t`'s own field, the second weapon-fn parameter) needs no registered field-type map entry: the generic `Expr::Member` fallback already treats an unregistered field as plain, correct access, exactly the precedent `player->mo` set earlier. `A_Lower`/`A_Raise`/`A_GunFlash`/`A_FireMissile`/`A_FireBFG`/`A_FirePlasma` (`p_pspr.c`) translated on this basis, all via the pre-existing `render_weapon_fn`, no renderer changes at all -- confirmed by first writing out each expected rendering by hand-tracing the real AST against the current code, then checking every prediction against the real parser output verbatim on the first attempt. `player->ammo[weaponinfo[player->readyweapon].ammo]--;` is the first real corpus use of the already-generic standalone `PostIncDec` statement arm against a doubly-nested index chain (`Index` inside `Index`) rather than a bare identifier -- no special-casing needed, since `render_expr`'s `Index` fallback recurses uniformly regardless of what the base or index expressions themselves are. **The other half of that same "not yet done" line stays a real, narrower gap, not resolved by this**: `A_FirePistol`/`A_FireShotgun`/`A_FireCGun` (same file, same weapon family) all pass `!player->refire` -- negating a plain, non-`bool` `int` field -- directly as a bare function-call *argument*, a third truthiness context this renderer has no C-truthiness-aware handling for yet (only a top-level `if` condition, via `render_bool_expr`, and a `&&`/`||` chain operand, via `render_binary_operand`, are covered so far); today this would silently render as Rust's bitwise `!` on an `i32`, the same documented class of trap `EV_DoLockedDoor`'s own `render_binary_operand` regression already burned this module on once, so it's deliberately left alone rather than guessed at. Six new exact-match tests. Verified compiling for real (`rustc --edition 2021 --crate-type lib`) against hand-written `Player`/`PlayerSpriteState`/`WeaponInfo`/`FixedT`/`Handle`/`Arena` stand-ins -- zero errors, only expected style lints.

**Call-argument truthiness negation closed, `A_FirePistol` translated**: the narrower gap the previous entry left alone -- `!player->refire` used directly as a bare `P_GunShot` call argument, a third context this renderer had no C-truthiness-aware negation for -- investigated first, not guessed at: a corpus-wide grep for the exact shape (`,\s*![a-zA-Z_]+->[a-zA-Z_]+\s*)`, a bare `!field` immediately preceding a call's closing paren) turned up exactly two hits, both `P_GunShot(player->mo, !player->refire)` (`A_FirePistol` and `A_FireCGun`, identical) -- one real idiom, no variation to design around. A new `render_expr` arm handles it: a bare `!` over an `Expr::Member` rooted at `self_param` whose *registered* `self_field_types` entry is neither `"bool"` nor `Option<...>`-shaped renders `field == 0` instead of falling through to the generic (and here wrong) `!field` -- deliberately keyed off the field's registered type, the same "generalize off what's actually known, not the one field name" call `is_option_valued`'s own analogous arm already made for `Option`-typed fields, rather than hardcoding `refire` specifically. An unregistered field (real type unknown to this renderer) still falls through to the old plain-`!` fallback unchanged -- correct for the one real case that depends on it, `!player->cards[idx]` inside a `&&` chain (`cards` is genuinely `bool`-valued but never registered, and indexed via `Expr::Index` besides, so it can't match this new `Expr::Member`-shaped arm at all regardless). `A_FirePistol` now translates end-to-end via the unmodified `render_weapon_fn`, needing no other new capability -- `refire`'s registered type (`i32`) is supplied the same `player_field_types` way `A_Light0`'s own `extralight` already was. `A_FireCGun` stays untranslated as a whole: beyond reusing the identical `P_GunShot` fix (verified in isolation against the real cloned AST, `test_a_fire_cgun_partial_gun_shot_arg_against_real_a_fire_cgun`, since the full function can't render yet), it also computes `weaponinfo[player->readyweapon].flashstate + psp->state - &states[S_CHAIN1]` -- real pointer arithmetic between two `&'static State` references (`psp->state`, a real corpus `state_t*` field, per `pspdef_t`'s own declaration in `p_pspr.h`, checked directly rather than assumed) -- a shape no existing arm computes: the established `sec-sectors` idiom subtracts a *base pointer* from a value-typed index, not two `&'static` references from each other, so this is a genuinely different, not-yet-designed piece, documented here rather than guessed at. One new exact-match test (`test_a_fire_pistol_renders_exactly`) plus the one isolated-fragment test above. Verified compiling for real (`rustc --edition 2021 --crate-type lib`) against hand-written `Player`/`PlayerSpriteState`/`WeaponInfo` stand-ins -- zero errors, only expected style lints.

**`A_FireShotgun` -- the weapon family's third new function this round, unlocked by an unrelated gap, not the truthiness fix**: its own `P_GunShot(player->mo, false)` passes the plain boolean *literal* `false`, not a negated field, which already rendered correctly before any of this round's work (`rust_ident_name` already special-cases `true`/`false` as real Rust literals rather than identifiers needing escaping). What actually blocked it: the first real corpus `for` loop with a *brace-less* single-statement body (`for (i=0 ; i<7 ; i++)\n\tP_GunShot (player->mo, false);`, no `{ }`) -- confirmed to need zero new code once checked, not assumed: `render_for` already delegates its own body to `render_block`, and `render_block` already dispatches a bare (non-`Compound`) statement straight to `render_stmt` (the same fallback `EV_StartLightStrobing`'s own `while` loop already exercises for its own brace-less body, just never yet exercised by a real `for` loop until this one). `int i;` (no initializer) is `render_decl`'s already-ordinary bare-`int` case. One new exact-match test (`test_a_fire_shotgun_renders_exactly`). Verified compiling for real with `rustc` -- zero errors.

**`A_SkullAttack`'s `dist` gap re-investigated, confirmed still genuinely blocked, not resolved by the newer `FixedT` operators**: checked directly against `runtime/fixed.rs` rather than assumed -- only `Div<i32> for FixedT` (representation-level division by a plain scalar, added for an earlier, different idiom) exists; `dist = dist / SKULLSPEED;` needs `FixedT / FixedT`, both operands genuinely fixed-point-scaled (`SKULLSPEED` is `20*FRACUNIT`), where a plain (non-rescaling) division deliberately cancels the shared `FRACUNIT` scale and yields a *plain* `i32` result (a tic count), not another `FixedT` -- a different operator (`Div<FixedT> for FixedT -> i32`) than anything built so far, and a real design decision (when does dividing two `FixedT` values produce a plain scalar instead of another `FixedT`?) needing its own corpus survey before adding, not just a mechanical trait impl. The same statement also has `dest->height>>1`, a bit-shift directly on a `FixedT`-typed field (`FixedT` implements no `Shr` at all) -- a second, independent piece the same statement needs. Left open, not attempted, with both concrete pieces now named for whichever future round takes it on.

**`A_SpawnFly` -- closed out, composing self-removal with target-deref/spawn-mut for the first time**: a corpus-wide survey of every remaining `A_*`/`T_*` definition in `p_enemy.c`/`p_pspr.c`/`p_lights.c`/`p_doors.c`/`p_floor.c`/`p_ceilng.c`/`p_map.c` not already covered by an exact-match test found exactly seven candidates; six needed a genuinely new mechanism (`A_Chase`/`goto`, `A_VileChase`/blockmap iteration, `A_VileAttack`/mutable writes through a pre-existing target-derived mobj, `A_BossDeath`/thinker-list iteration plus the already-rejected `EV_DoFloor`, `T_SlidingDoor`/writable `sides[]` -- likely-dead vanilla-Doom code besides). The seventh, `A_SpawnFly`, looked at first glance like it needed only a small, well-scoped generalization (a second `is_self_removal_call` callee, `P_RemoveMobj(mo)` alongside `P_RemoveThinker(&self->thinker)` -- confirmed by reading `p_mobj.c` directly that `P_RemoveMobj` just calls `P_RemoveThinker` internally) -- but actually tracing its full body (`targ = mo->target; ... fog = P_SpawnMobj(targ->x, ..); ... P_RemoveMobj(mo);`) surfaced that this combination was the *exact* shape `render_fn_impl` had been explicitly rejecting since the `P_SpawnMobj` write-access entry: self-removal composed with target-alias dereferencing and spawned-mobj arena access in the same body, previously left an open "not yet designed" error rather than guessed at. Resolved now that a real function needs it: both `arena.remove(handle)` (self-removal alone's own dedicated `arena: &mut Arena<Thinker>` parameter) and `thinkers.get`/`get_mut` (target-deref/spawn-mut's own `thinkers` parameter) operate on the very same real `Arena<Thinker>` -- a caller can't sensibly be asked for two independently-named `&mut Arena<Thinker>` parameters aliasing the same value, so composing the two needs is exactly *one* shared parameter. `FnBodyContext` gains `self_removal_ident: &'a str` (`"arena"` for the original self-removal-alone shape, unchanged; `"thinkers"` only when `render_fn_impl` has actually composed the two), and the self-removal statement's own hardcoded `"arena.remove(handle)"` text now renders through `format!("{}.remove(handle)", ctx.self_removal_ident)` -- the *only* rendering site self-removal has, so this was cheaper than renaming every `thinkers.get`/`get_mut` call site (six of them) the other direction. `needs_self_handle_value` composed with self-removal has no real corpus example yet, so that narrower combination is still explicitly rejected -- only the combination `A_SpawnFly` actually needs was resolved, not the whole cross product speculatively. Two smaller pieces closed the rest of the function out: `is_bool_returning_call` gains `P_LookForPlayers` (`p_enemy.c`'s own real `boolean`-returning declaration, confirmed by reading it directly, not assumed), a second real corpus function alongside `P_CheckMeleeRange`/`P_CheckSight` used bare as an `if` condition (`if (P_LookForPlayers (newmobj, true))`); and `render_decl` now applies `rust_ident_name` to a plain local's own binding name, not just field/parameter references -- `mobjtype_t type;` is the first plain local this module has translated that's literally named `type` (a Rust keyword), and the binding site itself had never been escaped before (only a *field* access via `rust_field_name`, or a bare-value *reference* via `rust_ident_name`, ever were -- the same class of gap `EV_DoCeiling`'s own unescaped `type` *parameter* was fixed for, just at a different, previously-unexercised site). Verified compiling for real (`rustc --edition 2021 --crate-type lib`) against real `Arena`/`Handle` plus hand-written `Mobj`/`MobjInfo`/`World`/`Thinker` stand-ins and stub forward-referenced functions -- zero errors, only expected style lints. `test_a_spawn_fly_renders_exactly` locks the whole function's real parsed-AST rendering against exact expected text; all 378 pre-existing tests pass unchanged, confirming the composition is a strict superset, not a behavior change for what already worked. **A survey-accuracy note**: an initial subagent survey of the same seven candidates misjudged `A_FireShotgun2` (p_pspr.c) as needing zero new capabilities -- re-checked directly against the real corpus before trusting it, and its own `angle = player->mo->angle;` is actually the *exact* already-documented "dereferencing through `player->mo`" gap (below), not a clean pick; left untranslated, and the "not yet done" list's existing note about that gap already covers it correctly, no correction needed there.

**`player->mo` dereference closed, `A_FireShotgun2` translated**: the gap the previous entry's "not yet done" list flagged (and an earlier survey subagent had wrongly called a clean, no-new-capability pick, then correctly re-flagged as genuinely blocked) is now resolved for the shape that actually appears: `is_self_bare_handle_field` (`render_expr.rs` -- a self-struct field, e.g. `player_t.mo` supplied via `render_weapon_fn`'s own `player_field_types` map since `player_t` isn't struct-mapped, registered `"Handle<Thinker>"`, bare rather than `Option`-wrapped) plus its own `render_expr` arm resolves a further `Member` chain (`player->mo->angle`) through a real `thinkers.get(player.mo)` lookup -- the mirror image of `thing->player`'s own inverse direction, but deliberately kept as a *separate* predicate/arm pair rather than folded into `is_target_tracer_typed`'s existing one: that arm's own `Option`-typed field always needs `.unwrap()` at its point of use, while a bare `Handle<Thinker>` field (corpus-checked: no real call site anywhere null-checks `player->mo` before dereferencing it, the same "no real call site ever null-checks this" reasoning already established for a `P_SpawnMobj` result) needs none, so the two shapes render differently and don't share one arm. Reading/passing `player->mo` *opaquely* (`S_StartSound`'s origin argument, `P_SetMobjState`'s first argument, ...) already rendered correctly before this, via the pre-existing generic `Member` fallback (an unregistered-as-cross-ref field just reads `.mo` plainly) -- only the further chain-through needed the new arm. `render_weapon_fn` now threads a conditional `thinkers: &Arena<Thinker>` parameter through (`body_has_self_handle_field_deref`, a new recursive expression-tree walker mirroring `expr_has_target_deref`'s own shape but keyed off the bare-field predicate, deliberately without an `aliases` parameter since no real corpus function has aliased `player->mo` into a plain local the way `A_SkullAttack` aliases `actor->target`), matching `render_fn_impl`'s own "only when a real body needs it" discipline for its sibling `needs_target_deref`. **A second, independently-motivated gap surfaced by the same function**: `A_FireShotgun2`'s own `angle_t angle; ... angle = player->mo->angle; angle += (P_Random()-P_Random())<<19;` compound-assigns ordinary `int`-typed arithmetic into a genuinely `u32`-typed *local* -- the identical class of bug `lhs_is_u32_self_field`'s existing `as u32` cast already fixes for a *self-struct field* (`A_FaceTarget`'s own `actor->angle += ...`), but that check only ever looked at a `Member`'s registered field type, never a bare local's own declared C type. `FnBodyContext::angle_t_locals` (`collect_angle_t_locals`, mirroring `plain_int_locals`'s own shallow top-level-only scan, just matching `[TypeSpecifier::TypedefName("angle_t")]` instead of `[TypeSpecifier::Int]`) closes it generally rather than patching the one call site. The plain `angle = player->mo->angle;` assignment itself needs no cast at all (both sides are genuinely `u32`, the same "stub matches the caller's real usage" precedent already established for `R_PointToAngle2`'s own return). `bulletslope` (`p_pspr.c`'s own file-scope `fixed_t` global, set by the not-yet-translated `P_BulletSlope`) needed no new handling at all: an unregistered bare identifier already renders as plain opaque pass-through text (the same `MISSILERANGE`/`textureheight[]` precedent), and `FixedT + i32` (`bulletslope + (...)<<5`) already has a real trait impl left over from `A_Tracer`'s own earlier work. One new exact-match test (`test_a_fire_shotgun2_renders_exactly`) -- passed against a fully hand-traced expected string on the very first attempt, confirming the design was right before ever running it, not iterated into shape. Verified compiling for real (`rustc --edition 2021 --crate-type lib`) against hand-written `Player`/`PlayerSpriteState`/`WeaponInfo`/`Handle`/`Arena`/`Thinker`/`Mobj`/`FixedT` stand-ins (`Handle<T>`'s own hand-written `Clone`/`Copy` impls over a `PhantomData<fn() -> T>` marker, matching the real project's own `runtime/arena.rs`, not a naive `#[derive(Copy)]`) and stub forward-referenced functions -- zero errors, only the expected Doom-naming style lints. All 380 tests pass, 379 pre-existing plus the one new, confirming this is a strict superset. **Scope check, not resolved by this**: `A_Punch`/`A_Saw`/`A_WeaponReady` (the three other functions the prior "not yet done" entry named as blocked on this same gap) remain untranslated -- each is blocked on its own *additional*, separate gap (`A_Punch`/`A_Saw` need `linetarget`, a bare nullable global `mobj_t*`; `A_WeaponReady` needs comparing `player->mo->state`/`psp->state` against `&states[N]`, a `&'static State` reference-equality comparison nothing renders yet), confirmed by reading each function's full body rather than assumed resolved just because they share the `player->mo` idiom.

**`A_SkullAttack`'s `dist` gap closed, `Shr<i32> for FixedT` added**: the last concretely-scoped piece the "not yet done" list carried since the target/tracer read-access entry. `dist`'s own C declaration is plain `int`, but `dist = P_AproxDistance(..);` assigns a real `fixed_t`-returning call's result into it (`p_local.h`: `fixed_t P_AproxDistance(fixed_t dx, fixed_t dy);`, confirmed by direct read) -- resolved by a new, deliberately narrow `is_approx_distance_call` check in `render_expr_stmt` (a plain `=` into a `plain_int_locals` name, RHS a bare `P_AproxDistance(..)` call by name) rendering `dist = (P_AproxDistance(..)).0`, extracting the newtype's raw `i32` representation the same way `sec-sectors`'s own `.0 as i32` already does for a different pointer-arithmetic idiom. **Deliberately not the general `expr_is_fixed_t_valued`**: tried first, and caught wrong by the existing test suite itself, not by inspection -- `A_BrainExplode`'s own plain-`int`-declared `x`/`y`/`z` locals are *also* assigned a `FixedT`-valued expression (`mo->x + (...)*2048`) but stay genuinely `FixedT`-typed for the rest of that function (only ever passed on to `P_SpawnMobj`, never used as raw `int` arithmetic), so the general check's "add a cast whenever the RHS is FixedT-valued" answer is right for `A_BrainExplode` and wrong for `A_SkullAttack` -- there's no way to tell the two shapes apart from the assignment site alone, only from what `dist` is used *for* afterward (`dist / SKULLSPEED`, `dist < 1`, both genuinely `int`-typed uses). Once `dist` is genuinely `i32` throughout, `dist / SKULLSPEED` (plain `i32` division -- `SKULLSPEED` is itself an unexpanded macro identifier, rendered as opaque pass-through text, the same `MISSILERANGE` precedent) and the function's final `(...) / dist` (`Div<i32> for FixedT`, already implemented) both need zero further new code. `dest->height>>1` -- a bit-shift directly on a `FixedT`-typed field -- needed the one genuinely new runtime piece: `Shr<i32> for FixedT` (`runtime/fixed.rs`), a thin wrapping bit-shift of the representation that stays `FixedT` (preserving the `FRACUNIT` scale exactly, the same idea `Div<i32>` already established via true division instead of a shift). Everything else in the function (the `dest` target-alias dereference chain, the `an = actor->angle >> ANGLETOFINESHIFT; ... FixedMul(SKULLSPEED, finecosine[an])` idiom, `S_StartSound(actor, ..)`, `actor->flags |= MF_SKULLFLY;`) reused machinery already proven by `A_FatAttack1`/`A_Pain`/`A_FaceTarget` with zero changes. `A_SkullAttack` now renders end-to-end for the first time, closing the isolated-fragment test's own long-standing "not investigated here" note (`test_target_tracer_local_alias_against_real_a_skull_attack`, kept alongside the new whole-function test since it still pinpoints the target-alias piece on its own) into a full exact-match test (`test_a_skull_attack_renders_exactly`) -- passed against a fully hand-traced expected string on the first attempt. Verified compiling for real (`rustc --edition 2021 --crate-type lib`) against hand-written `Mobj`/`MobjInfo`/`Handle`/`Arena`/`Thinker`/`World`/`FixedT` stand-ins and stub forward-referenced functions -- zero errors, only expected style lints.

**Real write access through `target`/`tracer`, `A_VileAttack` translated -- two genuinely different borrow conflicts found, only by actually compiling, not by inspection**: generalizes the existing *read-only* `target`/`tracer` chain-through (`70b94a0`'s entry, above) to a write counterpart. A new `render_expr_stmt` arm, keyed off the same `is_target_tracer_typed` predicate the read arm already uses (so it covers both a direct `actor->target->field` write and the same chain through a local alias, e.g. `fire->x = ..` once `fire = actor->tracer;`), renders `actor->target->momz = 1000*FRACUNIT/actor->target->info->mass;` through a fresh `thinkers.get_mut(actor.target.unwrap())` call at the write's own point of use -- the write-side mirror of the read arm's `thinkers.get(..)`. `1000*FRACUNIT/actor->target->info->mass` needed no `FixedT(..)` wrap at all: `expr_is_fixed_t_valued` already recognizes bare `FRACUNIT` as `FixedT`-valued, and the already-existing `Div<i32> for FixedT` renders the whole division as a genuine `FixedT` result through ordinary Rust operator overloading, not a special case. **First borrow conflict, expected and designed for up front**: `actor->target->info->mass`, read inside the RHS of the write to `actor->target->momz`, is a *different field of the same* `actor->target` value -- naively re-resolving it through a second, independent `thinkers.get(actor.target.unwrap())` would be a real second borrow of `thinkers` while the write's own `thinkers.get_mut(..)` is still live for the whole block, the identical class of conflict `same_handle_write` already solved for a `P_SpawnMobj`-fresh local. Generalized here as `FnBodyContext::same_target_write`/a new `target_tracer_key` helper: unlike `same_handle_write`'s plain `Option<&'a str>` identifier match, this is keyed by a *discriminator* (the field name for a direct `self.target`/`self.tracer` write, or the alias's own identifier for a local-alias write) rather than a boolean, since the written-to base can itself be a `Member` (`actor->target`), not just a bare local -- and a *different* target/tracer value appearing in the same RHS must be told apart from the one actually being written. **Second borrow conflict, only found by actually compiling the first draft with `rustc`, not anticipated**: `fire->x = actor->target->x - FixedMul(24*FRACUNIT, finecosine[an]);` writes through the alias `fire` while its own RHS reads `actor->target` -- a *genuinely different* target/tracer value, so `same_target_write`'s keying correctly does *not* resolve it to `fire`'s own bound `m` (right call), but the naive first draft then rendered that read's own `thinkers.get(..)` call *inside* the write's `if let Some(Thinker::Mobj(m)) = thinkers.get_mut(fire.unwrap()) { .. }` block -- still a second, live borrow of the same `thinkers`, a real `rustc` `E0502` the compile step actually raised, not a hypothetical this round anticipated in the design. Fixed the way `rustc`'s own diagnostic suggests: a new `expr_has_other_target_deref` predicate (mirroring `expr_has_target_deref`'s tree walk, but the leaf check additionally excludes the write's own key) detects a differently-keyed target/tracer read anywhere in the RHS, and when found, the RHS is precomputed into its own `let __rhs = ..;` *before* the mutable borrow starts, with the `if let` block itself only ever assigning from that already-computed `__rhs` -- splitting what was one C statement into two Rust statements (`render_stmt`'s existing `Vec<String>`-per-statement return shape already supports this with no signature change: the write arm's returned text is just `"let __rhs = ..; if let .. { m.field = __rhs; }"`, one `String`, still wrapped in the caller's own trailing `;` the same as every other `render_expr_stmt` result). `render_fn_impl` also gains `needs_target_write` (`body_has_target_write`/`stmt_has_target_write`, a statement-level walk mirroring `body_has_self_removal`'s shape rather than `expr_has_target_deref`'s full expression-tree one -- this module's own `render_expr_stmt` doc comment already establishes Doom's tick functions never nest an assignment inside a larger expression, so a statement-level walk is sufficient) to upgrade the generated `thinkers` parameter from `needs_target_deref`'s read-only `&Arena<Thinker>` to a real `&mut Arena<Thinker>` once a body writes through a target/tracer chain, not just reads through one. `A_VileAttack` (`p_enemy.c`) translates end-to-end: one new exact-match test, matched against a fully hand-traced expected string on the *first* attempt (confirming the design of the write arm and `same_target_write` was right before ever running it) -- the *second* borrow conflict only surfaced afterward, once the standalone `rustc --edition 2021 --crate-type lib` verification pass (real `Arena`/`Handle`/`FixedT` plus hand-written `Mobj`/`MobjInfo`/`World` stand-ins and stub `A_FaceTarget`/`P_CheckSight`/`S_StartSound`/`P_DamageMobj`/`FixedMul`/`P_RadiusAttack` functions) actually ran against the first draft's naive rendering and rejected it for real; fixed, then re-verified clean. 383/383 tests passing.

**`T_PlatRaise` (`p_plats.c`) -- structurally close to `T_MoveCeiling`/`T_VerticalDoor`, renders end-to-end through wholly pre-existing machinery, no new capability needed**: a self-struct tick function (`switch` on a `plat_e` status field, shared `case` labels in a nested `switch` on `plat->type`, the `(mobj_t*)&plat->sector->soundorg` sound-origin cast, `T_VerticalDoor`'s own `!--plat->count` negated-pre-decrement idiom, a self-field crossref chain-through reading a *different* struct's field through `plat->sector`). Confirmed by reading the whole function first, not assumed from its opening lines: the outer `switch(plat->status)` has no `default:` at all (four explicit `case` labels -- `up`/`down`/`waiting`/`in_stasis`, the last two adjacent with no `break` between them, a genuine fallthrough into `case in_stasis: break;`'s own empty body), so it gets a synthetic trailing `_ => {}`; the inner `switch(plat->type)` genuinely does have a `default: break;`, so its own already-empty trailing arm *is* that `_`, no synthetic arm added on top. `res == crushed && (!plat->crush)` is the first real corpus use of a bare `Unary::Not` operand over a genuinely `bool`-typed self field (not `Option`-typed, not plain `int`) inside a `&&` chain -- confirmed still routing through `render_binary_operand`'s own deliberately-narrow plain-`!` fallback (documented since `A_ReFire`, never exercised by a real `bool` field before this), producing the correct `!plat.crush`, not a wrongly-cast `== 0`. `P_RemoveActivePlat(plat)` confirms (a second time, after `T_MoveCeiling`'s own `P_RemoveActiveCeiling`) that a similarly-named but structurally different removal helper never false-positives `is_self_removal_call`'s narrow `P_RemoveThinker(&self->thinker)` shape. Hand-traced expected output matched the real renderer on the first attempt. Verified compiling for real (`rustc --edition 2021 --crate-type lib`) against hand-written `Plat`/`World`/`Sector` stand-ins and stub `T_MovePlane`/`S_StartSound`/`P_RemoveActivePlat` functions -- zero errors, only expected style lints. `test_t_plat_raise_renders_exactly`. 384/384 tests passing.

**Read-only `Arena<Thinker>` iteration -- designed and implemented, closing part of a gap deferred for many rounds**: surveyed all five real corpus candidates (`A_PainShootSkull`, `A_Look`, `A_KeenDie`, `A_BrainAwake`, `A_BossDeath`) before designing anything, rather than deferring again by default. `A_PainShootSkull` is the only one of the five with no *second*, unrelated blocker stacked on top of the iteration gap itself: `A_KeenDie` also needs a synthetic stack-local `line_t` passed by address into `EV_DoDoor` (the same open design question `A_BossDeath` already carries, see below); `A_BrainAwake`/`A_BossDeath` also need global mutable-variable writes (`numbraintargets`/`braintargeton`/`braintargets[]`, nothing this renderer has ever done before); `A_Look` needs `goto`. Scoped this round to exactly the shape `A_PainShootSkull` needs, not the whole family. `Arena::iter()` (`runtime/arena.rs`) is a plain forward scan over live slots (`self.slots.iter().filter_map(Option::as_ref)`) -- correct in the same insertion order the original's own `thinkercap` list traversal visits, since `insert` always appends at the true end and a tombstoned slot stays dead permanently for the rest of that arena's lifetime (this module's own already-documented design), so no bookkeeping is needed beyond the `filter_map` itself, unlike `run`'s own take-then-put-back dance (nothing here ever hands a slot's value back to the caller by `&mut`, so there's no reentrancy hazard to guard against). `render_thinker_list_scan` (`function_body.rs`) recognizes the whole real corpus two-statement raw-list-walk idiom (`curvar = thinkercap.next; while (curvar != &thinkercap) { if (curvar->function.acp1 == (actionf_p1)P_MobjThinker && <cond>) <body>; curvar = curvar->next; }`, confirmed against the real parsed AST before writing anything) and renders it as one `for thinker in thinkers.iter() { if let Thinker::Mobj(m) = thinker { if <cond'> { <body'> } } }`, consuming both source statements via `render_compound_items`'s new pair-lookahead (the same "look ahead across two adjacent top-level statements" shape its own `embedded_ctor` detection already established, generalized from a single fixed trigger to any matching pair). The tag check is stripped entirely, not rendered: matching `Thinker::Mobj`'s own closed-`enum` variant already *is* the check, under this project's own polymorphism design -- there's no C vtable left to compare a function pointer against. The `((mobj_t*)curvar)->type` cast-then-dereference idiom needed no dedicated `Cast`-aware rendering at all: `Expr::Cast`'s own existing pure-passthrough arm already reduces it to a bare `Ident(curvar)` before the new `FnBodyContext::thinker_scan_alias` rename (`curvar -> "m"`) ever needs to look at it -- a plain rename, not another `Arena` lookup, since `m` is already `&Mobj` straight out of the pattern match, unlike every other cross-reference alias this module tracks (`target`/`tracer` locals, `P_SpawnMobj` locals), which all hold a `Handle<Thinker>` needing one. **Deliberately scoped narrowly, not generalized to the sibling shape**: `A_KeenDie`/`A_BrainAwake` use a structurally different per-item filter (`if (!tagcheck) continue; <rest of the loop body, unconditional>`, not `A_PainShootSkull`'s combined `if (tagcheck && cond) stmt;`) -- left unhandled since both remain separately blocked regardless (the synthetic-`line_t` and global-write gaps above), so generalizing now would be guessing ahead of a real caller. **This still doesn't close out `A_PainShootSkull` end-to-end**: `prestep` (declared plain `int`, assigned a `FixedT`-valued expression, then used as `FixedT` in `FixedMul`) is a fresh instance of the same ambiguous-typing gap `A_SkullAttack`'s own `dist` and `A_BrainExplode`'s `x`/`y`/`z` locals already surfaced, still unresolved generically -- so this is proven the same way `T_VerticalDoor`'s own self-removal/`NULL`/shared-case-label pieces were before the whole function rendered: against the *real* cloned AST subtree (`A_PainShootSkull`'s own items 9-10), not a fabricated stand-in (`test_thinker_list_scan_against_real_a_pain_shoot_skull`). Verified compiling *and running* for real (`rustc --edition 2021 --crate-type bin`) against the real `Arena`/`Handle` plus hand-written `Mobj`/`Thinker` stand-ins -- zero errors, and the rendered scan produces the correct count over a mixed arena (real vs. non-matching `Mobj`s vs. a non-`Mobj` thinker), not just syntactically valid output. 386/386 tests passing.

**Global `linetarget` read access, self-handle-field writes, `A_Punch` translated -- three new pieces, all measured against the first real weapon-action body needing them**: `World::linetarget: Option<Handle<Thinker>>` joins `sectors`/`sides`/`players` (`p_local.h`'s own `extern mobj_t* linetarget;`, the last thing `P_AimLineAttack` hit, corpus-wide) -- `render_weapon_fn` registers it into `extra_cross_ref_idents` whenever `body_has_linetarget_ref` (a new full recursive expression/statement walk, mirroring `expr_has_self_handle_field_deref`'s own shape) finds a bare reference anywhere in the body, which turns out to be *enough* on its own for the existing `is_target_tracer_typed`/`is_option_valued` machinery to treat it exactly like a `target`/`tracer` alias for free -- neither needed a single line of change. `render_expr`'s own `Ident` arm gains one narrow case so the bare name itself renders as `world.linetarget`, not plain `linetarget` (a genuine global, not a real Rust local). `render_bool_expr` gains a bare, non-negated `Option`-valued-identifier truthiness arm (`.is_some()`) -- every earlier `Option`-typed truthiness check in the corpus so far only ever appeared negated (`EV_DoLockedDoor`'s own `!p`), so `if (linetarget)`'s un-negated shape had no arm at all yet. **The function's own core new capability**: writing *through* `player`'s own bare `Handle<Thinker>` field (`player->mo->angle = R_PointToAngle2(..);`, `is_self_bare_handle_field`'s write-side counterpart, mirroring the read arm `A_FireShotgun2` already established) -- a new `render_expr_stmt` arm, plus a matching `same_handle_write`-keyed read arm (keyed by the self field's own name, `"mo"`, since the base here is a `Member` rather than a bare local the way `A_FatAttack1`'s own `mo` is) so `player->mo->x`/`.y` read inside that same write's RHS reuses the write's own bound `m` instead of a second, conflicting borrow. **A genuinely new composition, caught by tracing through before shipping rather than only at the eventual `rustc` step**: `A_Punch`'s own RHS mixes a same-key read (`player->mo->x`/`.y`) *and* a different-key one (`linetarget->x`/`.y`) in one statement -- a shape `A_VileAttack`'s own target/tracer write arm never had to handle, since its one real corpus example never mixed the two. `expr_has_other_target_deref` already detects the different-key read correctly (once `linetarget` is registered as an `Option<Handle<Thinker>>` alias, it already counts as "other" to that predicate, no changes needed there either) and triggers the existing hoist-to-`let __rhs`-before-the-mutable-borrow path -- but naively reusing `rhs_ctx`'s `same_handle_write` shortcut *inside* that hoisted rendering would reference an `m` that doesn't exist yet at that point (nothing is mutably borrowed until the `if let ... get_mut(..)` starts) -- fixed by rendering the hoisted path's RHS through the *plain* context instead (every read, same-key included, gets its own ordinary fresh `thinkers.get(..)` call there, which is sound since nothing is mutably borrowed yet). `render_weapon_fn`'s own `world`/`thinkers` parameters are threaded through conditionally (`body_has_self_handle_write`, mirroring `render_fn_impl`'s own `needs_target_write` upgrade, upgrades `thinkers` to `&mut Arena<Thinker>` whenever the body writes through `player->mo`). **Two smaller pieces the same function surfaced**: `player->powers[pw_strength]` needs an explicit `as usize` cast on its own index (a bare enum-constant identifier, fixed `i32` elsewhere in the generated crate the same way `finecosine`/`finesine`'s own indexed identifiers already are, not a fresh local Rust can infer as `usize` here) -- scoped by the array field's own name (`powers`), the same "hand-match the one real array" style; and a bare `Expr::Index` used for truthiness (`if (player->powers[pw_strength])`) had no `render_bool_expr` arm at all yet, now a plain `!= 0`. **`A_Saw` (`p_pspr.c`) investigated as a second real caller of the same machinery, deliberately not attempted**: reuses everything above unchanged, but needs its own additional, genuinely new gap -- `-ANG90/20`, a real corpus example of unary minus over an `angle_t`/`u32`-valued expression (`ANG90` is a plain hex-literal macro, already rendered as opaque pass-through text with no type tracking) -- Rust's `u32` has no `Neg` impl, and no existing corpus precedent establishes how this should render (`wrapping_neg()`? something else?), so left for a future round rather than forcing an ad-hoc fix now. Verified compiling for real (`rustc --edition 2021 --crate-type bin`) against hand-written `Player`/`PlayerSpriteState`/`World`/`Mobj`/`Thinker`/`Arena`/`Handle` stand-ins and stub `P_AimLineAttack`/`P_LineAttack`/`S_StartSound`/`R_PointToAngle2` functions (matching `P_AimLineAttack`'s real `fixed_t` return type, confirming `slope`'s own plain-`int` declaration needs no special handling at all -- unlike `A_SkullAttack`'s own `dist`, `slope` is only ever used as `FixedT` again afterward, so Rust's ordinary deferred-`let` inference already gets it right) -- zero errors. `test_a_punch_renders_exactly`. 387/387 tests passing.

**`goto`/label support, `A_Look` translated -- closes the oldest open item on the "not yet done" list (flagged since the `states[]` entry, several rounds ago)**: `A_Look`'s own two `goto seeyou;` sites both jump *forward* over an intervening `if (!P_LookForPlayers(..)) return;` to reach one shared tail -- Rust has no `goto` at all, but this exact "every goto reachable in the label's own prefix targets that one label" shape is representable with a labeled block. `render_compound_items` gains a new transform, checked before every other per-item shape it already recognizes: `find_forward_goto_label` locates a top-level `Stmt::Labeled` in the item list; `collect_goto_targets` (a plain recursive scan through `if`/`switch`/`case`/`default`/`while`/`for`/nested blocks, mirroring `body_has_self_removal`'s own shape) confirms every `goto` reachable in the prefix before it targets that exact label, refusing the transform (falling through to render each item as normal, where a real `Stmt::Goto` reaching `render_stmt` with no matching label now fails loudly) rather than guessing at any other shape. When it matches, the prefix renders inside `'label: { .. }` under a new `FnBodyContext::active_goto_label` (added to all eighteen explicit `FnBodyContext` construction sites the same mechanical way every prior field addition to this struct has been), with each matching `goto` becoming `break 'label;` (`Stmt::Goto`'s own new `render_stmt` arm); the label's own statement and everything after it then render normally, at the *same* depth as before the label ever existed -- "falling out of the block" already reaches the same place "falling through to the label" did in the original, so the label itself needs no marker of its own in the output. `targ = actor->subsector->sector->soundtarget;` needed two more pieces, both narrow and by-name in this module's usual style: `World::subsectors` (a `SubsectorId`-indexed `Vec`, the same per-level shape as `sectors`/`sides`, joined for the same "a real body needs it" reason each earlier `World` field did), and a `Member` arm resolving `X->subsector->sector` (a self field whose *own* field is itself cross-reference-typed) into `world[..]` correctly -- the generic fallback only ever checks whether a `Member`'s immediate base is `self_param` itself, so a second level of self-struct chaining needed its own arm, the same reasoning `line->frontsector` already established for a different pair of fields. `sector_t.soundtarget` (`mobj_t*`) turned out to be exactly the same "always `Mobj`-shaped when present" kind of value `self.target`/`.tracer` already are (corpus-checked, not assumed) -- `is_target_tracer_typed` gained a narrow arm recognizing the whole `subsector->sector->soundtarget` chain by shape, which was *enough* on its own for `collect_target_tracer_aliases` (already generic over "any RHS `is_target_tracer_typed` accepts," no changes needed there at all) to register `targ` as an ordinary target/tracer-style local alias, so `targ->flags` dereferences through the pre-existing `Arena`-lookup machinery unchanged. The one genuinely new rendering gap: `targ && (targ->flags & MF_SHOOTABLE)` uses the bare, non-negated local directly as a `&&` operand -- `render_binary_operand` only ever special-cased a `Member`/non-comparison-`Binary` operand for real C truthiness before, not a bare `Option`-valued `Ident`, so `targ` alone would have rendered as plain (invalid -- `Option` isn't a valid `&&` operand) identifier text; fixed by routing that shape through `render_bool_expr` too, reusing the `.is_some()` arm `linetarget` already needed. The switch/`S_StartSound` tail is structurally identical to the already-translated `A_Scream` (same three-case sound-selection `switch`, same `MT_SPIDER`/`MT_CYBORG` full-volume check) and needed nothing new. **Verified compiling for real** (`rustc --edition 2021 --crate-type lib`) against real `World`/hand-written `Mobj`/`MobjInfo`/`Sector`/`Subsector`/`Arena`/`Handle`/`Thinker` stand-ins and stub forward-referenced functions -- zero errors on the function's own logic. **One genuine finding, not a new bug**: compiling the *whole* function surfaces a `rustc` move error at its final line (`P_SetMobjState(actor, ..)`, reading `actor` again after `S_StartSound(actor, sound)` "moved" it) -- traced to a pure artifact of this verification's own necessarily-generic `S_StartSound` stub (needed, as already documented in `EV_VerticalDoor`'s own entry, since this one function calls `S_StartSound` with both `None` and a real reference and no single concrete signature satisfies both without `Some(..)`-wrapping one of them), not a new problem: confirmed by isolating the two halves separately (the function compiles clean with the trailing `P_SetMobjState` call removed; separately, a minimal repro shows a *concrete*, non-generic `&Mobj`-typed stub lets `actor` be reborrowed rather than moved, with no reuse conflict at all) -- once `S_StartSound`'s real signature is eventually decided (the same already-documented cross-function-wiring gap), this consequence resolves for free rather than needing its own fix. `test_a_look_renders_exactly`. 388/388 tests passing.

**`render_while`'s general case surveyed, found not to need generalizing yet -- a documented negative result, not a gap**: before touching `render_while` (its own "not yet done" line had carried "only the `(x = f()) CMP y` idiom is recognized" for many rounds), measured what `while` condition shapes actually appear across the corpus with a throwaway survey (`examples/while_shape_survey.rs`, deleted before committing per this project's usual practice) rather than assuming a generalization was overdue. Scoped to the `p_*.c` files this project's own near-term roadmap actually draws from (the wider corpus, including `d_net.c`/`w_wad.c`/`wi_stuff.c`, turned up other shapes, but nothing on this project's own translation path reaches those files yet): of 15 real `while` loops, 7 are the exact already-handled `(secnum = P_FindSectorFromLineTag(line,secnum)) >= 0` idiom (verbatim, all 6 tagged-sector trigger functions); 3 are `currentthinker != &thinkercap`, the raw thinker-list-scan condition -- but this shape *never actually reaches* `render_while` at all, since `render_compound_items`'s own two-statement lookahead (`is_thinker_list_scan_init`/`is_thinker_list_scan_while`) intercepts the whole `init;while(...){..}` pair first; the remaining 5 are all `while (x--)` (`numspechit`/`count`, a post-decrement-as-condition counting-down idiom), appearing only in `p_map.c`/`p_maputl.c`/`A_VileChase`'s own blockmap scan -- none of them in a function anywhere near this project's current translation frontier (`A_VileChase` itself is already known-blocked on unrelated blockmap-iteration machinery this renderer doesn't have yet). **Conclusion: generalizing `render_while` right now would be guessing ahead of a real caller**, the exact discipline this project has followed throughout -- left exactly as it is, with the survey's own finding (the `while (x--)` idiom, and where it lives) recorded here so a future round doesn't have to re-derive it from scratch.

**`-ANG90/20`, `A_Saw` translated -- the last concretely-scoped piece the `A_Punch` entry had carried**: investigated first, not guessed at, and the investigation *changed* the design the task brief itself had proposed -- a naive `u32::wrapping_neg`-based `Neg` impl (what "wrapping arithmetic, matching C's unsigned overflow semantics" would suggest) is provably *wrong* here, not just unproven: hand-computing both paths shows dividing the wrapped-unsigned bit pattern by 20 gives a different number than dividing the *signed* `-1073741824` by 20 and reinterpreting *that* as unsigned only afterward -- confirmed numerically (`4241280205` either way *only* when the division stays in the signed domain; a plain `wrapping_neg`-then-unsigned-divide gives `161061273` instead, a real, silently-wrong-behavior divergence a less careful implementation would have shipped). A second real corpus example, found by grepping the *whole* corpus for `-ANG` rather than trusting the one `A_Saw` occurrence was unique, confirms this is a deliberate idiom, not a coincidence: `p_user.c`'s own `delta > (unsigned)-ANG5` reinterprets the exact same way, just with an explicit C cast instead of relying on the implicit usual-arithmetic-conversion this one does. `render_expr` gains a narrow arm (matched, this codebase's usual style, by the numerator being a bare unary-negated identifier whose name starts with `"ANG"`, divided by a literal) rendering `-ANG90/20` as `(-(ANG90 as i32) / 20) as u32`: cast to `i32` *before* negating (real signed negation, `i32` has `Neg`; `u32` doesn't), divide in the signed domain (truncating toward zero, matching C), and only convert to `u32` at the very end, at the point this value meets the real `angle_t` difference it's compared against -- confirmed bit-for-bit identical to C's own computation via a standalone numeric check, not just "compiles." Every other piece of the function reuses machinery already proven by `A_Punch` verbatim, needing zero changes: `player->mo->angle`/`.x`/`.y` reads and writes (`is_self_bare_handle_field`, including the write arm's existing support for a *compound* op, `-=`/`+=`/`|=`, not just plain `=`), the bare `angle_t angle;` local's own compound-assign `as u32` cast (`angle_t_locals`), and `linetarget` truthiness/dereferencing (`!linetarget`, the negated counterpart of `A_Punch`'s own un-negated `if (linetarget)`, already covered by the pre-existing `Option`-aware `Unary::Not` arm in `render_bool_expr`). `angle - player->mo->angle`, compared against either the new `-ANG90/20` arm or a plain (positive) `ANG90/20`/`ANG90/21` division, needed no changes to the comparison machinery itself at all -- the new arm slots into the existing generic `Binary`-operand rendering path with zero special-casing required at the comparison level, composing for free. Verified compiling for real (`rustc --edition 2021 --crate-type lib`) against hand-written `Player`/`PlayerSpriteState`/`World`/`Mobj`/`Thinker`/`Arena`/`Handle` stand-ins and stub forward-referenced functions -- zero errors, only expected style lints. `test_a_saw_renders_exactly`. 389/389 tests passing.

**`state_index`, `A_FireCGun` translated -- the last piece `A_FirePistol`'s own entry had left open**: `weaponinfo[player->readyweapon].flashstate + psp->state - &states[S_CHAIN1]` is real C pointer arithmetic between two `state_t*` values -- a genuinely different shape from `sec-sectors`'s own trick (that one subtracts a base pointer from a value that's *already* an index newtype; `state_t*` instead maps to a real `&'static State` reference, `struct_fields.rs`'s own established decision, which has no index of its own to read back out). Rather than modeling real pointer arithmetic, the whole expression is algebraically simplified before rendering: `int + state_ptr - state_ptr` is C's own "advance a state pointer by N states, then measure its distance from a known base" idiom, and since `&states[S_CHAIN1]`'s own index *is* `S_CHAIN1` by construction (no arithmetic needed to know it), the expression reduces to one real pointer-to-index lookup, `flashstate + state_index(psp.state) - S_CHAIN1`, not two. `state_index` (`states_data.rs`, new -- `render_state_index_fn`) is hand-rendered literal text, not corpus-mapped, the same category as `world.rs`'s own `render_world_struct`: new, invented Rust-only infrastructure with no direct corpus counterpart. Implemented via plain pointer-to-`usize` address arithmetic (`(s as *const State as usize - &STATES[0] as *const State as usize) / std::mem::size_of::<State>()`) -- safe, well-defined Rust for two pointers known to be elements of the *same* static array (no dereferencing of a possibly-invalid pointer happens, only integer arithmetic on addresses), matching C's own pointer-subtraction semantics without needing an `unsafe` block. `render_expr`'s new arm identifies the state-pointer operand of the inner `+` by its own field name, `"state"` (this codebase's usual by-name style, since both operands are plain `Member` reads and can't be told apart by AST shape alone). **A genuinely simpler function than its two recent siblings**: `player->mo` is only ever passed opaquely in `A_FireCGun` (never dereferenced through), and `psp->state` needs no `Arena` lookup at all, so `render_weapon_fn`'s own signature-extension logic correctly adds neither `world` nor `thinkers` -- `pub fn A_FireCGun(player: &mut Player, psp: &mut PlayerSpriteState)`, confirming those flags measure real body need rather than defaulting to "on." Everything else in the function (the negated `Expr::Index` early return, the already-fixed `P_GunShot(player->mo, !player->refire)` call-argument truthiness) reuses machinery proven correct by the earlier isolated-fragment test (`test_a_fire_cgun_partial_gun_shot_arg_against_real_a_fire_cgun`, kept alongside the new whole-function one). Verified compiling for real (`rustc --edition 2021 --crate-type lib`) against a hand-written `Player`/`PlayerSpriteState`/`State`/`WeaponInfo`/`Arena`/`Handle`/`Thinker` stand-in (including a real `state_index` matching `states_data.rs`'s own rendering verbatim) and stub forward-referenced functions -- zero errors, zero warnings. `test_a_fire_cgun_renders_exactly`, `test_renders_state_index_fn`. 391/391 tests passing.

**`A_PainShootSkull` closed out end-to-end -- the last function this section had been carrying since the `states[]` entry, and the first with a genuinely new function-signature shape**: three real pieces, none guessed at. (1) `render_fn_with_scalar_param` -- `A_PainShootSkull(mobj_t* actor, angle_t angle)` isn't itself a `state_t.action` entry (a plain helper called by the already-translated `A_PainAttack`/`A_PainDie`), and its second, genuinely scalar parameter needed `render_fn`'s own signature generation extended for the first time (a real fork, not a quick fix, exactly as this needed careful scoping before starting) -- confirmed to need *no* new `FnBodyContext` tracking at all: a bare reference to `angle` already renders correctly via Rust's own static typing, unlike a self-struct field, which genuinely needs cross-reference/`Option` awareness the renderer supplies. A thin wrapper over the existing `render_fn_impl`, the same pattern `render_bool_fn` already established, so `render_fn`'s own ~40 existing single-parameter call sites are untouched. (2) `thinker_scan_var_names` -- rendering the *whole* function (not just the already-proven two-statement scan fragment from several rounds ago) surfaced a real gap the isolated fragment test could never have caught: `thinker_t* currentthinker;`'s own declaration, sitting *before* the scan pair that replaces it wholesale with a real Rust `for` loop introducing its own `thinker`/`m` bindings, would otherwise render as an orphaned `let mut currentthinker;` with nothing ever assigning or reading it -- confirmed a genuine `rustc` "type annotations needed" rejection, not just dead code, by compiling the naive version first. Fixed by pre-scanning each block (`render_compound_items`'s own new helper) for every such variable and skipping its matching declaration, the same "drop a provably-dead local" treatment already given an `embedded_ctor` variable, just resolved one level up since `render_decl` itself has no visibility into a *later* sibling statement. (3) `mobjinfo[MT_SKULL].radius` -- `mobjinfo` already has a real, decided rendering elsewhere in this codebase (`mobjinfo_data.rs`'s own `pub static MOBJINFO`), so a bare reference now renames to match it (`render_expr`'s `Expr::Ident` arm, the same idea `linetarget`'s own rename already established) instead of keeping the still-undecided-elsewhere lowercase spelling `weaponinfo`/`finecosine` keep; the bare `MT_SKULL` index needs the same `as usize` cast `pw_strength`'s own array-index use already established. `P_TryMove` (`p_local.h`'s own real `boolean P_TryMove(mobj_t*, fixed_t, fixed_t)` declaration, confirmed by direct read, not assumed) joins `is_bool_returning_call`'s list, the same way `P_LookForPlayers` did. **`prestep`'s own ambiguous plain-`int`-vs-`FixedT` typing -- flagged for several rounds as the function's last remaining blocker, but tracing its actual arithmetic through shows it never needed anything at all**: unlike `A_SkullAttack`'s `dist` (genuinely mixing `FixedT`- and plain-`int`-typed uses of the same local, needing an explicit unwrap), `prestep` is assigned once (`4*FRACUNIT + 3*(actor->info->radius + mobjinfo[MT_SKULL].radius)/2` -- `mobjinfo_t.radius` is plain `int` in the corpus itself, confirmed by direct read, not `fixed_t`, so the inner `3*(...)/2` term stays genuine `i32` throughout, and only the outer `+` combines it with the `FixedT`-valued `4*FRACUNIT` via the already-established `Add<i32> for FixedT` overload) and used only as `FixedT` afterward (`FixedMul(prestep, ..)`), the exact same pattern `x`/`y`/`z`'s own `fixed_t`-declared locals already have -- Rust's ordinary deferred-`let` inference resolves it correctly with zero renderer-side casting, the same way it already does for them; this section's own earlier pessimistic framing turned out to be an assumption never actually checked against the real arithmetic. `newmobj->x`/`.y` (a spawned local's own fields, read opaquely as `P_TryMove` arguments) and `newmobj->target = actor->target;` (a spawned local's field write, RHS already `Option`-typed so no `Some(..)` wrap needed) both reuse machinery proven by `A_Tracer`/`A_FatAttack1` verbatim, no changes. Verified compiling *and running* for real (`rustc`, both `--crate-type lib` for the whole rendered function and a small standalone repro proving the pre-fix version really does hit `E0282`) against real `Arena`/`Handle` plus hand-written `Mobj`/`MobjInfo`/`World`/`Thinker` stand-ins and stub forward-referenced functions -- zero errors, and the early-return branch (21 skulls present, `count > 20`) ran to completion without panicking against a real mixed arena. `test_a_pain_shoot_skull_renders_exactly`; the earlier isolated-fragment test (`test_thinker_list_scan_against_real_a_pain_shoot_skull`) is kept alongside it, since it still pinpoints the scan piece on its own. 392/392 tests passing.

**`A_BFGSpray` closed out -- the last piece this section's own "not yet done" list had left open for it beyond `linetarget` access itself**: tracing the actual body (rather than the earlier entry's speculative list of maybe-blockers) found exactly one real gap, not three. `render_for`'s own translation of `for (init; cond; step) body` to a plain `while cond { body; step; }` only ever reaches `step` by falling off the bottom of `body` -- a bare `continue;` anywhere inside (`A_BFGSpray`'s own `if (!linetarget) continue;`, skipping the rest of an iteration once an offset angle misses) lowers to Rust's own `continue`, which jumps straight back to `cond` and silently skips `step` -- a real behavior change (every remaining offset angle after the first miss would be skipped, not just that one), confirmed by compiling the naive translation first and reasoning through what it actually does, not assumed. Fixed with a new `FnBodyContext` field, `active_for_continue_step: Option<&'a str>` -- `render_for` sets it to its own already-rendered step text for its body's own context, and `Stmt::Continue`'s own `render_stmt` arm emits that step immediately before the `continue;` it renders whenever it's `Some`; `render_while` clears it for its own body's context, since a `continue` reached there targets the `while` itself, not some outer `for` it happens to be nested in (a nested `for`'s own recursive call to `render_for` needs no matching reset, since it always overwrites the field with its own step before rendering its own body regardless); a `continue` reached through a `switch`'s own arms is deliberately left alone (`render_switch` passes `ctx` through unchanged), since it still targets the nearest enclosing *loop*, not the switch. The old up-front rejection (`body_has_bare_continue`/`stmt_has_bare_continue`) is gone along with it -- handled generally now, not specific to this one function. The second maybe-blocker the earlier entry had flagged, `mo->target` passed opaquely into `P_AimLineAttack`, turned out to need nothing at all: opaque passing of an `Option<Handle<Thinker>>`-typed field has been proven since `A_Punch`. The third, `P_SpawnMobj` using `linetarget`-derived coordinates, surfaced the real remaining gap instead: `linetarget` itself already had a hardcoded bare-identifier rendering (`"world.linetarget"`, `render_expr`'s `Expr::Ident` arm, checked by name), but *dereferencing* through it (`.is_some()`/`.is_none()`, `thinkers.get(world.linetarget.unwrap())` for `linetarget->x`/`.y`/`.z`/`.height`) only ever fires once `"linetarget"` is registered in `extra_cross_ref_idents` -- and only `render_weapon_fn` (the `player_t*`/`pspdef_t*` weapon shape, `A_Punch`/`A_Saw`'s own idiom) ever did that registration; `render_fn`/`render_fn_impl` (the plain `mobj_t*`-self shape `A_BFGSpray` itself uses) never had, since no earlier function of that shape referenced the global. Naive output before this fix: `if world.linetarget == 0` and raw `world.linetarget.x` field access, both outright wrong (`World::linetarget` is `Option<Handle<Thinker>>`, not a dereferenceable pointer) -- fixed by giving `render_fn_impl` the same `body_has_linetarget_ref`-gated registration `render_weapon_fn` already had, and extending its own `thinkers`-parameter decision (`needs_target_deref || needs_linetarget`) to match. `an = mo->angle - ANG90/2 + ANG90/40*i;` -- a *plain*-assign RHS mixing `mo->angle` (`u32`) with `i` (a bare `int` loop counter), a shape the module's own docs had flagged as needing a new cast arm -- turned out to need no renderer change either: `i`'s only real constraint in the whole function is this one expression, so Rust's own deferred-`let` inference settles it as `u32` to match, on its own, the same way `prestep`'s ambiguous typing resolved for free in the entry just above. `P_SpawnMobj(linetarget->x, .y, .z + (.height>>2), MT_EXTRABFG);` and `P_DamageMobj(linetarget, mo->target, mo->target, damage);` both reuse already-proven machinery verbatim (`Arena`-mediated member reads through a registered `Option<Handle<Thinker>>` identifier, `Shr<i32> for FixedT`, an `Option`-typed argument passed straight through with no unwrap needed, exactly `A_PainShootSkull`'s and `A_FatAttack1`'s own idioms respectively); the inner `for (j=0;j<15;j++) damage += (P_Random()&7)+1;` is an ordinary brace-less-body counted loop with a compound-assign accumulate, both already-proven shapes needing nothing new. Verified compiling for real (`rustc --edition 2021 --crate-type lib`) against hand-written `Arena`/`Handle`/`Mobj`/`World`/`Thinker`/`FixedT` stand-ins and stub forward-referenced functions -- zero errors. `test_a_bfgspray_renders_exactly`. 393/393 tests passing.

**`A_BrainSpit` closed out -- the last genuinely new *kind* of state this module hadn't represented yet, a `static` function-local**: `static int easy = 0;` persists across every call, unlike an ordinary local (a fresh Rust `let` resets it every time this function's own body runs) -- the same "outlives one call" character `linetarget`/`braintargeton` already have, just scoped to one function's own textual visibility rather than file-wide, so it gets the identical treatment: a new `World` field (`FnBodyContext::static_locals`, populated by the new `collect_static_locals`, folding the owning function's own name into the field name via a small purpose-built `fn_name_to_snake_case` so an unrelated future function's own same-named `static` can't collide -- `"easy"` -> `"a_brain_spit_easy"`), with the original declaration dropped entirely by `render_decl` (a real `let` would silently re-zero it every call) and every bare reference resolved to that field, the same `render_expr` `Expr::Ident` mechanism `linetarget` already uses. `easy ^= 1; if (gameskill <= sk_easy && (!easy)) return;` surfaced a second, independent gap while wiring the first: `!easy` is a `&&`-chain operand, and this module's own docs on `render_binary_operand` had explicitly left a bare `Unary::Not` operand there unrouted through `render_bool_expr`'s int-truthiness `== 0` cast, since nothing tracked enough of a local's real C type to tell a genuinely-`int` negation from a genuinely-`bool` one inside a chain (`!cards[idx]`, a real Rust `bool` array, needs the opposite treatment) -- but `easy` *is* one of the two registries that do carry a checked type (`plain_int_locals`/`static_locals`, both definitely `int`), so `render_binary_operand` gains one more narrowly-scoped condition recognizing exactly that pair, closing the ambiguity for this identifiable subset without touching the genuinely-unknown general case. `braintargets`/`numbraintargets`/`braintargeton` (`world.rs`'s own new fields, alongside `a_brain_spit_easy`) needed three small additions: a bare-name rewrite for all three (the same `linetarget` idiom), `braintargets` added to the by-name `as usize` index-cast list (`finecosine`/`finesine`/`mobjinfo`'s own precedent), and a fourth recognized source in `is_target_tracer_typed` (`braintargets[i]`'s own element type is `Option<Handle<Thinker>>`, the same as `self.target`/`.tracer`/`actor->subsector->sector->soundtarget`) so `targ = braintargets[braintargeton];` registers as an ordinary target/tracer-style alias with zero new dereferencing machinery needed for `targ->y` later -- this is also the first time this renderer has ever *written* a bare file-scope global (`world.braintargeton = ..`), not just read one, though `A_BrainAwake`'s own separate need to *populate* `braintargets[]` (capturing a handle mid-scan) remains unsolved, see below. **The one genuinely new hazard**: `newmobj->reactiontime = (targ->y - mo->y)/newmobj->momy/newmobj->state->tics;` dereferences through `targ` -- a *different* `Option<Handle<Thinker>>`-typed value than the `Handle<Thinker>`-typed `newmobj` this write's own `get_mut` already holds mutably -- a real second, conflicting borrow of `thinkers` (confirmed a real `rustc` E0502 by compiling the naive single-statement version first). Fixed by reusing `A_Punch`'s own `player->mo->angle = .. linetarget->x ..` fix verbatim: `expr_has_other_target_deref` already detects exactly this shape, so the spawned-mobj-write arm now hoists its whole RHS into a `let __rhs` before the mutable borrow starts whenever that check fires, the identical treatment its sibling arm already had, just extended to the one write arm that didn't have it yet. `newmobj->state->tics` (a real Rust reference field, `.state: &'static State`, dereferenced a second level to `.tics`) needed no changes at all -- an ordinary Rust field chain once `state` itself resolves. Verified compiling for real (`rustc --edition 2021 --crate-type lib`) against hand-written `Arena`/`Handle`/`Mobj`/`World`/`Thinker`/`State` stand-ins and stub forward-referenced functions -- zero errors. `test_a_brainspit_renders_exactly`. 394/394 tests passing.

**`PIT_VileCheck`/`A_VileChase` closed out -- the blockmap-callback pair this section's own "not yet done" list had assumed needed real spatial-iteration machinery, which turned out not to be true**: every corpus function call, including `P_BlockThingsIterator`/`P_CheckPosition`, is already treated as an opaque forward-referenced call under this project's own established convention (nothing here builds a real blockmap or `P_RunThinkers`-style dispatch loop yet) -- so translating `A_VileChase`'s own body needed no new blockmap subsystem at all, just the same kind of per-function gap-closing every other round here has done, plus generalizing `linetarget`'s own registration to a *second* real caller for the first time. `corpsehit`/`vileobj`/`viletryx`/`viletryy` (`world.rs`'s new fields) register into `extra_cross_ref_idents` the same way `linetarget` does; the underlying `body_has_linetarget_ref`/`expr_has_linetarget_ref` walk is now `body_has_any_ident_ref`/`expr_has_any_ident_ref`, taking a name list instead of being hardcoded to one name, since duplicating the whole recursive walk a second time for `corpsehit`/`vileobj` would have been pure copy-paste. Five more real gaps, each confirmed by compiling the naive translation first, not guessed at: (1) `corpsehit->momx = corpsehit->momy = 0;`, a chained assignment `render_expr` has no arm for at all (an assignment is only ever a *statement* here) -- split into two ordinary assignments in evaluation order, each still routed through every existing write-side special case independently. (2) `abs(thing->x - viletryx) > maxdist` -- Rust has no free `abs` function, only an inherent method, and the argument is `FixedT`-valued (`expr_is_fixed_t_valued` extended to also recognize a target/tracer-chain member read, not just `self`'s own fields or a spawned local's) -- unwraps to the raw `i32` magnitude via `.0` before `.abs()`, matching `fixed_t` genuinely being `int` at the C level, then compares against a plain `int` with no further cast. (3) `corpsehit->height <<= 2; ... >>= 2;` -- `FixedT` had `Shr<i32>` but no `Shl<i32>`/`ShlAssign<i32>`/`ShrAssign<i32>` yet, and the target/tracer write arm itself was scoped to a plain `=` only, rejecting any compound op through that chain -- both closed together, with the `FixedT`-field wrap logic staying `=`-only (a shift amount is a plain `i32` operand, never itself `FixedT`). (4) `corpsehit = thing;` -- assigning `self` into a *global* `Option<Handle<Thinker>>`, not a spawned local's own `target`/`tracer` field the existing `Some(handle)` arm covered -- `is_self_handle_value_assign` and `render_expr_stmt`'s own write dispatch both generalize to a bare-`Ident` LHS into a registered global, reusing the exact same `handle` parameter machinery `fog->target = actor;` already established. (5) `check = P_CheckPosition(..); if (!check) return true;` -- `check`'s own `boolean` decl carries no usable type info by itself, so without tracking that its *only* assignment source is a call `is_bool_returning_call` already recognizes, `!check` fell through to the generic `int`-truthiness `== 0` cast (`bool == 0` doesn't even compile) -- new `FnBodyContext::bool_locals`/`collect_bool_locals` closes this the same way `plain_int_locals`/`angle_t_locals` track other locally-inferred typing gaps. **A second-order gap this all surfaced, caught only once the *whole* function was compiled**: `needs_target_deref`/`needs_target_write` were computed against `target_tracer_aliases` (the narrow, spawn-derived alias map) instead of the fuller `extra_cross_ref_idents` map `corpsehit`/`vileobj` actually register into -- every individual render was correct, but the function's own signature never gained the `thinkers` parameter those renderings call `thinkers.get_mut`/`.get` through; widened to the superset map, which by construction only ever detects a real need more completely, never less. `A_VileChase` itself then surfaced two more: `stmt_has_target_write` had no `Stmt::For` arm at all (unlike its `stmt_has_target_deref` sibling), so writes nested inside `for (bx=xl;..) { for (by=yl;..) { .. } }` were invisible to it -- the exact same class of "signature missing `thinkers`" bug, just triggered by a `for` loop instead of the plain-statement case `PIT_VileCheck` itself hit; and `xl = (viletryx - bmaporgx - MAXRADIUS*2)>>MAPBLOCKSHIFT;` needed `viletryx`'s own `FixedT` value unwrapped to raw `.0` at the point of use (not the whole-RHS `.0`-wrap `is_approx_distance_call` already does for a bare call, since `world.viletryx - bmaporgx` doesn't even compile as `FixedT` arithmetic to begin with -- `FixedT` has `Add<i32>` but no `Sub<i32>`) -- the same "one value, genuinely mixed `FixedT`/`int` usage" idiom `A_SkullAttack`'s own `dist` already established, just on a global. Verified compiling for real (`rustc --edition 2021 --crate-type lib`) against hand-written `Arena`/`Handle`/`Mobj`/`MobjInfo`/`World`/`Thinker`/`FixedT` stand-ins and stub forward-referenced functions for each -- zero errors. `test_pit_vile_check_renders_exactly`, `test_a_vile_chase_renders_exactly`. 397/397 tests passing.

**`A_WeaponReady`/`A_Chase` closed out -- the state-pointer-equality gap this section's own docs had explicitly flagged since `state_index`'s own entry, plus five more real gaps found by compiling the whole functions, none individually large**: `player->mo->state == &states[S_PLAY_ATK1]` reuses `state_index` for the comparison itself (`state_index(x) == N`) rather than `std::ptr::eq`/deriving `PartialEq` on `State` -- converting both sides to a plain index and comparing needs no new machinery, matching `state_index`'s own "new, invented Rust-only infrastructure" category; a new `render_expr` arm recognizes the shape by field name, the same way `state_index`'s own pointer-subtraction arm already does. `!player->health` (a `&&`/`||`-chain operand) extends `not_of_known_int_local`'s own bare-local check (`A_BrainSpit`'s `easy`) to a self-struct-field sibling (`not_of_known_int_field`), since `health` has no `Option<..>`/`"bool"` registration either. `player->attackdown = true;` -- `true`/`false` are this corpus's own `boolean` enum constants (`doomtype.h`), valid anywhere an `int` is via ordinary enum-to-int conversion, but `rust_ident_name` always renders them as real Rust `bool` literals (correct for a genuinely `boolean`-typed field like `crush`, its only previous use) -- `attackdown`'s real C declaration is plain `int` (confirmed by direct read), so a literal `bool` doesn't compile into it; converts to the literal's own underlying value (`1`/`0`) instead, scoped to a field registered as neither `bool` nor `Option<..>`. `!(actor->target->flags&MF_SHOOTABLE)` -- a negated *non-comparison* `Binary` chain operand, unlike a negated `Member`/`Ident` (genuinely ambiguous, might be a real `bool` this module has no per-field registry for) never ambiguous at all (this corpus has no idiom combining two values through `&`/`|`/`+`/... into a genuine `bool`) -- routed through `render_bool_expr`'s own already-correct handling instead of the generic `Expr::Unary` fallback's bitwise `!` (a real silent-wrong-behavior trap, confirmed by rendering the naive version first). `A_Chase` itself then surfaced two more: `delta = actor->angle - (actor->movedir << 29);` (`u32 - i32` doesn't compile at all, unlike `Shr`/`Shl`, which Rust already tolerates a differently-typed operand for -- confirmed by first trying the same cast unconditionally and regressing `A_VileAttack`'s own shift-only `an = actor->angle >> ANGLETOFINESHIFT;`, a real test failure) fixed via a new `find_u32_self_field_names`, deliberately scoped to only descend through `Add`/`Sub`/`Mul`/`Div`/comparisons, never `Shr`/`Shl`; and `--actor->movecount<0 || !P_Move(actor)` (a `PreIncDec` buried inside a larger condition, not the whole condition the two existing `render_condition` cases handle) hoisted via a new last-resort arm that patches the already-rendered condition text rather than rebuilding the AST. `P_CheckMissileRange`/`P_Move` (confirmed `boolean`-returning by direct read) join `is_bool_returning_call`'s list; the `nomissile:` goto target needed no new transform at all, matching `A_Look`'s own already-nested-in-an-if shape exactly. Verified compiling for real (`rustc --edition 2021 --crate-type lib`) against hand-written stand-ins for each, with concrete (non-generic) callee signatures throughout `A_Chase`'s own check -- a generic stub, tried first, wrongly *moves* `actor` instead of reborrowing it at each call site, a real `rustc` E0382 this function's own many sequential `actor` reuses surfaced that `A_VileChase`'s own single-reuse check never exercised -- zero errors. `test_a_weapon_ready_renders_exactly`, `test_a_chase_renders_exactly`. 399/399 tests passing.

**`A_BrainAwake` closed out -- the last of the four functions this whole section's `Arena<Thinker>` iteration entry originally named, and the one genuinely needing new capability, not just gap-closing**: its own scan is a *third* real AST shape (`is_thinker_list_scan_for`), a `for` loop rather than `A_PainShootSkull`'s separate-statement `curvar = thinkercap.next; while (..)` form, with a *different* body shape too (`if (!tagcheck) continue; m = (mobj_t*)thinker; <rest>`, not `render_thinker_list_scan`'s combined `if (tagcheck && cond) stmt;`) -- both confirmed by direct read, not assumed to be the same idiom twice. `is_mobj_thinker_tag_check_op` generalizes the existing tag-check matcher to this shape's own negated `!=` guard; `render_thinker_list_scan_for` renders the body through a new, purely additive `Arena::iter_with_handle()` (every existing `iter()` caller keeps compiling unchanged) yielding each live slot's own `Handle<T>` alongside the borrowed reference -- needed because `braintargets[numbraintargets] = m;` stores the scanned thinker's own *handle*, not a borrowed value, the one real reason this couldn't just reuse `render_thinker_list_scan`'s existing `Arena::iter()`-based rendering. The redundant `thinker = thinkercap.next;` immediately before the real `for` loop (dead in the original C too) is silently dropped, extending `thinker_scan_var_names` to drop two declarations (`thinker` and `m`) instead of one. **The one genuinely new mechanism**: `m` means two different things depending on usage -- `m->type` wants the plain `&Mobj` (`thinker_scan_alias`, unchanged), but `braintargets[numbraintargets] = m;` wants the handle `iter_with_handle` just captured -- a distinction `Expr::Ident`'s own rendering can't make (both reach it through the identical bare-identifier shape), so a new `FnBodyContext::thinker_scan_handle_alias` is checked instead at the one *write-statement* shape that actually needs it, sidestepping the ambiguity rather than resolving it at the identifier level. A **second-order gap**, the same class as `A_VileChase`'s own: nothing in `render_fn_impl`'s signature-decision logic predates this new scan shape, so the generated signature silently omitted `thinkers` even though the body calls straight through it -- confirmed a real "undefined variable" `rustc` rejection by rendering the naive version first, fixed by a new `needs_thinker_scan_for` check folded into the existing read-only-`thinkers` disjunct. Verified compiling for real (`rustc --edition 2021 --crate-type lib`) against a hand-written `Arena`/`Handle`/`Mobj`/`World`/`Thinker` stand-in (including a real `iter_with_handle` matching `runtime/arena.rs`'s own verbatim) -- zero errors. `test_a_brain_awake_renders_exactly`. 401/401 tests passing.

**`P_ExplodeMissile` translated (`p_mobj.c`), opening a fresh batch of non-`A_*` tick/trigger/helper functions now that essentially every real `state_t.action` entry (73 of 73 minus the two permanently-blocked `A_KeenDie`/`A_BossDeath`) is translated** -- otherwise a clean, already-provisioned mix (`mobjinfo[mo->type].deathstate` via the already-proven variable-indexed `mobjinfo[]` rendering, `mo->tics -=`/clamp, an `MF_MISSILE` flag clear, `mo->info->deathsound` truthiness guarding `S_StartSound`), it needed two new, narrowly-scoped mechanisms. (1) `mo->momx = mo->momy = mo->momz = 0;` is a **three**-target chained assignment; the existing chain-flattening (`PIT_VileCheck`'s two-target `corpsehit->momx = corpsehit->momy = 0;`) only ever split off one level and handed its "first" half -- itself still a chain -- straight to `render_expr_stmt`, which has no `Expr::Assign` handling at all, so it would have failed outright on a third target. Generalized to flatten a chain of *any* length: walk the right-recursive `Assign` spine collecting every target plus the final value, then emit the ordinary single assignments in real evaluation order (innermost first). (2) Once flattened, `mo->momz = 0;` surfaced a second, genuine gap only visible by actually compiling the naive output with `rustc`: a bare integer literal written straight into one of the function's own **self-struct** `FixedT` fields (as opposed to a *target/tracer-typed* field, which already had its own wrap arm) rendered as a bare `0`, which doesn't build against the real `FixedT` newtype. Tried the same broad `!expr_is_fixed_t_valued(..)` guard the target/tracer arms use first; that was a real regression, not just extra caution -- it wrongly wrapped `T_MoveCeiling`'s already-shipped `ceiling->speed = CEILSPEED;` (`CEILSPEED` a `#define`d alias for `FRACUNIT`, `p_spec.h`, confirmed by direct read; this parser never macro-expands `#define`s, so `expr_is_fixed_t_valued`, which only recognizes the literal identifier `FRACUNIT`, can't tell it apart from a genuinely-plain-`int` constant by name alone) into `FixedT(CEILSPEED)`, an invalid `FixedT`-from-`FixedT` construction. Landed instead scoped to a genuine `Expr::IntLiteral` RHS only -- sufficient for this function's own need and provably safe against every other already-shipped translation (caught by the full suite, not inspection). **Compile-verified for real**: assembled alongside a hand-written `FixedT`/`Mobj`/`MobjInfo`/`MOBJINFO`/`World` stand-in and compiled with `rustc --edition 2021 --crate-type lib` -- zero errors (only the usual `non_snake_case` naming lints on Doom's own `A_`/`P_`-prefixed names). `test_p_explode_missile_renders_exactly`. 402/402 tests passing.

**`P_CheckMissileSpawn` translated (`p_mobj.c`)** -- a clean, already-fully-provisioned function needing no new mechanism at all: `tics -=`/clamp (`P_ExplodeMissile`'s own idiom, just closed above), `th->x += (th->momx>>1);` reusing `FixedT`'s existing `Shr<i32>`/`AddAssign` (first proven for `A_SkullAttack`'s own `dist` gap), and a `P_TryMove` call through the already-registered bool-returning-helper machinery (`is_bool_returning_call`) for the closing `if (!P_TryMove(..)) P_ExplodeMissile(th);` -- a forward reference to the just-translated sibling above, the same "call an already-translated function by name" convention every other cross-function call in this corpus already relies on. Compile-verified for real with a hand-written `FixedT`/`Mobj`/`World` stand-in (`rustc --edition 2021 --crate-type lib`) -- zero errors. `test_p_check_missile_spawn_renders_exactly`. 403/403 tests passing.

**`P_TryWalk` translated (`p_enemy.c`)** -- another clean, already-fully-provisioned function: a `boolean`-returning single-self-struct helper (`render_bool_fn`, the same shape `P_CheckMeleeRange` already proved), forward-referencing the not-yet-translated `P_Move` through the same by-name-call convention every other cross-function reference in this corpus already relies on (`P_Move` is already registered in `is_bool_returning_call`, so its negated-condition use, `if (!P_Move(actor))`, renders correctly with no body of its own needed yet). Compile-verified for real with a hand-written `Mobj`/`World` stand-in -- zero errors. `test_p_try_walk_renders_exactly`. 404/404 tests passing.

**`P_CheckMissileRange` translated (`p_enemy.c`), plus a real latent gap in `A_Tracer` closed along the way** -- `FnBodyContext::fixed_t_locals`/`collect_fixed_t_locals` (mirroring `plain_int_locals`/`angle_t_locals`) register a local genuinely declared `fixed_t` in the real C source (`dist`, unlike `A_SkullAttack`'s same-named but plain-`int`-declared local). `FixedT` deliberately has no `PartialOrd<i32>`/assignment-from-`i32` (the newtype's whole point is catching an accidental raw-`int` mix at compile time), so a bare comparison (`dist > 14*64`, `P_Random() < dist`, either operand order) or assignment (`dist = 200;`) against one of these locals now wraps the plain-`int` side in `FixedT(..)`, guarded by the existing `expr_is_fixed_t_valued` so an already-`FixedT`-valued operand is never double-wrapped. **Compile-verifying this surfaced a second, independent real gap**: `runtime/fixed.rs` had `Div<i32> for FixedT` but no bare `Div for FixedT` (same-type) -- `A_Tracer`'s own `slope = (dest->z+40*FRACUNIT - actor->z) / dist;` genuinely divides one `fixed_t` by another via a bare `/`, never through `FixedDiv`, a real corpus idiom this project's own type system had no operator for yet. This was previously masked: `A_Tracer`'s own verification harness had deliberately stubbed `P_AproxDistance` to return `i32` instead of its real `fixed_t` signature specifically to dodge `dist`'s ambiguous typing (documented at the time as a known shortcut, not a real fix) -- now that the general mechanism exists, `A_Tracer`'s own `dist`/`slope` render correctly as genuine `FixedT` throughout, and its stub is corrected to match the real `p_local.h` signature (its own expected test string updated to match, `FixedT(1)` in place of a bare `1`). Compile-verified for real with a hand-written `FixedT`/`Mobj`/`MobjInfo`/`Thinker`/`Arena`/`Handle`/`World` stand-in (`rustc --edition 2021 --crate-type lib`) -- zero errors (this round's own verification independently re-caught the missing bare `Add for FixedT` in an early draft of the stand-in itself, a mistake in the test harness rather than the renderer, fixed before concluding). `test_p_check_missile_range_renders_exactly`. 406/406 tests passing.

**`P_AproxDistance` translated (`p_maputl.c`), the first function needing `render_pure_fn`'s own leaner shape** -- a small self-contained helper touching no global state at all (no `self`-struct receiver, no `World`, no `Arena<Thinker>`, just its own parameters in and a plain value out). `render_trigger_fn` always appends a fixed `, world: &mut World, thinkers: &mut Arena<Thinker>)` to its signature, which would be actively wrong here (no real corpus call site anywhere passes those to `P_AproxDistance`, confirmed by every already-translated caller's own rendered text, e.g. `A_Tracer`'s `P_AproxDistance(dx, dy)`) -- `render_pure_fn` renders a bare signature instead, reusing `param_types` as `extra_cross_ref_idents` the same way `render_trigger_fn` already does. Two new mechanisms: `expr_is_fixed_t_valued` gains a `"FixedT"`-registered-parameter arm (`dx`/`dy` themselves, distinct from a self-struct field or a `fixed_t`-declared local) so `abs(dx)` extracts its raw magnitude correctly (`PIT_VileCheck`'s own precedent); `dx = abs(dx);` (self-reassignment through `abs()`) re-wraps that extraction back into `FixedT(..)` since `dx` stays genuinely `fixed_t`-typed afterward (`dx>>1`, `dx+dy`), unlike `PIT_VileCheck`'s own use, which stays bare `i32` for a plain-`int` comparison. **Compiling the naive output surfaced a second, independent gap**: `dx`/`dy` are Rust function *parameters*, and reassigning one needs `mut` in the signature -- the first corpus function to reassign one of its own parameters (every earlier `render_trigger_fn` caller only ever reads its params). `render_params` now checks each parameter against the new `body_reassigns_ident` (a precise write-only scan, not the looser `body_has_any_ident_ref`, which would also fire on a plain read and produce a spurious `clippy::unused_mut` on every never-reassigned parameter). Compile-verified for real with a hand-written `FixedT` stand-in (`rustc --edition 2021`, both `--crate-type lib` and a `--crate-type bin` smoke run actually executing `P_AproxDistance(3, -4) == 6`) -- zero errors. `test_p_aprox_distance_renders_exactly`. 407/407 tests passing.

**`P_AddActivePlat`/`P_AddActiveCeiling` translated (`p_plats.c`/`p_ceilng.c`)** -- the first functions reading/writing `World::activeplats`/`activeceilings`, global `[Option<Handle<Thinker>>; MAXPLATS/MAXCEILINGS]` arrays (`NULL` marking an empty slot), the same hand-matched-by-name category `braintargets` already established. `Handle<Thinker>`'s existing hand-written `PartialEq`/`Eq` (`runtime/arena.rs`, not derived) makes `activeplats[i] == NULL` -> `world.activeplats[i as usize] == None` -- `Option<Handle<Thinker>>`'s own derived equality against the `None` variant -- compile with no new trait work at all; the write side (`activeplats[i] = plat;`) needed a new `Some(..)` wrap (`lhs_is_activeplats_index`), the array-index-write mirror of the existing self-struct target/tracer field write. Also the first function needing a bare C string-literal call argument (`I_Error ("P_AddActivePlat: no more plats!");`) -- `render_expr` gains an `Expr::StringLiteral` arm, passing the AST's own already-quoted text straight through (not a general C-to-Rust string-escape translator, just this one real shape). `P_AddActiveCeiling` is `P_AddActivePlat`'s exact twin, one `Thinker` variant over, and slightly simpler: no `I_Error` fallback at all, confirmed by direct read rather than assumed from its sibling's shape. Compile-verified for real with hand-written `Handle`/`Arena`/`Thinker`/`World` stand-ins (`rustc --edition 2021 --crate-type lib`) -- zero errors, both functions. `test_p_add_active_plat_renders_exactly`/`test_p_add_active_ceiling_renders_exactly`. 409/409 tests passing.

**`P_RemoveActivePlat`/`P_RemoveActiveCeiling`/`P_ActivateInStasis`/`P_ActivateInStasisCeiling`/`EV_StopPlat`/`EV_CeilingCrushStop` investigated, deliberately deferred, not forced** -- the natural next step after `P_AddActivePlat`/`P_AddActiveCeiling`, but each needs real new mechanism work, confirmed by reading the full body rather than assumed clean from the survey's own earlier characterization. `P_RemoveActivePlat`'s own `if (plat == activeplats[i])` compares a bare `Handle<Thinker>` parameter against an `Option<Handle<Thinker>>` array element -- doesn't type-check directly, needs a new comparison-side `Some(..)` wrap (the `Handle`-vs-`Option<Handle>` equality mirror of the `FixedT`-literal-wrap idiom this round already added twice for other types). `(activeplats[i])->sector->specialdata = NULL;` dereferences *through* an array element holding a `Thinker::Plat`, not `Thinker::Mobj` -- every existing target/tracer/self-handle dereference arm hardcodes `Some(Thinker::Mobj(m)) => m.field` specifically, so reading a `Plat`'s own field this way needs the whole dereference mechanism generalized to a caller-specified `Thinker` variant, not a narrow patch. `P_RemoveThinker(&(activeplats[i])->thinker);` is self-removal of an *other*, array-looked-up handle -- `is_self_removal_call` only recognizes `&self_param->thinker`, a new shape. `P_ActivateInStasis`/`EV_StopPlat`'s own reassignment of `thinker.function.acp1`/`.acv` restates the same open design question the survey already flagged: this project's enum-dispatch `Thinker` model has no function-pointer field to reassign at all, so there's a real "what does this even mean here" question, not a quick mechanical translation. None attempted this round -- each is its own separately-scoped gap, matching this project's own "confirm the real blocker by reading the body, don't force a translation past it" discipline.

**`T_MovePlane` (`p_floor.c`) translated -- the sector-mutating helper every already-translated mover tick function has been calling as a forward-referenced stub**: `T_MoveFloor`/`T_MoveCeiling`/`T_PlatRaise`/`T_VerticalDoor` all already render a real call to `T_MovePlane(sector, speed, dest, crush, floorOrCeiling, direction)`, but the function itself was never translated until now. Confirmed by reading the whole body first: a nested `switch(floorOrCeiling)`/`switch(direction)`, neither with a `default:` (needing a synthetic trailing `_ => {}` on both), each innermost `if`/`else` reading/writing `sector->floorheight`/`.ceilingheight` directly and calling the still-not-translated `P_ChangeSector` (a forward-reference stub, the same convention as `S_StartSound`). **Needed no new expression/statement-shape mechanism at all** -- every piece was already generalized enough: `sector`'s own plain `SectorId` parameter type already resolves `sector->floorheight` to `world[sector].floorheight` through the existing generic `is_cross_ref`/`extra_cross_ref_idents` lookup (the same path a trigger loop's own `sec`/`sector` local already uses, not something hardcoded to that one shape), `boolean flag;`/`fixed_t lastpos;` (both bare `TypedefName` locals, no pointer, no initializer) already fall through `render_decl`'s existing deferred-`let`-inference case, nested nested `switch`es were already proven by `T_VerticalDoor`/`T_PlatRaise`, and `FixedT`'s own `Add`/`Sub`/`AddAssign`/`SubAssign`/`PartialOrd` were all already in place. **The only genuinely new piece is `render_world_fn`**, a thin `render_pure_fn` sibling: `pub fn NAME(..params.., world: &mut World) -> RET`, no `self`, no `Arena<Thinker>` (this function never touches a `Thinker` at all -- no self-removal, no target/tracer, no spawn) -- neither `render_pure_fn` (no `World` access) nor `render_trigger_fn` (always appends `Arena<Thinker>` too, and assumes a tagged-sector-loop body shape this function doesn't have) fit as-is. Verified compiling for real (`rustc --edition 2021 --crate-type lib`) against hand-written `FixedT`/`SectorId`/`Sector`/`World` stand-ins and a stub `P_ChangeSector(SectorId, bool) -> bool` matching this real call site's own argument shape -- zero errors, only expected style lints. `test_t_move_plane_renders_exactly`. 410/410 tests passing.

**`P_ThingHeightClip` (`p_map.c`) translated -- a plain `boolean`-returning self-struct function, `render_bool_fn`'s own existing shape (`P_CheckMeleeRange`/`P_CheckMissileRange`), confirmed by reading the whole body first rather than assumed clean from the shape alone**: `thing->z`/`.floorz`/`.ceilingz`/`.height` are all already-registered `FixedT` `Mobj` fields, `P_CheckPosition(thing, thing->x, thing->y)` is a plain forward-referenced call (`S_StartSound`'s own convention), and its two C-style "out parameters," `tmfloorz`/`tmceilingz`, are bare global reads (`leveltime`'s own pass-through convention, not something this function itself writes). **One real gap surfaced and fixed**: `boolean onfloor;` is assigned `onfloor = (thing->z == thing->floorz);` -- a direct comparison, not a call -- then tested bare (`if (onfloor)`). `collect_bool_locals` only recognized a local assigned from a real `bool`-returning *call* (`PIT_VileCheck`'s own `check = P_CheckPosition(..);` idiom), not one assigned straight from a comparison/logical expression; generalized (`is_bool_valued_rhs`, reusing `render_bool_expr`'s own `is_comparison_or_logical` op set) to cover both. `render_bool_expr`'s own bare-`Ident` truthiness dispatch also only had the *negated* `bool_locals` case (`!check`, mirroring `is_option_valued`'s own negated arm) -- the un-negated sibling (`if (onfloor)`) had never been needed until this function, so it was missing entirely; added alongside `is_option_valued`'s own un-negated arm. Verified compiling for real (`rustc --edition 2021 --crate-type lib`) against hand-written `Mobj`/`World` stand-ins, bare `FixedT` statics, and a stub `P_CheckPosition` matching this real call site's own argument shape -- zero errors. `test_p_thing_height_clip_renders_exactly`. 411/411 tests passing.

**`PIT_StompThing` (`p_map.c`) translated -- a `boolean`-returning `P_BlockThingsIterator` callback function, confirmed passable as a bare forward-referenced identifier with zero special handling (`A_VileChase`'s own `P_BlockThingsIterator(bx, by, PIT_VileCheck)`, already proven a round ago), needing two genuinely new pieces once its own body was read in full**: (1) `tmthing` is a bare, file-scope `mobj_t* tmthing;` -- grepped every real reference to it across the whole of `p_map.c` (not assumed): never null-checked anywhere, unlike `corpsehit`/`vileobj`, so `render_fn_impl` now registers it *bare* `Handle<Thinker>` (a new `needs_tmthing` check, the same by-name registration those two already get, just the non-`Option` variant) -- `tmthing->radius`/`.player` then dereference through the fully generic, already-existing `Handle<Thinker>` `Expr::Member` arm with zero new rendering code. (2) `thing == tmthing` ("don't clip against self") compares the function's own receiver's *identity* (`thing`, a `&mut Mobj`) against `tmthing` (a `Handle<Thinker>` value) -- doesn't type-check as a bare passthrough. Added `body_has_self_identity_comparison` (the comparison-side sibling of `body_has_self_handle_value`'s own assignment-side check -- recurses into `if`/`while` *conditions* too, since this pattern lives inside one, unlike the assignment-only check) driving the same `handle: Handle<Thinker>` signature-extension parameter, plus a new `render_expr` `Binary` arm (`Eq`/`Ne`) substituting `handle` for the self side. `abs(thing->x - tmx)` (a `FixedT` operand) and `!tmthing->player` (`Option<PlayerId>`, `.is_none()`) both rendered correctly against wholly pre-existing machinery, no new code -- confirmed by running the real corpus AST through the renderer first and checking its actual output against a hand-trace, not assumed. Verified compiling for real (`rustc --edition 2021 --crate-type lib`) against hand-written `Mobj`/`World`/`Handle`/`Arena` stand-ins (`Handle`'s real hand-written, non-derived `PartialEq`/`Eq` from `runtime/arena.rs`), a bare `Handle<Thinker>`-typed `tmthing`/`FixedT` `tmx`/`tmy` statics, and a stub `P_DamageMobj` matching this real call site's own argument shape -- zero errors. `test_pit_stomp_thing_renders_exactly`. 412/412 tests passing.

**`PIT_RadiusAttack` (`p_map.c`) translated -- another `P_BlockThingsIterator` callback, generalizing `PIT_StompThing`'s bare-`Handle<Thinker>`-global registration to its siblings `bombspot`/`bombsource`** (grepped never null-checked either, zero new mechanism beyond widening the by-name list), **plus three genuinely new pieces surfaced by this function specifically**: (1) `dx>dy ? dx : dy` -- C's ternary operator, never needed until now; `Expr::Conditional` had no `render_expr` arm at all, added one rendering straight into Rust's own `if`/`else` expression. (2) `(dist - thing->radius) >> FRACBITS` shifts a `FixedT` by the real `FRACBITS` constant, whose declared Rust type is `u32`, not the `i32` shift-count `Shr<i32> for FixedT` already handles (`dest->height>>1`) -- added `Shr<u32> for FixedT` (`runtime/fixed.rs`), the same "both operand types get impl'd" precedent `Add<i32>`/`Mul<i32>` already established (Rust's own `-` binding tighter than `>>`, matching C's precedence exactly, means the rendered text needs no parens here either). (3) `bombdamage - dist` -- `dist` is genuinely `fixed_t`-declared (correctly `FixedT`-typed throughout its own earlier `0`-clamp comparison, via wholly pre-existing machinery), but has just been reassigned through the `>> FRACBITS` shift into a plain map-unit scalar -- the same "one C variable, two roles" idiom already documented for `A_Tracer`'s own `dist`/`A_BrainExplode`'s `x`/`y`/`z`. Confirmed a real `rustc` rejection first (`cannot subtract FixedT from i32`), not assumed: `Add<FixedT> for i32`'s own `Output = FixedT` choice would be wrong reused here, since this value's real destination (`P_DamageMobj`'s `damage: int` parameter) is plain `i32` -- fixed with a new, deliberately narrow `render_expr` arm (`Sub` only, RHS a bare `fixed_t_locals` ident, LHS not already known `FixedT`-valued) extracting the raw representation (`.0`) rather than widening `Sub`'s trait impls with an output-type choice that would collide with `Add`'s existing one for the identical operand pairing. Verified compiling for real (`rustc --edition 2021 --crate-type lib`) against hand-written `Mobj`/`World`/`Handle`/`Arena` stand-ins, a plain `i32` `bombdamage` static, and stub `P_CheckSight`/`P_DamageMobj` matching this real call site's own argument shapes -- zero errors. `test_pit_radius_attack_renders_exactly`, `test_fixed_shr_by_fracbits_like_pit_radius_attack`. 414/414 tests passing.

**`P_FindSectorFromLineTag`/`getNextSector` (`p_spec.c`) translated -- opens a fresh batch of non-`A_*`/non-mover functions, the first two of which need no `line_t`-by-address design work at all, just ordinary gap-closing**: `P_FindSectorFromLineTag` (already called as a forward-referenced stub by every already-translated tagged-sector trigger -- `EV_DoCeiling`/`EV_DoDoor`/`EV_DoPlat`/`EV_StartLightStrobing`'s own `while ((secnum = P_FindSectorFromLineTag(line,secnum)) >= 0)`) fits `render_world_fn`'s existing shape directly (a plain `line: &Line`/`start: i32` parameter pair, `-> i32`, real `World` access, no `self`-struct, no `Arena<Thinker>`), needing only two new by-name global registrations: `numsectors` (`p_setup.c`'s own file-scope `int numsectors;`) renders as `world.sectors.len() as i32` rather than a fresh `World` field, since it's a read-only quantity always exactly equal to the sector array's own length (both set together at level load in the real engine) -- a second field would just be a redundant place for the same number to drift out of sync, the same "don't duplicate the source of truth" reasoning `mobj_t.info`/`sector_t.linecount` already established at the struct-field level, just applied here at the expression-rendering level since no struct field actually holds it. Bare (non-`&`) `sectors[i]` (`sectors[i].tag`) generalizes the existing bare `sides[i]` special case to its sibling global array (`sector_t*` already maps to `SectorId`, so this needs no real indexing either, just wrapping the index with `is_crossref: true`); `line->tag` needs nothing new (`tag` is a plain `i16` field on both `Sector`/`Line`, confirmed by direct read of `r_defs.h`, not assumed). `getNextSector` is a small pure helper (no `self`, no `World`/`Arena` access at all -- `render_pure_fn` fits directly) that surfaces the first real need for `Stmt::Return` to wrap a value in `Some(..)`: its own return type is `Option<SectorId>`, and while `return line->backsector;` needs no wrap (`backsector` is already the corpus-verified nullable twin), `return line->frontsector;` returns the bare, non-nullable sibling -- a new `FnBodyContext::return_type` field (threaded through `render_pure_fn`/`render_world_fn`, `None` everywhere else, mechanically added to all thirty `FnBodyContext` construction sites the same way every prior field addition has been) lets `Stmt::Return` recognize this shape, hand-matched by field name (`"frontsector"` specifically -- "no general struct-field-type registry yet," the same style `sides[i].sector`/`specialdata` already established) rather than a general is-this-bare-`Member`-cross-reference-typed check unproven beyond this one real case. `return NULL;` needed nothing new (the existing generic `Expr::Ident("NULL") -> None` arm already covers it); `!(line->flags & ML_TWOSIDED)` reused the already-proven negated-non-comparison-`Binary` truthiness arm (`A_Chase`'s own `!(actor->target->flags&MF_SHOOTABLE)`) unchanged -- `ML_TWOSIDED` itself is a plain `#define`d macro (`doomdata.h`, confirmed by direct read, not an enum constant), rendered as opaque pass-through text, the same `MISSILERANGE`/`SKULLSPEED` precedent; `line->frontsector == sec` needed nothing new either, since `SectorId` already derives `PartialEq`/`Eq`. Verified compiling and running for real (`rustc --edition 2021 --crate-type bin`, not just `--crate-type lib`) against hand-written `Line`/`Sector`/`World` stand-ins, exercising both functions against real inputs, not just syntax -- zero errors. `test_p_find_sector_from_line_tag_renders_exactly`, `test_get_next_sector_renders_exactly`. 416/416 tests passing.

**`PIT_ChangeSector` (`p_map.c`) translated -- a `boolean`-returning `P_BlockThingsIterator` callback, `PIT_ChangeSector`/`PIT_StompThing`'s own family, needing only two new by-name global registrations and otherwise reusing wholly pre-existing machinery**: `World` gains `crushchange`/`nofit` (`p_map.c`'s own file-scope `boolean crushchange; boolean nofit;`, the same "genuine mutable game state a callback and its caller communicate through" category `corpsehit`/`vileobj` already established, just plain `bool` this time); `is_bool_returning_call` gains `P_ThingHeightClip` (already a real translated `boolean`-returning function, called here the same forward-reference-by-name way every other cross-function call in this corpus already is -- `render_bool_expr`'s existing bare-call-as-condition arm then applies with zero further changes). `thing->flags &= ~MF_SOLID;` needed nothing new (`UnaryOp::BitNot` already renders as Rust's own bitwise `!`); `thing->height = 0;`/`.radius = 0;` reuse the already-shipped `Expr::IntLiteral`-into-self-`FixedT`-field wrap (`P_ExplodeMissile`'s own `momz = 0;` idiom); `P_DamageMobj(thing,NULL,NULL,10)` needed nothing new either -- the existing generic `Expr::Ident("NULL") -> None` arm already covers a bare `NULL` argument wherever it appears, not just the `specialdata`-assignment shape it was first built for. `mo = P_SpawnMobj(..)` / `mo->momx = (P_Random()-P_Random())<<12;` reuses `A_BrainExplode`'s own precedent exactly: `momx`/`momy` registered `"FixedT"` in `self_field_types` (`Mobj`'s own field-type map, shared between a `self` write and a spawned-local write of the same struct shape), so the plain-`i32`-shift-expression RHS gets wrapped in `FixedT(..)` by the same `is_fixed_t_field && !expr_is_fixed_t_valued(rhs, ..)` check, needing no renderer changes. **One real finding, caught by the test's own mismatch, not a bug**: `P_RemoveMobj(thing)` is confirmed genuine self-removal -- `thing` *is* this callback's own receiver, so `is_self_removal_call`'s existing `P_RemoveMobj(mo)` arm (`A_SpawnFly`'s own precedent) applies exactly, gaining the function its own `handle: Handle<Thinker>` parameter and rendering `thinkers.remove(handle)`; the first draft's own hand-traced expected string guessed this wrong (a plain opaque call), and the resulting test failure is what caught it before it could ship silently wrong. Verified compiling and running for real (`rustc --edition 2021 --crate-type bin`) against hand-written `Mobj`/`World`/`Handle`/`Arena` stand-ins (a hand-written, non-derived `Copy`/`Clone`/`PartialEq` impl for the generic `Handle<T>` stand-in, not a naive `#[derive(Copy)]` that would wrongly require `T: Copy`) and stub `P_ThingHeightClip`/`P_SetMobjState`/`P_DamageMobj`/`P_SpawnMobj`/`P_Random` -- zero errors. `test_pit_change_sector_renders_exactly`. 417/417 tests passing.

**`P_RemoveMobj` (`p_mobj.c`) translated -- the canonical "remove this thing" helper every already-translated caller (`PIT_ChangeSector` above, and `A_SpawnFly`'s own precedent before it) already calls by name, needing one new global-state batch and one new self-removal call shape**: `World` gains `itemrespawnque`/`itemrespawntime`/`iquehead`/`iquetail` (its own file-scope respawn-queue state -- `mapthing_t itemrespawnque[ITEMQUESIZE]; int itemrespawntime[ITEMQUESIZE]; int iquehead; int iquetail;`, `ITEMQUESIZE` a `#define`d `128` confirmed by direct read of `p_local.h`, written out literally in the hand-rendered `World` struct the same way every other array length here already is), the same by-name global category `crushchange`/`nofit` just established. `P_RemoveThinker((thinker_t*)mobj)` is a second, genuinely different self-removal shape from `T_VerticalDoor`'s own `P_RemoveThinker(&door->thinker)`: a direct cast of the receiver itself to `thinker_t*`, not an address-of a `.thinker` field -- `mobj_t`'s own embedded `thinker_t thinker;` header is dropped entirely under this project's memory model (the `Thinker` enum wrapper replaces its role), so there's no `.thinker` to take the address of here at all. `is_self_removal_call` gains a second arm recognizing `P_RemoveThinker(Cast(self_param))` alongside its existing address-of-field one. Everything else in the function reuses wholly pre-existing machinery: the four-way `&&` chain (`(mobj->flags & MF_SPECIAL) && !(mobj->flags & MF_DROPPED) && (mobj->type != MT_INV) && (mobj->type != MT_INS)`) needed no changes at all, confirming the bare-vs-negated-non-comparison-`Binary`-truthiness split (unconditional parens for the bare case, none for the negated case, both already proven) composes correctly inside a chain with real comparisons alongside it; `world.itemrespawnque[world.iquehead as usize] = mobj.spawnpoint;` (a `MapThing`-*valued* array write, not a cross-reference) needed only the by-name `as usize` cast-list addition, no new assignment machinery; `P_UnsetThingPosition`/`S_StopSound` are plain forward-referenced calls. Verified compiling and running for real (`rustc --edition 2021 --crate-type bin`) against hand-written `Mobj`/`MapThing`/`World`/`Handle`/`Arena` stand-ins and stub `P_UnsetThingPosition`/`S_StopSound` -- zero errors. `test_p_remove_mobj_renders_exactly`. 418/418 tests passing.

**`World.lines`/`.vertices` added, and `P_LineOpening` (`p_maputl.c`) translated -- opens the geometry-adjacent (non-`A_*`) function family this round's own task brief flagged as the next low-risk step**: `World` gains `lines: Vec<Line>`/`vertices: Vec<Vertex>` (with matching `Index`/`IndexMut<LineId>`/`<VertexId>` impls), mirroring `sectors`/`sides`/`subsectors` exactly -- mechanical, proactive infrastructure (not itself needed by `P_LineOpening`, whose own `linedef` parameter is a real `&Line` reference, `getNextSector`'s own precedent, not a `LineId`), added since several already-surveyed functions (`PIT_CheckLine`, `EV_TurnTagLightsOff`/`EV_LightTurnOn`, `P_PointOnLineSide`) independently need a real `Line`/`Vertex` lookup. `P_LineOpening` itself needed two new, narrowly-scoped mechanisms: (1) `front`/`back` (`sector_t* front; sector_t* back;`, assigned from `linedef->frontsector`/`->backsector`) -- a new `collect_line_sector_aliases` collector (hand-matched by field name alone, the same style `line->frontsector`'s own dedicated `Expr::Member` arm already established) registers `front` as bare `"SectorId"` and `back` as `"Option<SectorId>"`, merged into `render_world_fn`'s own `extra_cross_ref_idents` the same way `render_fn_impl` merges `target_tracer_aliases` -- `front->ceilingheight` then resolves through wholly pre-existing machinery (the generic `Expr::Member` fallback already wraps any cross-ref-typed base in `world[..]`), while `back->ceilingheight` (genuinely nullable -- corpus-verified, `struct_fields.rs`'s own asymmetric `frontsector`/`backsector` mapping) needed the existing `Option<PlayerId>` unwrap arm generalized to also accept `Option<SectorId>`, identical rendering either way since `World` indexes both. (2) `openrange = 0;` -- `World` gains four more hand-matched-by-name `FixedT` globals (`opentop`/`openbottom`/`openrange`/`lowfloor`, `p_maputl.c`'s own file-scope state, the same category `viletryx`/`viletryy` already established), and a bare integer literal written into one of them needed `is_world_fixed_t_global` to extend the existing self-struct-field `FixedT`-literal wrap (`P_ExplodeMissile`'s own `momz = 0;` precedent) to a *global* write, the one shape that precedent didn't cover. `linedef->sidenum[1] == -1`, the brace-less `if`/`else` single-statement branches, and the closing `openrange = opentop - openbottom;` (plain `FixedT - FixedT`, needing no new runtime arithmetic at all) all reused wholly pre-existing machinery -- confirmed by rendering the real parsed AST and matching a hand-traced expected string on the first attempt. Verified compiling for real (`rustc --edition 2021 --crate-type lib`) against a hand-written `Line`/`Sector`/`World`/`FixedT` stand-in -- zero errors. `test_p_line_opening_renders_exactly`. 419/419 tests passing.

**`P_ZMovement` (`p_mobj.c`) translated -- `render_fn`'s existing self-struct-tick shape, needing four new, narrowly-scoped pieces, one of them caught as a real regression before shipping**: (1) `mo->player->viewheight`/`.deltaviewheight` -- the first read/write *through* a self-struct field itself `Option<PlayerId>`-typed (`Mobj.player`, unlike `EV_DoLockedDoor`'s own `player_t*` *parameter*), two new `render_expr`/`render_expr_stmt` arms needing no `Arena` borrow-scoping dance at all: `World::players` is a plain array, not an arena with a `get`/`get_mut` API, so multiple accesses to the same player within one statement's RHS pose no Rust borrow conflict the way a `Handle<Thinker>` access would. (2) `if (mo->player && ...)` -- the bare, un-negated truthiness counterpart of the already-known negated `!mo->player` arm: `render_bool_expr` had no arm at all for a bare self-struct `Option<...>`-typed *field* other than the hardcoded `"specialdata"` name or a bare `Ident` (`linetarget`) -- confirmed a real `render_bool_expr` `Err` otherwise, not assumed, closed by one more arm keyed off the already-general `is_option_valued`. (3) `dist < -(delta*3)` -- `expr_is_fixed_t_valued`'s own `Ident` arm never checked `ctx.fixed_t_locals` at all (only self-struct fields/`FRACUNIT`/registered parameters), so `delta` (a genuinely `fixed_t`-declared local) buried inside a larger arithmetic expression looked *not* already `FixedT`-valued, which would have double-wrapped an already-real `FixedT` value in `FixedT(..)` a second time -- fixed by extending that one `Ident` arm, not by touching the comparison-wrap arm itself. (4) `mo->momz < 0`/`== 0`/`> 0` -- a comparison between a genuinely `FixedT`-valued self-struct field and a bare integer literal, needing its own new, deliberately narrow arm (`is_self_fixed_t_field`) rather than a blanket widening of the existing `fixed_t_locals`-vs-literal comparison-wrap arm to `expr_is_fixed_t_valued` on both sides -- tried first, and a real regression, not just extra caution: `T_PlatRaise`'s own already-shipped `plat.sector.floorheight == plat.low` compares two genuinely `FixedT` values, but `expr_is_fixed_t_valued` has no way to recognize a cross-reference *chain* (`plat.sector.floorheight`, reached through `Sector`, a different struct than `self_param`'s own `Plat`) as already-`FixedT` -- a blanket XOR wrongly wrapped that already-correct side a second time, caught by the full test suite itself (not inspection) before landing the narrower fix (an unambiguous `Expr::IntLiteral` on the *other* side, which can never itself be `FixedT`-valued, unlike an unrecognized `Member`). Everything else in the function -- the target-dereference chain (`mo->target->x`/`.y`/`.z`, already proven), `Shr<i32>`-based `FixedT` arithmetic, the bare unconditional `{ mo->z = ..; }` block after an `if` with no `else`, and every opaque macro identifier (`FLOATSPEED`/`GRAVITY`/`VIEWHEIGHT`, assumed correctly `FixedT`-typed wherever the real generated crate eventually defines them, the same `MISSILERANGE`/`CEILSPEED` precedent) -- reused wholly pre-existing machinery; confirms (again) that Rust's `-`/`>>` precedence matches C's exactly (no parens needed around `VIEWHEIGHT - viewheight` before the `>>3`) and that this renderer's `else`-`if` chains always render as nested `else { if .. }`, never collapsed `else if` syntax (both semantically identical either way, confirmed by grepping every prior expected test string -- zero already use collapsed `else if`). Verified compiling for real (`rustc --edition 2021 --crate-type lib`) against hand-written `Mobj`/`World`/`Player`/`Handle`/`Arena`/`FixedT` stand-ins (`Handle<T>`'s own hand-written, non-derived `Copy`/`Clone` impl over a `PhantomData<fn() -> T>` marker, not a naive `#[derive(Copy)]` that would wrongly require `T: Copy` -- re-caught by this round's own verification, the same mistake already documented once before) and stub forward-referenced functions -- zero errors. `test_p_zmovement_renders_exactly`. 420/420 tests passing.

**`P_RecursiveSound`/`P_NoiseAlert` (`p_enemy.c`) translated -- `render_world_fn`'s existing shape, exercising `World.lines`/`opentop`-family globals for the first time since they were added, plus three more new pieces**: `World` gains `validcount`/`soundtarget` (`p_setup.c`'s own file-scope flood-fill generation counter and `p_enemy.c`'s own file-scope `mobj_t* soundtarget;`, the same hand-matched-by-name category `viletryx`/`linetarget` already established -- `sec->validcount == validcount` needed no new rendering machinery at all, since a `Sector` *field* and a bare *global* sharing the same name render through entirely separate code paths, `Expr::Member` vs. `Expr::Ident`, no collision risk). `collect_line_sector_aliases` (`P_LineOpening`'s own collector) gains two more recognized shapes: `check = sec->lines[i];` (indexing a `Vec<LineId>` field, `Sector.lines`) registers as `"LineId"`; `other = sides[check->sidenum[1]].sector;` (the existing dedicated `sides[i].sector` rendering shape, now also recognized as an alias *source*) registers as `"SectorId"`. `sec->lines[i]` also needed the by-name `as usize` index-cast list extended to `field == "lines"`, since `i` (a fresh loop counter Rust could otherwise freely infer as `usize`) is also compared against `sec->linecount` (a genuine `i32` field) in the same loop header. `openrange <= 0` widened `is_self_fixed_t_field` (`P_ZMovement`'s own narrow literal-comparison-wrap arm) to also recognize a bare `World`-global `FixedT` identifier, not just a self-struct field. `P_NoiseAlert` itself needed two more pieces: the existing `actor->subsector->sector` dedicated arm (`A_Look`'s own precedent) widened to also match a bare `Handle<Thinker>`-registered *parameter* as the `subsector`'s own base (not just `self_param`, which this shape has none of); `render_world_fn` gains its own conditional `thinkers: &Arena<Thinker>` parameter (mirroring `render_fn_impl`'s `needs_target_deref`), a no-op for every earlier caller. `soundtarget = target;` needed one more wrap combination, `lhs_is_option_handle_global && rhs_is_handle_local`: assigning a bare `Handle<Thinker>`-registered *parameter* (not `self_param` itself, the only source the existing `rhs_is_self` arm covered) into the hand-matched-by-name `Option<Handle<Thinker>>` global. `validcount++;` needed nothing new -- the existing generic standalone `PostIncDec` arm already renders through any bare identifier's own rename. Both functions' recursive/forward-referenced calls use the original C argument list, the same already-documented cross-function-wiring gap every other call in this module has. Verified compiling for real (`rustc --edition 2021 --crate-type lib`) against hand-written `Sector`/`Side`/`Line`/`World`/`Handle`/`Arena`/`Thinker`/`Mobj`/`Subsector`/`FixedT` stand-ins and stub forward-referenced calls matching their own real call-site argument shapes -- zero errors. `test_p_recursive_sound_renders_exactly`, `test_p_noise_alert_renders_exactly`. 422/422 tests passing.

**`EV_TurnTagLightsOff` (`p_lights.c`) translated -- closes the "comma-operator multi-step `for` walking a raw `sector_t*` in lockstep with its own loop index" blocker `World.lines`'s own entry flagged, a new mechanism proven correct on the first attempt**: `is_sector_walk_priming`/`is_sector_walk_for` (mirroring `is_thinker_list_scan_init`/`_for`'s own two-statement-lookahead shape, `render_compound_items`'s own dispatch loop) recognize the whole `sector = sectors; for (j=0;j<numsectors;j++,sector++) { .. }` pair and render it as one ordinary counted loop over `world.sectors.len()`, with a new `FnBodyContext::sector_walk_alias` resolving every bare reference to the pointer-local as `SectorId({counter} as u32)` directly -- the same "the pointer already *is* the index" reasoning `sec-sectors`'s own pointer-arithmetic special case already established, just resolved per-reference instead of via arithmetic; the pointer-local's own now-redundant `sector++`/declaration are both dropped, the identical treatment `currentthinker`'s own already gets. `render_sector_walk_for` builds a synthetic single-part step (`j++` alone) and hands it to the *existing*, unmodified `render_for`, rather than teaching that function a new comma-step shape only this one idiom needs. `sector->lines[i]`/`getNextSector(templine,sector)` reuse `P_RecursiveSound`'s own alias-collector extensions unchanged, but `tsec = getNextSector(..)` needed one more: a new by-callee-name rule registering a local assigned from `getNextSector`'s own real `Option<SectorId>` return, plus `is_option_valued`'s own `Ident` arm widened to recognize `"Option<SectorId>"` alongside its existing `Option<PlayerId>`/`Option<Handle<Thinker>>` pair -- `if (!tsec) continue;` needed exactly this to render `tsec.is_none()`. `min`'s own plain-`int` declaration needs no cast at all despite `Sector.lightlevel` being `i16`, not `i32`: Rust's ordinary deferred-`let` inference settles it as `i16` from its own consistent uses, the same "resolves for free" precedent `prestep`/`x`/`y`/`z` already established. Verified compiling for real (`rustc --edition 2021 --crate-type lib`) against a hand-written `Sector`/`Line`/`World` stand-in with a stub `getNextSector` -- zero errors. `test_ev_turn_tag_lights_off_renders_exactly`. 423/423 tests passing. **`EV_LightTurnOn` (`p_lights.c`, the sibling function using the identical `sector`-walk idiom) investigated, deliberately deferred**: its own `bright` parameter (`int bright` -- a genuine `i32` by this project's own established `int`->`i32` mapping) is compared against and finally assigned *from* `Sector.lightlevel` (`i16`, `struct_fields.rs`'s own mapping) in three different places, unlike `min` above (a plain *local* Rust can freely infer as `i16` throughout) -- a function *parameter*'s Rust type is fixed by its own signature, not inferable, so `if (temp->lightlevel > bright)` is a real, new `i16`-vs-`i32` mismatch this module has no cast mechanism for yet, and `sector->lightlevel = bright;` needs the reverse-direction cast too. A real, narrowly-scoped gap, not forced in speculatively.

**`P_ActivateInStasis`/`EV_StopPlat` (`p_plats.c`), `P_ActivateInStasisCeiling`/`EV_CeilingCrushStop` (`p_ceilng.c`) translated -- the first functions dereferencing *through* a `Handle<Thinker>` stored in one of the two global "active movers" arrays**: `activeplats`/`activeceilings` (`P_AddActivePlat`/`P_AddActiveCeiling`'s own `Option<Handle<Thinker>>` arrays) had, until now, only ever been compared against `None` or freshly assigned -- never read or written *through* to the underlying `Plat`/`Ceiling` struct's own fields, which all four of these functions need (scanning for a slot matching a given `tag`, then pausing/resuming it). `active_array_variant` (hand-matched by array name, the same "no general struct-field-type registry, name it explicitly" style `sides`/`sectors` already established for their own dedicated `Expr::Index` arms) resolves which `Thinker` variant and bound match-arm name (`Plat`/`p`, `Ceiling`/`c`) a dereference needs; a bare `activeplats[i]` used for truthiness (`is_option_valued`'s own new `Expr::Index` arm, plumbed through both `render_bool_expr` and `render_binary_operand`'s own `&&`-operand routing) gets `.is_some()`. Each field *read* gets a fresh `thinkers.get(..)` call at its own point of use (the same per-access-borrow discipline `mutating_handle`'s own read arm already established) -- *except* when the read is of a *different* field of the exact same handle a sibling *write* in the same statement already holds mutably (`(activeplats[i])->status = (activeplats[i])->oldstatus;`, all four functions' own "swap two fields of the same handle" idiom): reusing `same_handle_write` (keyed here by the fixed bound name, `"p"`/`"c"`, rather than a bare identifier the way `A_FatAttack1`'s own `mo` is) avoids a real overlapping-borrow rejection a naive second `thinkers.get(..)` inside the write's own `get_mut` block would hit -- confirmed by actually compiling the naive version with `rustc` first, not assumed. `(activeplats[i])->thinker.function.acp1 = (actionf_p1) T_PlatRaise;` / `.acv = (actionf_v)NULL;` (restarting/pausing an *existing* thinker) is discarded entirely by a new `is_active_array_function_pointer_assign` check in plain `render_stmt` (returning an empty `Vec`, a first for this renderer) -- `innermost_member_field` factors the "walk to the deepest `.thinker`" logic `is_function_pointer_assign` already had out into its own helper, shared by both the ctor-var base (a plain `Ident`) and this new array-index base. The discard is exact, not an approximation: the enum variant tag already fixes which function a `Plat`/`Ceiling` dispatches to, so reassigning (or nulling) the function pointer changes nothing real in the translated design, confirmed by `T_PlatRaise`'s own already-translated `in_stasis => {}` no-op arm, which already makes "the tick function is skipped entirely" and "the tick function runs but does nothing" behaviorally identical. **A genuine, narrowly-scoped `i16`-vs-`i32` mismatch, the same category `EV_LightTurnOn`'s own deferred blocker is, but resolved here rather than deferred**: `EV_StopPlat`/`P_ActivateInStasisCeiling`/`EV_CeilingCrushStop` all compare `(activeplats[j])->tag == line->tag` -- `plat_t.tag`/`ceiling_t.tag` are plain `int` (confirmed by direct read of `p_spec.h`), while `line_t.tag` is `i16` (`struct_fields.rs`'s own mapping) -- but unlike `EV_LightTurnOn`'s two-directional gap (a parameter both compared *and* assigned into an `i16` field), this is a read-only comparison, so a single new, narrowly-scoped `Expr::Binary` arm (checked before the generic comparison fallback, matched specifically on an active-array `"tag"` field against a `line`-based `"tag"` field) widens `line`'s own `i16` side with an explicit `as i32` -- always lossless, matching C's own implicit int promotion here. `EV_CeilingCrushStop`'s `int`-returning shape (`rtn = 0`/`1`, unrelated to any thinker/array machinery) needed nothing new via `render_trigger_fn`'s existing `return_type` parameter. Verified compiling for real (`rustc --edition 2021 --crate-type lib`) against hand-written `Plat`/`Ceiling`/`Line`/`World`/`Handle`/`Arena`/`Thinker` stand-ins for all four functions -- zero errors. `test_p_activate_in_stasis_renders_exactly`, `test_ev_stop_plat_renders_exactly`, `test_p_activate_in_stasis_ceiling_renders_exactly`, `test_ev_ceiling_crush_stop_renders_exactly`. 427/427 tests passing.

**`P_GiveBody`/`P_GiveArmor`/`P_GiveCard`/`P_GivePower` (`p_inter.c`) translated -- opens a fresh `player_t*`-shaped pickup/give-helper family, the first real functions using `render_fn`/`render_bool_fn` with `Player` (rather than every earlier caller's `Mobj`) as the self-struct receiver**: `player_t` still isn't struct-mapped in `struct_fields.rs`, so `self_field_types` is hand-supplied directly per function, the same convention `render_weapon_fn`'s own `player_field_types` already established. `P_GiveBody`'s `boolean`-returning *and* second-plain-scalar-parameter (`int num`) combination needed a third small `render_fn_impl` wrapper, `render_bool_fn_with_scalar_param` (neither `render_bool_fn` nor `render_fn_with_scalar_param` alone covers both at once). **The real new capability**: `player->mo->health = player->health;`/`player->mo->flags |= MF_SHADOW;` write *through* `player`'s own bare (non-`Option`) `Handle<Thinker>` field (`mo`) -- every earlier `render_fn`/`render_bool_fn` caller has been `Mobj`-shaped, whose own bare-handle fields (`target`/`tracer`) are `Option`-wrapped and already covered by `needs_target_deref`/`needs_target_write`, so `render_fn_impl` itself never computed the plain-bare-field version at all (only `render_weapon_fn` had, for this identical `player.mo` shape). Two new flags, `needs_self_handle_field_deref`/`_write`, reuse `render_weapon_fn`'s own already-generic `body_has_self_handle_field_deref`/`body_has_self_handle_write` helpers verbatim (both already parameterized over `self_param`/`self_field_types`, no player-specific assumption in either) rather than duplicating them, folded into the same `thinkers_part` signature decision `needs_target_deref`/`_write` already drive. `cards` (a genuine Rust `bool` array, `struct_fields.rs`'s own mapping) needed two small new pieces for `P_GiveCard`: a bare (non-negated) `Expr::Index` truthiness arm in `render_bool_expr` (checked before the generic `!= 0` fallback, which would otherwise wrongly apply plain-`int` truthiness to an already-`bool` value -- the un-negated sibling of `!player->cards[idx]`'s own already-correct plain-`!` fallback, never needed bare until this first real example), and a literal `1`/`0`-to-`true`/`false` write conversion (`player->cards[card] = 1;`, C's own `int`-as-`boolean` idiom, the same "one value, two different real types" gap `FixedT`'s own literal-into-field wrap already closes for `fixed_t`) -- any other literal fails loudly rather than guessing, no real corpus site needs one. `cards` also joined the by-name `as usize` index-cast list alongside `powers`/`lines` (`card`'s own declared type is a fixed function *parameter*, not a local Rust could freely infer as `usize`). **A real, latent bug in two already-shipped tests, caught by the full suite, not this new code's own test**: adding `cards` to the cast list surfaced that `EV_DoLockedDoor`/`EV_VerticalDoor`'s own existing `cards[it_bluecard]`-style expected strings were missing the identical `as usize` cast every other enum-constant array index already gets (confirmed a genuine `rustc` `E0277` by direct reproduction, not a style nit) -- their own original "verified compiling for real" claim must have used a non-canonical stand-in. Fixed by correcting both existing tests to match the more broadly-correct rendering, not by narrowing the new `cards` cast-list entry to dodge the mismatch. Verified compiling for real (`rustc --edition 2021 --crate-type lib`) against hand-written `Player`/`Mobj`/`World`/`Handle`/`Arena`/`Thinker` stand-ins for all four functions (a separate stub `P_GiveBody` matching its own original two-argument C call shape was used to verify `P_GivePower`'s own call site, the usual cross-function-wiring convention) -- zero errors. `test_p_give_body_renders_exactly`, `test_p_give_armor_renders_exactly`, `test_p_give_card_renders_exactly`, `test_p_give_power_renders_exactly`. 431/431 tests passing.

**`P_Thrust` (`p_user.c`) translated -- a genuinely *third* real parameter, one more than `render_fn_impl`'s existing `scalar_param_type` slot alone (`A_PainShootSkull`'s own single extra parameter) supports**: a new `render_fn_impl_with_two_scalar_params`/`render_fn_with_two_scalar_params` pair (every earlier `render_fn_impl` caller unaffected, now a thin wrapper passing `None` for the new third slot). Two more small, generally-scoped gaps surfaced and fixed, both confirmed safe by the full suite (neither triggers on any earlier function): (1) `move` (`P_Thrust`'s own third parameter name) is a genuine Rust keyword -- every earlier scalar parameter (`angle`/`num`/`armortype`/`power`/`card`) happened not to collide, so `render_fn_impl`'s own signature-building path had never applied `rust_field_name`'s raw-identifier escape at all (unlike `render_params`, which already does for every `render_trigger_fn`/etc. parameter) -- a bare body reference to `move` already escaped correctly on its own, through `render_expr`'s unrelated generic `Expr::Ident` fallback, so only the signature text itself needed the fix. (2) `angle >>= ANGLETOFINESHIFT;` reassigns its own scalar parameter -- the identical "a reassigned parameter needs `mut`" gap `render_params`'s own `body_reassigns_ident` check already covers elsewhere (`P_AproxDistance`'s `dx = abs(dx);`), never needed by a `render_fn_impl`-family scalar parameter before. `player->mo->momx += FixedMul(move,finecosine[angle]);` needed nothing new beyond composing three already-proven pieces: `is_self_bare_handle_field`'s write arm (`P_GiveBody`'s own `player.mo` precedent), `FixedMul`'s own already-`FixedT`-valued bare-call recognition (`A_Tracer`'s precedent), and `finecosine`/`finesine`'s own by-name `as usize` index cast. Verified compiling for real (`rustc --edition 2021 --crate-type lib`) against a hand-written `Player`/`Mobj`/`World`/`Handle`/`Arena`/`Thinker`/`FixedT` stand-in -- zero errors. `test_p_thrust_renders_exactly`. 432/432 tests passing.

**`P_SpawnPuff`/`P_SpawnBlood` (`p_mobj.c`) translated -- the first `render_trigger_fn` callers spawning their own `mobj_t*` local with no embedded constructor at all**: both are plain, self-struct-free `fixed_t`-parameter helpers (`render_trigger_fn`'s existing shape), each doing `th = P_SpawnMobj(...); th->field = ...;` directly rather than through any `CtorSpec`/`embedded_ctor` machinery. `render_trigger_fn` doesn't auto-detect a `P_SpawnMobj` local the way `render_fn_impl`'s own `collect_spawn_mobj_locals` does for a self-struct function, but its existing `local_var_types` parameter (already used for `EV_DoLockedDoor`'s own `p: Option<PlayerId>`) registers `th: Handle<Thinker>` directly, letting every already-generic `extra_cross_ref_idents`-keyed read/write arm (`A_Tracer`'s own `th->field`, a fresh `thinkers.get`/`get_mut` call at each point of use) apply completely unchanged -- no renderer code needed for this shape at all, just the right caller-supplied map. **The one real new gap, shared by both functions**: `z += ((P_Random()-P_Random())<<10);` compound-assigns a plain raw `int` expression into a `fixed_t`-declared *parameter* -- the existing literal-into-field wrap only ever fires for a bare `=`, not a compound op, and no `AddAssign<i32> for FixedT` existed yet (only `Add<i32>`, its non-assigning sibling) -- added to `runtime/fixed.rs`, the same "thin wrapping-arithmetic pass-through" pattern every other operator there already follows. `attackrange` (`p_mobj.c`'s own file-scope `extern fixed_t attackrange;`, `P_SpawnPuff`'s own "don't make punches spark on the wall" check) joins the by-name `World` global list (`viletryx`'s own category). The original C source's own `((P_Random()-P_Random())<<10)` parens turned out redundant in both languages (`-` already binds tighter than `<<` in C and Rust alike), so the precedence-aware renderer correctly drops them rather than preserving them verbatim -- confirmed by tracing through, not assumed, since a first hand-written expected string got this detail wrong before the test itself caught it. `P_SpawnBlood` reuses every piece unchanged (`th->momz = FRACUNIT*2;`, `Mul<i32> for FixedT`'s existing precedent) plus a plain-`i32` `damage` parameter driving an ordinary `if`/`else if` two-way branch. Verified compiling for real (`rustc --edition 2021 --crate-type lib`) against a hand-written `Mobj`/`World`/`Handle`/`Arena`/`Thinker`/`FixedT` stand-in for both functions -- zero errors. `test_p_spawn_puff_renders_exactly`, `test_p_spawn_blood_renders_exactly`. 435/435 tests passing.

**`P_CalcSwing` (`p_pspr.c`) translated -- an ordinary `Player`-shaped self-struct function, needing only one small new piece**: `swingx`/`swingy` (`p_pspr.c`'s own file-scope `fixed_t swingx; fixed_t swingy;`) join the by-name `World` global list (`viletryx`/`attackrange`'s own category) -- a bare *write* (`swingx = ..;`) needed no separate mechanism at all, since `render_expr_stmt`'s generic assignment path already renders its own LHS through the same `render_expr` `Expr::Ident` arm a read would use. `swing` (a genuinely `fixed_t`-declared local) and `angle` (a plain `int` local, reused across two separate `finesine[angle]` index shapes) are both handled automatically by already-existing machinery (`collect_fixed_t_locals`, `finesine`'s own blanket by-name `as usize` index cast). Both `(FINEANGLES/70*leveltime)&FINEMASK`-shaped expressions needed no parens in either language (`&`'s C/Rust precedence is lower than `*`/`/`/`+`), confirming the precedence-aware renderer drops them correctly, the same "redundant original parens, dropped correctly" pattern `P_SpawnPuff`'s own entry above just proved. Verified compiling for real (`rustc --edition 2021 --crate-type lib`) against a hand-written `Player`/`World`/`FixedT` stand-in and stub `FixedMul` -- zero errors. `test_p_calc_swing_renders_exactly`. 436/436 tests passing.

**`P_DropWeapon` (`p_pspr.c`) translated -- a one-statement `Player`-shaped self-struct function, needing no new mechanism at all**: `weaponinfo[player->readyweapon].downstate` reuses the already-established `weaponinfo[..].field` array-of-struct access (`A_WeaponReady`'s own precedent), generic over `ctx.self_param` regardless of whether the caller is `render_fn` or `render_weapon_fn`. Verified compiling for real (`rustc --edition 2021 --crate-type lib`) against a hand-written `Player`/`World` stand-in and a stub `weaponinfo`/`P_SetPsprite` -- zero errors. `test_p_drop_weapon_renders_exactly`. 437/437 tests passing.

**`P_FindLowestFloorSurrounding`/`P_FindHighestFloorSurrounding` (`p_spec.c`) translated -- `getNextSector`'s own direct sibling family, needing zero new renderer code for the shape itself**: same file, same `for (i=0;i<sec->linecount;i++) { check = sec->lines[i]; other = getNextSector(check,sec); if (!other) continue; ... }` idiom `P_RecursiveSound`'s own `check`/`other` alias collector rules (`collect_line_sector_aliases`'s `Expr::Index{base: Member{field:"lines"}}` -> `"LineId"` and `Expr::Call{callee:"getNextSector"}` -> `"Option<SectorId>"`) already anticipated by expression *shape*, not variable name, so `render_world_fn` renders both directly. **One real, narrowly-scoped bug found and fixed along the way, confirmed by direct `rustc` reproduction before touching anything**: comparing a bare `fixed_t`-declared local (`floor`) against `other->floorheight` wrongly took the bare-`fixed_t`-local-vs-plain-`int` comparison-wrap arm (`expr_is_fixed_t_valued` had no way to recognize a cross-reference-chain field -- `other` is a plain `Option<SectorId>`-registered local, not `self_param`'s own field or a `Handle<Thinker>` -- as already-`FixedT`, the identical class of gap `T_PlatRaise`'s own `plat.sector.floorheight` entry already documented, just a different concrete shape never hit before), producing a double-wrapped `FixedT(world[other.unwrap()].floorheight)` that fails to compile. Fixed with a new, narrow arm in `expr_is_fixed_t_valued` recognizing `floorheight`/`ceilingheight` (`Sector`'s own two corpus-verified `FixedT` fields, `r_defs.h`) through a `SectorId`/`Option<SectorId>`-registered base -- hand-matched by field name, the same discipline `sides[i].sector`/`line->frontsector` already established, not a general struct-field-type registry. Full suite re-run after the change (439/439, no regression) since this generalizes existing logic, not just adds a narrow arm nothing else could hit. **`P_FindLowestCeilingSurrounding`/`P_FindHighestCeilingSurrounding` (same file, same shape) deliberately NOT translated**: both initialize `fixed_t height` straight from an opaque macro (`MAXINT`) or bare literal (`0`) with no cross-ref/`FRACUNIT` involvement at all -- `render_decl` has no wrap for a plain local's own declaration initializer (only a self-struct-field *write* gets the `FixedT(..)` wrap), so the local's Rust type gets fixed by that initializer and then conflicts with the loop's own real `FixedT` reassignment/the function's own `FixedT` return type; confirmed a real `E0308` by direct minimal reproduction, not assumed. Left as a precisely-scoped follow-up (an initializer-wrap for `fixed_t`-declared locals, mirroring the write-side wrap but for `render_decl`). Verified compiling for real (`rustc --edition 2021 --crate-type lib`) against a hand-written `Sector`/`Line`/`World`/`FixedT`/stub-`getNextSector` stand-in for both translated functions -- zero errors. `test_p_find_lowest_floor_surrounding_renders_exactly`, `test_p_find_highest_floor_surrounding_renders_exactly`. 439/439 tests passing.

**`P_FindLowestCeilingSurrounding`/`P_FindHighestCeilingSurrounding` (`p_spec.c`) translated too, closing the follow-up the entry above just opened**: `render_decl` now wraps a `fixed_t`-declared local's own declaration initializer in `FixedT(..)` whenever it isn't already `expr_is_fixed_t_valued` (an opaque macro like `MAXINT`, or a bare integer literal) -- guarded the identical way the pre-existing self-struct-field-*write* wrap already is, so an initializer that's already genuinely `FixedT`-valued (`sec->floorheight`, `-500*FRACUNIT`, the previous entry's own two functions) is never double-wrapped, confirmed by re-running those two functions' own tests unchanged. Full suite re-run after the change (441/441, no regression) since this generalizes `render_decl` itself, the same "measure, don't just add a narrow arm" discipline the `expr_is_fixed_t_valued` fix just above also followed. Otherwise wholly identical in shape to `P_FindLowestFloorSurrounding`/`P_FindHighestFloorSurrounding`, needing nothing else new. Verified compiling for real (`rustc --edition 2021 --crate-type lib`) against a hand-written `Sector`/`Line`/`World`/`FixedT`/stub-`getNextSector` stand-in for both -- zero errors. `test_p_find_lowest_ceiling_surrounding_renders_exactly`, `test_p_find_highest_ceiling_surrounding_renders_exactly`. 441/441 tests passing.

**`P_BringUpWeapon` (`p_pspr.c`) translated -- the array-of-struct-typed self field a previous round flagged as a genuinely new mechanism worth attempting**: `player->psprites[ps_weapon].sy = WEAPONBOTTOM;` (`psprites: pspdef_t[NUMPSPRITES]`, `p_pspr.h`) turned out to need only one small, narrow addition once actually read against the real renderer, not a new mechanism: `psprites` joins the existing by-name self-array-field index-cast list (`powers`/`lines`/`cards`, `pw_strength`'s own precedent) so `ps_weapon` -- a bare `psprnum_t` enum-constant identifier, fixed `i32` elsewhere in the generated crate, not a fresh local Rust could infer as `usize` here -- gets its own explicit cast. The *write* itself (`self.psprites[ps_weapon as usize].sy = WEAPONBOTTOM;`) needed no new rendering code at all: unlike a *direct* self field, it's nested through an `Index`, so none of the existing self-field-write special cases match and it falls straight through to the wholly generic assignment fallback -- already correct, the same way `sides[i].toptexture = ..;`-shaped writes already are. `WEAPONBOTTOM` (`p_pspr.c`'s own file-scope `#define WEAPONBOTTOM 128*FRACUNIT`) renders as a bare opaque identifier, the same `CEILSPEED` precedent (a `#define`d alias this parser never macro-expands, `T_MoveCeiling`'s own entry): no wrap fires for it (its RHS is neither a self-field write nor an `Expr::IntLiteral`), so it passes through unwrapped, correct provided the eventual generated crate gives it a real `FixedT` type of its own, the same deferred responsibility `CEILSPEED` already carries. Reading `player->mo` in `S_StartSound(player->mo, sfx_sawup)` reuses the already-proven "registered `Handle<Thinker>` self field read as a plain opaque value" case (`A_FireShotgun2`'s own entry). Verified compiling for real (`rustc --edition 2021 --crate-type lib`) against a hand-written `Player`/`World`/`Handle`/`FixedT` stand-in and a stub `S_StartSound`/`P_SetPsprite`/`weaponinfo`/`WEAPONBOTTOM` -- zero errors. `test_p_bring_up_weapon_renders_exactly`. 442/442 tests passing.

**`P_FireWeapon` (`p_pspr.c`) translated -- an ordinary `Player`-shaped self-struct function, needing exactly one new piece**: `P_CheckAmmo` (this same file's own sibling, its real declared C return type confirmed `boolean` by direct read) joins the by-name `is_bool_returning_call` allowlist (`P_ThingHeightClip`'s own precedent) so `if (!P_CheckAmmo(player)) return;` needs no `!= 0`/`== 0` truthiness cast, bare or negated. Everything else reuses wholly pre-existing machinery: `P_SetMobjState(player->mo, S_PLAY_ATK1)` reuses the already-proven "registered `Handle<Thinker>` self field read as a plain opaque value" case (`player.mo` passed by value with no further dereference); `weaponinfo[player->readyweapon].atkstate` reuses `P_DropWeapon`'s own already-established `weaponinfo[..].field` access unchanged; `P_NoiseAlert(player->mo, player->mo)` is itself already a real translated function (`P_RecursiveSound`'s own entry), called here as an ordinary forward reference with two plain `Handle<Thinker>` values. Verified compiling for real (`rustc --edition 2021 --crate-type lib`) against a hand-written `Player`/`World`/`Handle` stand-in and a stub `P_CheckAmmo`/`P_SetMobjState`/`P_SetPsprite`/`P_NoiseAlert`/`weaponinfo` -- zero errors. `test_p_fire_weapon_renders_exactly`. 443/443 tests passing.

**`P_BulletSlope` (`p_pspr.c`) translated -- an ordinary `Mobj`-shaped self-struct function, plus a real regression found and fixed in an already-shipped test**: `bulletslope` (this file's own file-scope `fixed_t bulletslope;`) joins the by-name `World` global list (`viletryx`/`attackrange`/`swingx`/`swingy`'s own category). `linetarget` (`P_AimLineAttack`'s own "did we hit anything" side channel) reuses the already-established `World` field and negated-truthiness handling (`A_Punch`'s own precedent) unchanged. `an += 1<<26;`/`an -= 2<<26;` need nothing new: `an` is a genuinely `angle_t`-declared local, and the existing `lhs_is_angle_t_local` compound-assign wrap (`A_Chase`'s own precedent) already casts each RHS shift `as u32`. **The regression**: registering `bulletslope` as a `World` field changed how the already-shipped `A_FireShotgun2` (`bulletslope + (...)<<5`) must render too -- from a bare opaque-identifier pass-through (its own original, now-stale rendering, back when nothing had registered `bulletslope` at all) to `world.bulletslope`, caught by the full suite itself failing that exact test, not assumed. Fixed at the actual root: `render_weapon_fn`'s own `world` parameter was wired to fire only on `needs_linetarget`, with no path for a *different* by-name `World` global to need it -- widened to `needs_world = needs_linetarget || needs_bulletslope` (`body_has_any_ident_ref`), and `A_FireShotgun2`'s own expected test output updated to the more broadly-correct rendering (a real second parameter, `world: &mut World`, plus `world.bulletslope` in its own body) rather than narrowing `P_BulletSlope`'s own registration to dodge the conflict. Verified compiling for real (`rustc --edition 2021 --crate-type lib`) against a hand-written `Mobj`/`World`/`FixedT`/`Handle`/`Arena`/`Thinker` stand-in and a stub `P_AimLineAttack` -- zero errors. `test_p_bullet_slope_renders_exactly`. 444/444 tests passing.

**`P_SetupPsprites` (`p_pspr.c`) translated -- an ordinary `Player`-shaped self-struct function, needing no new mechanism at all**: `player->psprites[i].state = NULL;` reuses `psprites`'s own by-name `as usize` index-cast rule (`P_BringUpWeapon`'s own entry) unchanged -- that rule fires on *any* index shape into `psprites`, a bare loop counter included, not just an enum-constant one -- and `Expr::Ident("NULL")`'s existing generic `-> None` rendering (`getNextSector`'s own precedent) covers the write with nothing new either. `player->pendingweapon = player->readyweapon;` reuses `P_BringUpWeapon`'s own already-proven plain self-field write, and `P_BringUpWeapon(player);` is itself already a real translated function (this same round's own earlier entry), called here as an ordinary forward reference. Verified compiling for real (`rustc --edition 2021 --crate-type lib`) against a hand-written `Player`/`PlayerSpriteState` stand-in and a stub `P_BringUpWeapon` -- zero errors. `test_p_setup_psprites_renders_exactly`. 445/445 tests passing.

Not yet done (updated after the entry above -- `P_RemoveMobj` is translated, along with the `itemrespawnque`/`itemrespawntime`/`iquehead`/`iquetail` globals and the cast-shaped self-removal call it needed, so this is removed from the list below; `PIT_ChangeSector` is translated, along with the `crushchange`/`nofit` globals and `P_ThingHeightClip`-as-bool-returning-call registration it needed, so this is removed from the list below; `P_FindSectorFromLineTag`/`getNextSector` are translated, along with the `Stmt::Return`/`Option`-wrap mechanism, bare `sectors[i]`, and `numsectors` global it needed, so this is removed from the list below; `P_ExplodeMissile`/`P_CheckMissileSpawn`/`P_TryWalk`/`P_CheckMissileRange`/`P_AproxDistance`/`P_AddActivePlat`/`P_AddActiveCeiling` are translated, along with the N-ary chained-assignment flattening and self-struct `FixedT`-literal write wrap it needed, so both are removed from the list below, and the "the *other* ~67 tick/`A_*` function bodies" line further down is corrected: a corpus-wide sweep this round found every real `state_t.action` entry except the two permanently-blocked `A_KeenDie`/`A_BossDeath` was already translated by the time this round started, so the real remaining ~66 functions are non-`A_*` tick/trigger/helper functions (`P_*`/`T_*`/`EV_*`) across `p_enemy.c`/`p_mobj.c`/`p_maputl.c`/`p_plats.c`/`p_ceilng.c`/`p_floor.c`/`p_tick.c`/`p_switch.c`/`p_lights.c`, not more `A_*` siblings -- `P_ExplodeMissile` above is the first of this new batch): `A_BrainAwake` is translated, along with the for-loop-shaped thinker scan and handle-yielding iteration it needed, so both are removed from the list below; `A_KeenDie`'s own remaining blocker is restated to drop the now-solved for-loop-scan half of it): and the blockmap-callback machinery this list had assumed they needed turned out unnecessary -- `P_BlockThingsIterator`/`P_CheckPosition` are just two more opaque forward-referenced calls under this project's own existing convention, so that bullet is removed below; the `while (x--)` idiom note it carried stays, since `A_VileChase`'s own scan turned out to be `for`-shaped, not that idiom -- its real home (`p_map.c`/`p_maputl.c`) is untouched by this round): a `static` function-local and the `!easy`-in-a-`&&`-chain truthiness gap are both resolved, `World` gains `braintargets`/`numbraintargets`/`braintargeton` (writable, not just readable -- the first bare-global *write* this renderer has ever done) and `a_brain_spit_easy`, so all of that is removed from the list below; `A_BrainAwake`'s own remaining needs are restated more precisely now that its actual C source (not just a guess from this list's own earlier wording) has been read directly): `render_for`'s bare-`continue` limitation is resolved, and `render_fn_impl` now registers `linetarget` the same way `render_weapon_fn` already did, so all three are removed from the list below): `T_PlatRaise` is translated; read-only `Arena<Thinker>` iteration is designed and implemented, and (once `render_fn_with_scalar_param`/the decl-skip fix/`mobjinfo` renaming closed the remaining gaps) fully unblocks `A_PainShootSkull` end-to-end, though not any of its four siblings, each still blocked by its own separate gap; `linetarget` global read access plus self-handle-field writes are designed and implemented, translating `A_Punch`; `goto`/label support is designed and implemented, translating `A_Look`; `render_while`'s general case was surveyed and found not to need generalizing yet, a documented negative result; `-ANG90/20` is solved, translating `A_Saw`; state-pointer arithmetic is solved via the new `state_index` helper, translating `A_FireCGun` -- also, in the same pass, correcting two stale lines this list had carried since the `A_FirePistol`/`A_FireShotgun` entries closed them without this list ever being updated: the "truthiness-aware negation for a bare function-call argument... blocks `A_FirePistol`/`A_FireShotgun`/`A_FireCGun`" line was wrong the moment `A_FirePistol`'s own entry closed that gap generally, and both named functions have been fully translated for several rounds now; `A_PainShootSkull` itself is now fully translated too, closing the whole family this section's earlier entries opened -- see its own entry below): `A_KeenDie`/`A_BossDeath` need a synthetic stack-local `line_t junk; junk.tag = 666; EV_DoFloor(&junk, lowerFloorToLowest);` built fresh on the C stack and passed *by address* into an already-translated trigger function that searches by tag -- `EV_DoFloor`/`EV_DoDoor` expect a real `LineId` (an index into the level's own `lines[]` `Vec` under this project's memory model), which has no way to represent a fabricated line that isn't part of the level at all; a real design question (a searches-by-tag helper taking a bare `tag: i16` instead of a `LineId`? something else?), not attempted; `A_KeenDie`/`A_BossDeath` share the same for-loop-shaped scan `A_BrainAwake`'s own entry above just closed (`is_thinker_list_scan_for`/`render_thinker_list_scan_for`, now implemented and proven), so that half of their own blocker is resolved -- both remain separately blocked by the `line_t`-by-address design question above regardless, not yet re-investigated beyond that; the new `render_compound_items` goto transform is deliberately scoped to exactly `A_Look`'s own shape (every goto in a label's prefix targets that one label, in the *same* block the label sits in) -- a backward-jumping goto (a loop-like idiom), a label buried in a *nested* block one of the gotos jumps out of, or multiple distinct labels in one function all still fail loudly rather than being guessed at, since no real corpus function needing any of those has been found yet; the `while (x--)` idiom `render_while`'s own survey turned up, still only in `p_map.c`/`p_maputl.c` (not `A_VileChase`, which turned out to be `for`-shaped) -- nothing on this project's near-term path reaches those files yet; `render_while`'s general case is still only the `(x = f()) CMP y` idiom (surveyed this round and found genuinely not yet needed by anything on this project's near-term path -- see its own entry above -- so still deliberately narrow, not a gap to close speculatively); cross-function signature wiring (every call to an already-translated function still uses the *original* C argument list, not the real translated signature -- fine for compiling one function's own output in isolation, as every smoke test so far has done, but real multi-function integration needs this resolved); the *other* ~67 tick/`A_*` function bodies; wiring any of the translated functions into `Thinker::tick`'s real dispatch arms or real call sites (blocked on `p_lights.c`/`p_doors.c`/`p_floor.c`/`p_ceilng.c`/`p_plats.c`'s own module placement, and on disk emission existing at all); actually writing generated output to disk (directory layout, a `Cargo.toml` for the generated crate, ...); every other struct/table in the corpus beyond thinkers, this core geometry cluster, and `MobjInfo`/`State`/`mobjinfo[]`/`states[]` (e.g. `r_defs.h`'s own `drawseg_t`/`vissprite_t`/`visplane_t`/... further down the same file); `World.lines`/`.vertices` are now real and `P_LineOpening` is translated, so the "`World.lines`/`.vertices` don't exist yet" blocker is removed from the list above -- three functions it was flagged as unlocking were investigated directly rather than assumed clean, and each has its own separate, still-open gap: `PIT_CheckLine` (`p_map.c`) additionally needs the still-undesigned indexed-`FixedT`-array global (`tmbbox[BOXTOP]`) and array-of-`LineId` global (`spechit[]`), the same pair already blocking `PIT_CheckThing`/`P_Move`, not resolved by this round's own work; `EV_TurnTagLightsOff` (`p_lights.c`) needs a genuinely new idiom, `sector = sectors; for (j=0;j<numsectors;j++,sector++) { if (sector->tag==line->tag) ... }` -- a comma-operator multi-step `for` header (`render_for_step` only ever handles one step today) walking a raw `sector_t*` in lockstep with its own loop index, which would need recognizing as an aliased `SectorId(j as u32)` rather than a real pointer walk before `sector->lines[i]`/`getNextSector(templine,sector)` can resolve -- a real, not-yet-designed mechanism, not attempted; `EV_LightTurnOn`/`P_PointOnLineSide` (`p_maputl.c`) not yet read in full this round -- `P_PointOnLineSide` was skimmed and found to need its own new gap (a bare comparison directly `return`ed from an `int`-returning, not `boolean`-returning, function -- `return line->dy > 0;` needs a `(..) as i32` wrap at the `Stmt::Return` site, a different context than `render_binary_operand`'s existing arithmetic-operand-only bool-as-`i32` cast), left for a future round to design properly rather than forced in now; `P_ZMovement` is translated, along with self-struct `Option<PlayerId>`-field read/write, `expr_is_fixed_t_valued`'s `fixed_t_locals` recognition, and the self-field-vs-literal `FixedT` comparison wrap it needed, so all three are removed from the list above. **Three more `p_mobj.c` functions investigated this round, deliberately deferred, not forced**: `P_XYMovement` (its own `ceilingline` global -- `p_map.c`'s file-scope `line_t* ceilingline;`, set by the still-untranslated `PIT_CheckLine` -- is genuinely nullable and, once dereferenced (`ceilingline->backsector->ceilingpic`), needs a *two-level* nested `Option` unwrap chain (`Option<LineId>` -> `Line.backsector: Option<SectorId>` -> `.ceilingpic`) this module has no precedent for yet -- every existing chain-through-a-cross-ref arm assumes at most one level of `Option` in the chain); `P_NightmareRespawn` (`R_PointInSubsector`'s own `subsector_t*` return needs a new by-name alias registration analogous to `collect_spawn_mobj_locals`, plus `ANG45 * (mthing->angle/45)` mixes an opaque-macro-assumed-`u32` value with plain-`i32` arithmetic in a `Mul`, the same class of gap `find_u32_self_field_names` already solved for `Sub` but not yet for `Mul`); `EV_BuildStairs` (`p_floor.c`) needs a `do`/`while` loop containing a *second*, independent embedded-constructor invocation reusing the same C variable name (`floor = Z_Malloc(...); P_AddThinker(...); ...` twice, once before the loop and again inside it) -- a genuinely new shape `render_ctor_body`'s current "exactly one construction per function" design has never had to handle, a real architectural question, not a quick extension. None attempted -- each is its own separately-scoped gap, matching this project's own "confirm the real blocker by reading the body, don't force a translation past it" discipline. `P_RecursiveSound`/`P_NoiseAlert` are translated, along with the `validcount`/`soundtarget` globals, the `Sector.lines`/`sides[i].sector` alias-collector extensions, the widened `subsector->sector` chain arm, and `render_world_fn`'s own new conditional `thinkers` parameter, so all of that is removed from the list above. `EV_TurnTagLightsOff` is translated, along with the `sector = sectors; for(...,sector++)` pointer-walk-in-lockstep mechanism (`is_sector_walk_priming`/`is_sector_walk_for`/`FnBodyContext::sector_walk_alias`) and the `getNextSector`-result `Option<SectorId>` alias rule it needed, so that bullet is removed from the list above and restated precisely: `EV_LightTurnOn` (the sibling function using the identical `sector`-walk idiom) remains separately blocked, now on a narrower, newly-identified gap -- its own `bright: i32` parameter is compared against and assigned *from* `Sector.lightlevel: i16` in three places, a real `i16`-vs-`i32` mismatch (unlike a plain *local*, a parameter's Rust type is fixed by its own signature, not freely inferable) this module has no cast mechanism for yet; `P_PointOnLineSide` (`p_maputl.c`) still not yet read in full. `P_FindLowestFloorSurrounding`/`P_FindHighestFloorSurrounding`/`P_FindLowestCeilingSurrounding`/`P_FindHighestCeilingSurrounding` are all translated, along with the cross-reference-chain `FixedT`-recognition fix (`floorheight`/`ceilingheight` through a `SectorId`/`Option<SectorId>` base) and the `fixed_t`-declared-local initializer-wrap fix (`render_decl` now wraps a bare opaque macro or integer literal initializer in `FixedT(..)` when the declared type is `fixed_t` and the initializer isn't already `expr_is_fixed_t_valued`) both needed, so all four are removed from the list above (further reducing "the *other* ~67 tick/`A_*` function bodies" count) -- no new blocker surfaces in their place, `getNextSector`'s whole surrounding-sector-query family (`P_FindMinSurroundingLight` aside) is now fully translated. `P_FindMinSurroundingLight` (`p_spec.c`, the same family's remaining member) is investigated and deliberately deferred, not forced: its own `min`/`max` locals need to be genuinely `i32` (matching the function's own `int`-typed parameter/return), but the loop body also compares/assigns `check->lightlevel` (`Sector.lightlevel: i16`) straight into `min` -- a real `i16`-vs-`i32` mismatch on *both* a comparison and an assignment (not just a read-only comparison, the narrower shape `EV_StopPlat`'s own `tag` fix already covers), needing its own new widening mechanism rather than reusing that one. `P_BringUpWeapon` is translated, along with the `psprites` by-name self-array-field index-cast addition it needed, so this is removed from the list above. `P_FireWeapon` is translated, along with the `P_CheckAmmo`-as-bool-returning-call registration it needed, so this is removed from the list above too. `P_BulletSlope` is translated, along with the `bulletslope` by-name `World` global registration and the `render_weapon_fn` `needs_world` widening (and `A_FireShotgun2` test fix) it needed, so this is removed from the list above as well. `P_SetupPsprites` is translated (needed nothing new), so this is removed from the list above too. **`P_MovePsprites` (`p_pspr.c`, `P_SetupPsprites`'s own sibling) investigated and deliberately deferred, not forced**: `psp = &player->psprites[0]; for (i=0;i<NUMPSPRITES;i++,psp++) { if ((state = psp->state)) { ... } }` is the identical "pointer walk in lockstep with a loop index" idiom `EV_TurnTagLightsOff`'s own `sector = sectors; for(...,sector++)` mechanism (`is_sector_walk_priming`/`is_sector_walk_for`/`FnBodyContext::sector_walk_alias`) already solved -- but for a *self-owned, embedded-by-value* array field (`player.psprites`, indexed directly, no `World`/`Arena` indirection at all) rather than a `World`-indexed global array of index newtypes (`SectorId`), which `sector_walk_alias`'s own design assumes throughout (it resolves every bare reference to `SectorId({counter} as u32)` specifically). A genuinely new variant of that mechanism, not a drop-in reuse -- not attempted this round. **`twoSided`/`getSide`/`getSector` (`p_spec.c`) re-investigated this round, deliberately deferred, not forced**: their own *call-site* usage (as opaque forward-referenced calls from already-translated functions like `EV_DoFloor`) has been fully handled for a while (see this doc's own `EV_DoFloor` entry above), but their *own bodies* -- the actual thing this round's task is about -- were not re-checked until now, and turn out to still be genuinely blocked, not simply stale: all three chain an inline `(sectors[param].lines[param])->field` expression with *no* intermediate local variable (`getNextSector`/`P_RecursiveSound`'s own `check = sec->lines[i]; ... check->field` idiom always goes through a local, which is exactly what `collect_line_sector_aliases` keys on) -- the generic `Expr::Index` fallback always returns `is_crossref: false` for an indexed-array-field read (unlike the special-cased bare `sides[i]`/`sectors[i]` globals), so the immediately-following `Expr::Member` chain (`->sidenum`/`->flags`/`->sector`) has no way to know its own base is `LineId`-typed and never wraps it in `world[...]` -- confirmed by tracing the real AST shape, not assumed. `twoSided` additionally hits the exact same still-open gap `P_PointOnLineSide`'s own entry documents: its real declared return type is `int`, but `(sectors[sector].lines[line])->flags & ML_TWOSIDED` is `i16`-valued (`Line.flags: i16`), needing a `Stmt::Return`-site `as i32` wrap this module still doesn't have. `getSide`/`getSector` return `side_t*`/`sector_t*` (`SideId`/`SectorId` under the existing memory model) built from that same unresolved inline chain, so both share the first gap without needing the second. Neither gap is a design question -- both are real, scoped additions (an inline-chain `is_crossref` propagation for an array-of-cross-ref-typed field read, and the already-known `Stmt::Return` narrowing cast) -- just not attempted this round. `twoSided`/`getSide`/`getSector` are now translated: a new dedicated `Expr::Index` arm recognizes `Sector.lines: Vec<LineId>` indexing specifically and returns `is_crossref: true` (unlike the generic array-field fallback, which assumes a plain, non-cross-reference-typed element), closing the inline-chain gap their own entry above documented; `getSide` additionally needed a `&sides[complex_expr]` arm (mirroring `&sectors[i]`) and a `sidenum[side]` index cast scoped narrowly to a bare-`Ident` index over a `sidenum` base specifically (not blanket-added to the by-name array-field list, which would have silently changed `EV_VerticalDoor`'s own already-shipped `sidenum[side^1]` rendering -- confirmed a real regression by trying the broader version first); `twoSided`'s own `Stmt::Return`-site `i16`-as-`i32` wrap is implemented as `return_expr_needs_i32_cast`, covering both a bitwise-AND-on-`flags` result and a bare comparison result, so this closes both bullets, and all three names are removed from the list above. `P_PointOnLineSide` is translated too, closing its own long-carried "not yet read in full" entry: besides the `Stmt::Return`-site cast just mentioned, it needed `!line->dx`/`!line->dy`'s own truthiness fix (a new `is_line_dx_dy_field` predicate, the `is_self_fixed_t_field`-style "direct field known `FixedT`" check but for a `render_world_fn`-style function with no real `self_param` at all, narrowly scoped to exactly `dx`/`dy` by name) and a real, previously-undiscovered vertex-dereference gap: `Line.v1`/`.v2: VertexId` is a cross-reference index, not an inline value, so `world[line].v1.x` doesn't compile at all (confirmed directly with `rustc`, not assumed) without a second `world[...]` indirection -- a new hand-matched `v1`/`v2` arm (mirroring the existing `frontsector`/`backsector` arms) closes it, so the "vertex-dereference arm not yet modeled" phrasing is removed from the list above. `P_FindMinSurroundingLight` is translated as well, closing the deferred entry above: the needed widening mechanism is `collect_i32_widened_locals` (mirroring `collect_line_sector_aliases`'s own "scan once, extend `extra_cross_ref_idents`" shape), registering a local as genuinely `"i32"`-typed only when it's assigned somewhere from an independently-`i32` identifier (a parameter) -- distinguishing `min = max;` (needs widening) from `EV_TurnTagLightsOff`'s identically-shaped `min = sector->lightlevel;` (needs none, since Rust's deferred-`let` inference already settles it as `i16` for free) by the one real structural difference between them, confirmed necessary by hitting a real regression against that already-shipped test when a broader `plain_int_locals`-only check was tried first. The comparison-widening arm also needed its cast parenthesized (`(x as i32) < y`, not `x as i32 < y`) -- confirmed a real `rustc` parse rejection ("`<` is interpreted as a start of generic arguments"), not assumed. `EV_LightTurnOn` is translated too, closing round 6's own "narrower, newly-identified gap" entry: it reuses `EV_TurnTagLightsOff`'s `sector`-walk mechanism verbatim, and all three of its own `lightlevel`-vs-`bright` mismatch sites render correctly with *zero* further renderer changes beyond what `P_FindMinSurroundingLight` just added -- `bright` is a genuine function parameter, already registered `"i32"` in `extra_cross_ref_idents` directly from `render_world_fn`'s own existing `param_types.clone()` (no `collect_i32_widened_locals` pre-scan needed, unlike a local), so the read-direction comparison/assignment (the identical shape `P_FindMinSurroundingLight` proved) and the reverse-direction narrowing write (`sector->lightlevel = bright;` -> `lhs_is_lightlevel_field && rhs_is_plain_i32_ident -> as i16`, proven for the first time here) both just worked on the first real attempt. **`P_MovePsprites` (`p_pspr.c`) remains the one candidate from round 6's list still separately blocked**, unaffected by any of this round's work: its own "pointer walk in lockstep with a loop index" idiom needs a genuinely new variant of `sector_walk_alias` for a *self-owned, embedded-by-value* array field (`player.psprites`), not a `World`-indexed global array of index newtypes -- see its own entry above, not re-investigated this round. `P_MovePsprites` is translated, closing out this round's own long-deferred entry: the self-owned-array-field pointer-walk variant is `is_psprite_walk_priming`/`is_psprite_walk_for`/`FnBodyContext::psprite_walk_alias`, `sector_walk_alias`'s own sibling but resolving a bare pointer-local reference directly to the full indexed self-field expression (`player.psprites[i as usize]`, not crossref-flagged, so the existing generic `Expr::Member` fallback appends `.field` with no `world[..]` wrap needed) rather than to an index newtype, and bounded by the fixed `NUMPSPRITES` enum constant rather than the runtime `numsectors` global. The assignment-as-if-condition idiom (`if ((state = psp->state))`) needed a genuinely new `render_condition` arm (no earlier `if` in this corpus had a bare `Expr::Assign` as its whole condition): hoists the assignment as its own statement immediately before the `if`, then tests the now-plain local via a new narrow `is_option_valued` arm keyed off `psprite_walk_alias` (recognizing `psp->state`'s own real `pspdef_t.state` nullability, `p_pspr.h`'s "a NULL state means not active"). A third, smaller gap surfaced only once the whole body was read, not predicted by round 6's own scoping: `psp->state->nextstate` chains a field read straight through that same `Option<&'static State>` value (unlike `state_index`'s own non-`Option` `&'static State` precedent, `A_FireCGun`'s idiom) -- closed by a new `Expr::Member` arm scoped narrowly to exactly the `psp->state->field` shape (not the broader `is_option_valued(base, ctx)` predicate, which also matches several already-handled shapes -- `mo->player->field`, `actor->target->field`, ... -- each with its own differently-shaped, already-ordered-earlier rendering; the guard is kept narrow rather than relying on match-order precedence to stay correct) that inserts the `.unwrap()` a `state_t*` dereference needs, always genuinely `Some` here since the real corpus call site already guards the whole block with the `if ((state = psp->state))` just discussed. `player->psprites[ps_flash].sx = player->psprites[ps_weapon].sx;` (and its `.sy` sibling) needed nothing new, reusing the already-established `psprites`-by-name index-cast rule (`P_BringUpWeapon`'s own precedent) verbatim for any bare enum-constant index. Verified compiling for real (`rustc --edition 2021 --crate-type lib`) against a hand-written `Player`/`PlayerSpriteState`/`State`/`STATES` stand-in and a stub `P_SetPsprite` -- zero errors. **`P_PlayerInSpecialSector` (`p_spec.c`) investigated this round, deliberately deferred, not forced**: `sector = player->mo->subsector->sector;` needs a *three*-level chain starting from a self-struct field itself `Handle<Thinker>`-typed (`player.mo`), not through `self_param` directly the way every existing `subsector->sector` arm assumes (`A_Look`'s own `actor->subsector->sector`, `P_NoiseAlert`'s widened `emmiter->subsector->sector`) -- reading `.subsector` off `player->mo` first needs a genuine `Arena` lookup (`match thinkers.get(player.mo) { Some(Thinker::Mobj(m)) => m.subsector, .. }`, the same shape `thing->player`'s own dedicated arm already uses, but for a different field), which no existing arm produces since that one is hand-matched to field `"player"` specifically, not `"subsector"`. Once that lookup exists, `line_sector_field_alias_type` (the `collect_line_sector_aliases` pre-scan behind the `sector = ...;` local-alias registration this function's own body needs) would also need widening to recognize the new three-level shape as a `"SectorId"` source. Two real, plausibly-small, but *separate* additions (a new `Expr::Member` arm generalizing the `.player`-specific `Arena`-lookup shape to `.subsector`, plus the alias-collector widening), not attempted this round for lack of remaining budget to verify both with the same rigor (real corpus test, `rustc` compile-check) as this round's other work -- left as a precisely-scoped starting point for a future round. `switch`/`case`/`default` rendering itself (`render_switch`) is already proven elsewhere and is *not* part of this function's own blocker. `P_PlayerInSpecialSector` is translated, closing out this round's own precisely-scoped candidate: both additions this entry left open landed exactly as scoped -- the existing `.subsector->sector` `Expr::Member` arm (`A_Look`'s own `actor->subsector->sector`, already widened once for `P_NoiseAlert`'s bare-`Handle<Thinker>`-parameter case) gains a *third* base shape, `is_self_bare_handle_field(bb, ..)` (`bb` here is `player->mo` itself, a `Member`, not a bare `Ident` either of the two existing guards matched), and `line_sector_field_alias_type` gains a matching `.mo.subsector.sector` arm so the local (`sector`) itself is registered `"SectorId"`, not just the standalone expression rendering correctly in isolation -- `collect_line_sector_aliases` also needed threading into `render_fn_impl_with_two_scalar_params` for the first time (every earlier `render_fn` caller only ever needed `target`/`tracer`/spawned-mobj aliases, `render_world_fn`'s own equivalent merge was the only precedent). Compiling the whole body for real surfaced two further, genuinely new gaps neither addition predicted: a bare `;` (C's own empty statement, here trailing the `switch`'s closing brace, `};`) had no `render_stmt` arm at all, rejected outright rather than silently mishandled -- now a no-op, rendering as literally nothing. And C's adjacent-string-literal concatenation (`I_Error`'s own two-line message, `"P_PlayerInSpecialSector: "` directly followed by `"unknown special %i"`) turned out to have been *wrong* since the parser's very first corpus-wide pass: `parse_primary_expr`'s merge loop naively concatenated each token's own already-quoted raw text (`"a""b"`, two adjacent Rust string literals side by side -- not valid Rust syntax at all, confirmed by a real `rustc` rejection) instead of merging their *content* into one real quoted string (`"ab"`) -- fixed at the parser level, with the two existing tests that had encoded the old (wrong) behavior as their own "expected" value corrected to the real one (undetected for this long simply because no earlier corpus function actually needed a multi-line message). A third, smaller gap: `!player->powers[pw_ironfeet]` used as one operand of a `||` chain (`if (!player->powers[pw_ironfeet] || (P_Random()<5))`) needed its own new `render_binary_operand` arm -- the existing `not_of_known_int_field` check only ever matched a bare `Expr::Member` operand, not an `Expr::Index` one, so this fell through to the generic `Expr::Unary` fallback's plain (bitwise) `!`, a real `rustc` type error (`expected bool, found i32`) confirmed directly, not guessed at; scoped narrowly to exactly `powers` by name (`cards`'s own identically-shaped array is genuinely `bool`-valued and already correct as plain `!`, a real, distinct corpus example, not a hypothetical). Everything else in the body -- the `FixedT` `z`/`floorheight` comparison, `world[sector].special` read *and* write through the new alias (the write needed no dedicated arm at all, falling straight through the generic `Expr::Assign` fallback), `render_switch`'s already-general fallthrough (`case 16: case 4: ...`)/`default:` handling, a two-argument `I_Error` call (the existing `Expr::Call` arm already renders an arbitrary argument list generically -- only a *single*-string-argument call had a real precedent before now), `player->cheats &= ~CF_GODMODE;` (`UnaryOp::BitNot`'s already-generic rendering as Rust's own bitwise `!`, `thing->flags &= ~MF_SOLID;`'s own precedent, just through a plain self field instead of a `Handle<Thinker>` one), and `player->secretcount++;`/`player->health <= 10`/`G_ExitLevel()` -- composed from wholly pre-existing generic machinery with no further changes. Verified compiling for real (`rustc --edition 2021 --crate-type lib`) against a hand-written `Player`/`World`/`Sector`/`Mobj`/`Thinker`/`Arena`/`Handle` stand-in -- zero errors. `P_GunShot` (`p_pspr.c`, `A_FirePistol`'s own forward-referenced callee, found by this round's own corpus-wide sweep once the explicit candidate list ran dry) is translated too, needing only one small new piece: a scalar parameter declared `bool` in Rust (`accurate`, `render_fn_with_scalar_param`'s own `scalar_param_type`) had no `bool_locals` registration at all, so `!accurate` rendered through the generic *int*-truthiness fallback as `accurate == 0` -- a real `rustc` type error (`bool` compared to `{integer}`), not just a style nit, confirmed directly. Fixed by registering a `bool`-typed `scalar_param`/`scalar_param2` name into `bool_locals` the same way a `boolean`-declared *local* already is. Everything else in the body -- `angle`'s own `u32`-local compound-assign cast (`A_FaceTarget`'s exact precedent, now reached through `collect_angle_t_locals` rather than a self field), `bulletslope` (`world.bulletslope`, unconditionally in scope since `render_fn_impl` always threads `world` through, unlike `render_weapon_fn`'s own conditional version of the same field), and a plain opaque forward-referenced call (`P_LineAttack`, itself not yet translated) -- composed from wholly pre-existing machinery with no further changes. Verified compiling for real the same way, against a hand-written `Mobj`/`World` stand-in and a stub `P_LineAttack` -- zero errors. A corpus-wide sweep of every `p_*.c` function this round (226 real corpus functions total, 145 already translated before this round's own two closures, 81 remaining after) found no *other* well-scoped candidate: `p_switch.c`'s `P_ChangeSwitchTexture` (an `EV_Do*`-family opaque callee, like `P_GunShot`) turns out to need two genuinely new globals (a runtime-sized `switchlist[]` table keyed by `numswitches*2`, and `buttonlist`, a linked-list-head global unlike any array/table this project has modeled yet) plus its own still-untranslated sibling `P_StartButton`, not a small addition; `P_CheckAmmo` (`p_pspr.c`, already registered as an opaque `is_bool_returning_call`) needs a `do`/`while` loop with no existing `render_stmt` arm at all (`Stmt::DoWhile` is only ever scanned by the various `body_has_*` predicates, never actually rendered) *and* a local (`ammo`) indexing a self-array-field while also being compared against an enum constant, the same real `int`-vs-index-type ambiguity this module's `plain_int_locals`/`angle_t_locals`/etc. machinery exists to resolve case-by-case, not yet decided for this new shape; `p_tick.c`'s thinker-list primitives (`P_AddThinker`/`P_RemoveThinker`/`P_RunThinkers`/...) manipulate a raw intrusive linked list this project's own `Arena<Thinker>` has already replaced outright, so translating their C bodies verbatim doesn't apply to this project's memory model at all, not merely deferred. Every other remaining function falls under an already-documented blocked category above (`tmbbox`/`spechit`, the blockmap subsystem, real `Deref`, `A_KeenDie`/`A_BossDeath`, `P_XYMovement`, `P_NightmareRespawn`, `EV_BuildStairs`) or is a large, multi-piece setup/archive routine (`p_setup.c`/`p_saveg.c`, loading/serializing level data -- a different, not-yet-scoped category of work, `P_SpawnMobj`/`P_SpawnMissile`/etc.'s own real constructor bodies among them) outside this round's own scope. `P_CheckAmmo` is translated, closing out this round's own two-part blocker exactly as scoped: `Stmt::DoWhile` gains its own `render_stmt` arm (`render_do_while`), `render_while`'s own twin, just reusing the generic `render_condition` entry point (`Stmt::If`'s own) rather than `render_while`'s narrower `(x = f()) CMP y`-only test, since a real `do`/`while`'s own condition here is a plain comparison with no embedded assignment; and the `ammo`-local int-vs-index-type ambiguity resolves by joining `ammo`/`weaponowned` to the existing by-name `as usize` index-cast list (`powers`/`cards`/`psprites`/`sidenum`), the same "hand-match the one real array" discipline already established, even though `ammo`'s own index shape (a fresh local, not an enum constant or struct field) superficially looks like `sidenum`'s already-safe uncast case -- the real tiebreaker is that `ammo` is *also* compared against a bare enum constant (`am_noammo`) elsewhere in the same function, which fixes its real type at `i32` and would conflict with a `usize` inference from the indexing site (confirmed a real `rustc` E0308 by trying the uncast version first, not assumed). A third, previously-unexercised gap surfaced only once the whole body was read: a bare (non-negated) `Expr::Index` operand of a `&&`/`||` chain (`player->weaponowned[wp_plasma] && ...`, `player->ammo[am_cell] && ...`) had no truthiness handling at all in `render_binary_operand`'s own operand-routing logic (only the *negated* `powers` case and an *`Option`-valued* index were routed to `render_bool_expr` before this) -- fixed generically rather than by another narrow by-name check, since a bare `Expr::Member` operand was already routed unconditionally: a bare `Expr::Index` operand now is too, letting `render_bool_expr`'s own already-correct match (`Option`-valued -> `.is_some()`, `cards`/now `weaponowned` -> already-`bool`-no-cast, everything else -> `!= 0`) do the real work with no new by-name scoping needed at that second site. Verified compiling for real (`rustc --edition 2021 --crate-type lib`) against a hand-written `Player`/`WeaponInfo`/`World` stand-in -- zero errors. `test_p_check_ammo_renders_exactly`. 455/455 tests passing. `P_ChangeSwitchTexture`/`P_StartButton` (`p_switch.c`) remain exactly as scoped by the previous round's own entry above: a runtime-sized `switchlist[]` table and a `buttonlist` linked-list-head global, neither modeled by this project yet, not attempted this round either.
- **Module Structure (`.c`/`.h` -> Rust module)**:
  - Each `.c`/`.h` pair (e.g. `p_map.c` + `p_map.h`) merges into one Rust module. Items declared in the `.h` become `pub`; items that only exist in the `.c` stay private. This mirrors Rust's own visibility model and the original C author's intent -- the header already *is* their declaration of "here's my public API."
  - **Headers with no matching `.c`** (pure declaration headers: shared typedefs/structs/macros, no implementation) become their own module, with everything `pub`. Confirmed via corpus scan: 13 such headers exist -- `doomtype.h`, `doomdata.h`, `d_player.h`, `d_think.h`, `d_ticcmd.h`, `d_event.h`, `d_textur.h`, `d_englsh.h`, `d_french.h`, `p_local.h`, `r_defs.h`, `r_local.h`, `r_state.h`. **Implemented**: `transpiler/src/codegen/modules.rs` (`build_module_graph`) enumerates all 75 modules (62 Source, one per `.c` file, plus these 13 HeaderOnly) and, for every Source module, which of the 13 it needs a `pub use header_only_module::*;` from -- following its own `#include` graph *transitively*, the same closure `ExportResolver`/`DeclaredTypesResolver`/`ImportResolver` already walk for their own purposes, reused here rather than reimplemented. 55 of the 62 Source modules need at least one; `doomtype` alone is needed by 53. A glob is safe here because everything a header-only header declares really is defined in that Rust module -- one line regardless of how much it covers, so "declared, not used" (mirroring C's own `#include` semantics) costs nothing.
  - **Implemented -- `.c`-to-`.c` exact-path imports**: a header (of either kind) can `extern`-declare a function/global actually *defined* in a different `.c` file -- `p_local.h` declares `P_TryMove`, but the item lives in the `p_map` module, not `p_local`'s, so no glob of `p_local` would ever surface it. `build_module_graph`'s `source_symbol_imports` resolves these to an exact `use owner::name;` per symbol, built from two existing pieces: `resolve_module_visibility`'s public sets, unioned corpus-wide into a `name -> defining module` map (unique per C's one-definition rule for external linkage); and `typecheck::resolve`'s Step 1 symbol resolver, run *unseeded* per file, whose `unresolved` identifiers are exactly the ones that trace to some other module. This tier is deliberately *usage-pruned*, unlike the header-only glob: measured against the corpus, "declared, not used" here would have meant `p_map.c` (player movement) picking up exact-path imports for symbols from `r_main`/`r_draw` (rendering) it never calls -- 13,709 exact-path imports corpus-wide, ~249 per importing module on average, mostly dead. Pruned to actual references: 1206 total, 442 module-pair edges, 50 of 62 Source modules need at least one. The one pattern the resolver can't distinguish -- `RawDeclarationIndex`'s bare top-level redeclaration with no header at all (e.g. `m_misc.c`'s own `extern int key_right;`) -- is resolved locally by the file's own redeclaration and would never show up unresolved even when used, so its mere existence is treated as need instead; narrow enough (2 corpus-wide symbols) not to chase further.
  - **Implemented -- rendering the `use` block**: `transpiler/src/codegen/use_stmt.rs` (`render_use_block`) turns the two import maps above into the actual Rust text a module's own file needs at its top: one `pub use crate::header_only::*;` line per header-only import (sorted), then one grouped `use crate::owner::{a, b, ...};` line per exact-path owner (owners and the names within each line both sorted, since the underlying maps are hash-based and iterate in unspecified order -- generated source needs to be deterministic regardless). Exact-path lines are deliberately plain `use`, not `pub use`: they're a module's own internal calls into another's public API, not something it means to re-export. `p_map`'s real rendered block, for example, globs in its 10 needed header-only modules and pulls precise symbol lists from 15 different Source modules (`p_maputl`, `p_mobj`, `r_main`, ...), nothing from unrelated subsystems like `r_draw`.
  - **Implemented -- the crate module tree**: `transpiler/src/codegen/mod_tree.rs` (`render_mod_declarations`) renders the other half of the boilerplate `use_stmt.rs` doesn't cover -- a `pub mod name;` line for every one of the 75 modules, sorted. `pub`, not private: `use_stmt.rs`'s own output reaches every module by a `crate::name::...` path, which needs the module itself publicly reachable from the crate root regardless of what `resolve_module_visibility` decided about its individual items.
  - **Implemented -- true `extern` linkage vs. "declared in the matching header"**: "declared in this file's own `.h` => `pub`, else private" is a heuristic that follows C *convention*, not C's actual linkage rules. Measured with `transpiler/examples/extern_linkage_survey.rs` (see `docs/KNOWN_LIMITATIONS.md`): of 1360 externally-linked definitions corpus-wide, only 25% were both declared in their own matching header *and* genuinely visible to other files only through it. `transpiler/src/codegen/visibility.rs` (`resolve_module_visibility`) replaces it with four combined signals: (1) declared by any header reachable through the defining `.c` file's own `#include` graph (`ExportResolver::resolve_via_includes`, not just the name-matching header -- covers `doomstat.h`-style centralization and the 13 no-matching-header `.c` files, whose symbols attach to whichever shared header they actually include, e.g. `p_local.h`); (2) declared in the file's own matching header even when that file never `#include`s it itself (`r_bsp.c` never includes `r_bsp.h`, yet `r_bsp.h`'s declarations are still real); (3) redeclared as a bare prototype/`extern` directly at another `.c` file's own top level, bypassing headers on both ends (`RawDeclarationIndex`); private otherwise. Cross-checked against the survey's independent "used elsewhere" ground truth: 2 of 1360 definitions remain wrongly private (`rndindex`, `skyflatnum` -- both declared only in `doomstat.h`, which their defining files have no `#include` relationship to at all), left undone as not worth more machinery for a two-symbol gap.

---

## 🔷 Target 2: .NET / C# Transpiler

### 1. Architectural Choices
- Generate C# structs / classes.
- Use `unsafe` blocks for low-level memory and buffer manipulation if required, or translate to safe managed arrays / `Span<T>`.
- Map global state to static or singleton manager classes.

---

## 🧪 Validation & Execution Strategy
1. **Compilation Validation**: Ensure generated Rust code compiles cleanly via `cargo check` and `cargo build`. **First real exercise of this, and it passed**: every piece of Struct-translation output built so far (the `runtime` module's real source files verbatim, `p_spec`'s 8 thinker structs, `r_defs`'s 8 geometry structs, `doomdata`'s `MapThing`, `p_mobj`'s `Mobj`, `p_tick`'s `Thinker` enum + dispatch stub, each with `type_placement.rs`'s own computed `use` imports) was assembled into one real multi-module Rust source file and compiled with `rustc --edition 2021 --crate-type lib` directly -- a smoke test, not a permanent artifact (the assembly script and its output weren't committed). Result: **zero errors, zero warnings**, except the two already-documented, deliberately-deferred `MobjInfo`/`State` references (`Mobj.info`/`.state`, stubbed out with trivial placeholder types purely to isolate them) -- confirming the module placement, import computation, field-type mapping, and arena/runtime types all genuinely integrate correctly with each other, not just pass in isolation.
2. **Deterministic Output Comparison**: Compare game tick execution and fixed-point math against native Doom to verify behavior equivalence.
